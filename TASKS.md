# Tasks — Implementation Milestones

Breaks the implementation into milestones in the exact order confirmed by Mohammed (2026-07-30): multi-tenant schema → authentication → roles and permissions → organizations → properties → units → owners → tenants → applications → leases → documents → OCR → maintenance → accounting → notifications → email → WhatsApp → AI → Super Admin → responsive web → native iOS → native Android → automated testing → deployment → engineering requirements. (Supersedes this file's previous, more coarsely-grouped 16-milestone structure — reordered 2026-07-30 per restated instruction, not a re-derivation from scratch; see `DECISIONS.md`.)

Each milestone is scoped so the repository **compiles, passes its tests, has updated docs, and is deployable** at the end of it. Status legend: `[ ]` not started, `[~]` in progress, `[x]` done. Updated at the end of every milestone, not just at the end of a session.

---

## M0 — Safety checkpoint

- [x] `pre-propertyvault-pivot` backup branch created at the last committed PropVault-era commit (non-destructive branch pointer, no commit made).
- [~] Pre-existing uncommitted working-tree changes (`.github/`, error boundaries, `components/tables/`, `reference/`, noted at session start) remain uncommitted and untouched — not silently discarded, not committed without Mohammed's explicit ask.

## M1 — Multi-tenant schema

- [x] Migrations: `organizations`, `organization_members`, `organization_invites`, `plans`, `organization_subscriptions`, `subscription_payments`, `support_access_sessions` (`DATABASE.md` §1-2).
- [x] `units`, `owners`, `property_owners` (org-scoped from day one) + nullable `properties.org_id` "expand" step (`DATABASE.md` §3).
- [~] `properties.owner_user_id` → `org_id` "contract" step done for `properties` (M5, relaxed not dropped — see M5's own entry); `documents`/`bills`/`payments`/`payment_matches`/`extraction_jobs`/`subscriptions`/`audit_events` (`EXISTING_CODEBASE_AUDIT.md` §2) not yet cut over.
- [x] **All 26 migrations verified against a genuinely clean local Postgres database** (`supabase start`/`supabase db reset`, 2026-07-30, re-verified 2026-07-31 after two more migrations). 2026-07-30 pass found/fixed 4 real bugs: a forward-reference (policy in migration 17 needing a table from migration 18), a DROP COLUMN blocked by cross-table RLS dependencies (migration 23), an infinite-recursion RLS policy on `organization_members`, and a project-wide missing-`GRANT`s gap (migration 24). 2026-07-31 pass found/fixed 2 more: `organizations.status`'s documented `archived` enum value had never actually been added (migration 25), and `organization_invites` had no INSERT policy at all — the invite-creation half of the invitations flow could never have worked end-to-end (migration 26). Full detail: `WORKLOG.md` 2026-07-30/31, `RISK_REGISTER.md` R-02.
- [x] RLS isolation tests **executed and passing**: 31/31 pgTAP assertions across `rls_isolation.test.sql` (4), `multi_tenant_isolation.test.sql` (13), and new `multi_tenant_foundation_integration.test.sql` (14, 2026-07-31 — walks the full create-org→invite→accept→role-gated-write→multi-org-switch journey in one file, per Mohammed's "treat M1-M5 as one integrated subsystem" instruction) — cross-org isolation, role-scoped write denial, platform-admin/support-session table isolation, and full invitation-flow correctness all confirmed for real. Also found real bugs in the _tests themselves_ while first running them (invited user can't self-query their own pending invite before joining — correct RLS behavior, wrong test assumption; direct `organization_members` insert has no client policy — also correct, wrong test assumption) — both fixed to match how the real flow actually works.
- [x] **Known, deliberately unenforced gap found 2026-07-31**: `organizations.status` (suspended/archived/cancelled) is not checked by any RLS policy — proven via a real pgTAP assertion, not implemented speculatively pending a product decision on intended behavior. `TECHNICAL_DEBT_REGISTER.md` TD-17 / `RISK_REGISTER.md` R-22.
- **Exit criteria**: met for everything in scope — net-new multi-tenancy tables exist, RLS'd, verified against a real database, `pnpm typecheck`/`lint` green. Cutover of the remaining seven pre-existing single-owner tables is tracked under M5/TD-02, not treated as part of "schema" in the abstract.

## M2 — Authentication

- [x] Supabase Auth retained unchanged as identity provider (`ARCHITECTURE.md` § Retained from PropVault) — no rebuild needed.
- [x] Session resolution for multi-portal identity — `resolvePortalSession()` (`apps/admin/lib/orgSession.ts`) resolves org memberships + owner identities for the authenticated caller. Tenant-identity resolution deferred to M8 (table doesn't exist yet).
- [x] Demo-mode bypass fix (`SECURITY.md`) — **implemented and build-verified 2026-07-30**: `packages/config/src/demoMode.ts` (`resolveDemoMode()`, dual-gated, both default false), `apps/admin/lib/demoMode.ts` (+ `server-only` import), `apps/mobile/src/lib/supabase.ts`, `apps/mobile/eas.json` (production profile omits the second gate), `.github/workflows/ci.yml`. Verified: `pnpm --filter admin build` with only `NEXT_PUBLIC_DEMO_MODE=true` set produces zero demo-mode activation; with both gates set, demo mode activates as intended. CI production-_deploy_ assertion (distinct from this build-time fix) remains M24 work, since no deploy pipeline exists yet to assert against.
- [x] **Web registration + OAuth, 2026-08-03** (`DECISIONS.md`, `AUTHENTICATION.md`): `/register` (email/password + verification), Google/Apple sign-in (`OAuthButtons.tsx`), `/auth/callback` (code + token_hash exchange, provider-error handling), `/forgot-password`/`/reset-password` (built same day, prior entry), identity linking (`LinkedAccountsPanel.tsx`, Supabase's native `linkIdentity`/`unlinkIdentity`). Email/password + tenant-activation verified end-to-end against real local Supabase; Google/Apple cannot be live-verified without real provider credentials (`TECHNICAL_DEBT_REGISTER.md` TD-29). Microsoft OAuth documented as a later enhancement only, not built (`AUTHENTICATION.md` §7).
- **Exit criteria**: met — demo-mode bypass closed, session resolution built and typechecked, web registration/OAuth/password-reset/identity-linking built and verified (OAuth live round-trip excepted, TD-29). Tenant-portal session resolution (needs the `tenants` table) is M8's concern, not a gap in this milestone's own scope.

## M3 — Roles and permissions

- [x] `has_org_role()` security-definer helper, org-role RLS pattern (`DATABASE.md` §12, `PERMISSIONS.md` §2).
- [x] `create_organization()`/`accept_organization_invite()` RPCs enforce role assignment atomically (principal on creation, invited role on acceptance).
- [ ] `is_admin()` → `is_platform_admin()` / `admin_users` → `platform_admin_users` rename — deferred to M19 (Super Admin), see `DECISIONS.md` 2026-07-30.
- [x] RLS isolation tests **written AND executed** (3 files, `supabase test db`, 2026-07-30/31) — 31/31 pgTAP assertions pass: cross-org isolation, role-scoped write denial (viewer cannot write within their own org either), platform-admin/support-session table isolation, and (2026-07-31) the full org-creation→invite→accept→role-gated-write→multi-org-switch flow end to end. Docker was confirmed available in this environment; the "no Docker" assumption this milestone previously carried was stale and never re-checked. R-02 closed (`RISK_REGISTER.md`).
- **Exit criteria**: fully met. Helper functions, policies, and test coverage exist, typecheck, and are verified passing against a real database — nothing left open in this milestone.

## M4 — Organizations

- [x] `organizations`, `organization_subscriptions`, `organization_invites` schema.
- [x] Org signup flow — `POST /api/v1/organizations` (`apps/admin/app/api/v1/organizations/route.ts`) + `/onboarding/create-organization` UI, 2026-07-30. Invite-acceptance endpoint (`POST /api/v1/organizations/invites/accept`) also implemented, pairing with `accept_organization_invite()`.
- [x] Invite-**creation** endpoint — `POST /api/v1/organizations/:orgId/invites` (2026-07-31). Found missing while verifying the invitations flow end-to-end: `organization_invites` had a SELECT policy but no INSERT policy and no route ever called it, so the whole invite-creation half of this feature had never actually been usable despite looking schema-complete. Fixed with migration `20260101000026` (manager+ RLS gate) + the new route (finer "manager can't invite a peer/superior manager or principal" rule enforced at the API layer per `PERMISSIONS.md`'s role table). End-to-end flow (create→invite→accept) now verified for real via `multi_tenant_foundation_integration.test.sql`.
- [ ] Organisation compliance-profile settings screen (CIPC/VAT/SARS/POPIA/FFC, Team Seats) — web UI not yet built; `PATCH /api/v1/organizations/:orgId` endpoint also not yet built.
- **Exit criteria**: partially met — signup/invite-create/invite-accept flow is real, execution-verified end-to-end, and typecheck/lint/build-verified; the compliance-profile settings screen (secondary to signup) remains open.

## M5 — Properties

- [~] **The `owner_user_id` → `org_id` cutover for `properties`** — **corrected 2026-07-30**: originally reported as fully done including dropping `owner_user_id`; a real `supabase start` run against a clean database proved that claim wrong — the column cannot yet be dropped because `document_categories.sql`/`documents.sql`'s RLS policies (still owner_user_id-scoped, unmigrated) reference it via cross-table subqueries, which Postgres's dependency tracking blocks. Actually done: `org_id` is `not null` and RLS-enforced (`has_org_role()`-based, matching `units`'s pattern) — this is the real ownership boundary going forward. `owner_user_id` is relaxed to nullable and left in place, inert, until TD-02's broader cutover makes dropping it possible. See `supabase/migrations/20260101000023_properties_org_contract.sql`'s revision note and `TECHNICAL_DEBT_REGISTER.md` TD-01/TD-02.
- [x] `packages/types/src/property.ts`: `ownerUserId` → `orgId`.
- [x] `apps/mobile/src/features/properties/propertyRepository.ts`/`usePropertiesQuery.ts`, demo store/mock data, local dev seed script, `rls_isolation.test.sql` — all updated. New `useCurrentOrgId()` hook resolves org context for the two mobile screens that create properties.
- [ ] `apps/admin` customer-page framing (→ organization framing) — **not done**: `customers/page.tsx`/`processing/page.tsx`/`subscriptions/page.tsx`/`adminMockData.ts` all have substantial unrelated pre-existing uncommitted changes (present before this work began) and were deliberately left untouched rather than risk conflicting with them. This piece of M5 is blocked on those pre-existing changes being resolved (committed or clarified) first — flagged to Mohammed, not silently dropped.
- [x] API endpoints for `properties` (`API_SPEC.md` §3, 2026-07-30): `GET/POST /api/v1/properties` (cursor-paginated list, org-role-checked create via `requireOrgRole()`/`has_org_role()` RPC — see `apps/admin/lib/portfolio.ts`), `GET/PATCH/DELETE /api/v1/properties/:id` (`DELETE` archives, never hard-deletes; cross-org access 404s rather than 403ing per `API_SPEC.md` §0). Web UI: Properties list/detail/create — still not built.
- [ ] Mobile onboarding gap (`apps/mobile/app/(onboarding)/add-first-property.tsx`): this screen now requires an org to exist first, but mobile has no create-organization screen (only web does, `/onboarding/create-organization`) — flagged inline in the file and in `TECHNICAL_DEBT_REGISTER.md`, not solved here (real UX design work, not a mechanical follow-on to the schema cutover).
- **Exit criteria**: the schema/type/mobile-data-layer cutover (the item this milestone exists for) is done and verified (`pnpm typecheck`/`lint`/`format` green); API endpoints done and verified (`pnpm typecheck`/`lint`/`test` green, real `next build` run confirming route registration); web UI and the admin customer-page reframing remain open, the last of which is blocked on pre-existing uncommitted work rather than new engineering.

## M6 — Units

- [x] Schema, RLS (`DATABASE.md` §3).
- [x] API endpoints (`API_SPEC.md` §3, 2026-07-30): `GET/POST /api/v1/properties/:propId/units` (folder is physically named `[id]` — Next.js requires sibling dynamic segments to share one slug name with `properties/[id]/route.ts`), `GET/PATCH /api/v1/units/:id`. Same cursor-pagination/org-role/404-vs-403 patterns as the properties endpoints, sharing `apps/admin/lib/portfolio.ts` and `apps/admin/lib/cursorPagination.ts`.
- [ ] Web UI, AI-assisted bulk unit generation (evidenced `PROPVIEW_SCREENSHOT_AUDIT.md` IMG_7998 — flagged in `PRODUCT_SPEC.md` §5 as needing its own design pass, not yet architected) — not started.
- **Exit criteria**: schema and API endpoints done and verified (`pnpm typecheck`/`lint`/`test` green — new `apps/admin/lib/__tests__/cursorPagination.test.ts`, 7 tests; real `next build` run); Web UI/AI-assist not started.

## M7 — Owners

- [x] Schema, RLS, multi-owner `property_owners` (`DATABASE.md` §3).
- [x] API endpoints (`API_SPEC.md` §3, 2026-07-30): `GET/POST /api/v1/owners`, `GET/PATCH /api/v1/owners/:id`, `GET/POST /api/v1/properties/:propId/owners` (attach owner + ownership_pct; upserts on `(property_id, owner_id)` so re-attaching adjusts the existing share rather than erroring; explicitly checks the owner's `org_id` matches the property's `org_id` before insert — `property_owners`'s own RLS policy only constrains via the owner side, not the property side, so this cross-org guard is API-layer-only and load-bearing, not redundant). `bankingRef`/mandate-date fields deliberately not exposed yet — real onboarding workflow fields, not mechanical CRUD (`TECHNICAL_DEBT_REGISTER.md`).
- [ ] Web UI (Owners directory, mandate fields) — not started.
- **Exit criteria**: schema and API endpoints done and verified (`pnpm typecheck`/`lint`/`test` green, real `next build` route-table check); Web UI not started.

## M8 — Tenants

- [x] Schema: `tenants` table (`DATABASE.md` §4, migration `20260101000028`, 2026-07-31), decoupled from `auth.users` like `owners`. Prerequisite `encrypted_secrets` pointer table (`DATABASE.md` §11) also built (migration `20260101000027`) — schema only, matching the documented shape for `tenants.id_number_ref`/`owners.banking_ref` (the latter's FK was retrofitted here, previously unconstrained); the application-layer encrypt-before-insert pipeline is deliberately not built yet, since nothing calls it (`TECHNICAL_DEBT_REGISTER.md` TD-18).
- [x] RLS: `tenants_select_org_or_self` (org staff viewer+, or the tenant's own portal identity if `user_id` is set — mirrors `owners`), `tenants_write_agent_plus` (no self-write policy — no tenant portal exists in V1). `encrypted_secrets`: zero client policies, service-role only.
- [x] API: `GET/POST /api/v1/tenants`, `GET/PATCH /api/v1/tenants/:id` (`API_SPEC.md` §4) — same cursor-pagination/`requireOrgRole`/404-vs-403 patterns as Properties/Units/Owners, sharing `apps/admin/lib/leasing.ts` (new) + the existing `portfolio.ts`/`cursorPagination.ts` helpers.
- [x] Tests: new `supabase/tests/tenants_isolation.test.sql` (10 assertions — cross-org isolation, role-scoped write denial, the self-access carve-out specifically, and `encrypted_secrets` deny-by-default) + full regression suite (41/41 pgTAP assertions across 4 files, real `supabase db reset` + `supabase test db`). Full monorepo `pnpm typecheck`/`lint` (7/7 packages) and a real `next build` (both new routes registered, no conflicts) also green.
- [ ] Web UI (Tenant directory) — not started, consistent with how Properties/Units/Owners were sequenced (API first). (Note: superseded by M20's later Tenants vertical slice — see that entry.)
- [x] **Tenant activation-code system, 2026-08-03** (`DECISIONS.md`, `AUTHENTICATION.md`): `tenant_invitations` schema + RLS + `create_tenant_invitation()`/`accept_tenant_invitation()` (migration `20260101000059`, a dedicated table, not a reuse of `organization_invites` — different lifecycle/security needs). `GET/POST /api/v1/tenants/:id/invitations`, `POST .../invitations/:invitationId/revoke`, `POST /api/v1/tenant-invitations/accept`. Staff UI (`TenantInvitationPanel.tsx`, wired into the tenant detail page) and tenant-facing UI (`/activate`). Hashed token/short-code storage, one-time plaintext display, failed-attempt lockout, masked destinations, email/WhatsApp dispatch (mock providers, `TECHNICAL_DEBT_REGISTER.md` TD-30). Verified: 26 new pgTAP assertions (`tenant_invitations.test.sql`) covering cross-org attack rejection, replay prevention, lockout, expired/revoked/archived/suspended-org handling, email mismatch, already-linked conflicts; 8 new vitest component tests; real-browser check clean.
- **Exit criteria**: schema, RLS, API, and tenant activation-code system done and execution-verified; Tenant-directory Web UI superseded by M20.

## M9 — Applications

- [x] Schema: `applications` (`DATABASE.md` §4, migration `20260101000029`, 2026-07-31). RLS mirrors the `properties`/`units`/`tenants` viewer-select/agent-write pattern. Two DB-level invariants beyond the documented column list: a decision's bookkeeping columns (`decision`/`decided_at`) are set together or not at all (CHECK), and `screening_status` cannot leave `not_started` before `screening_consent_at` is captured (CHECK) — both enforced independently of the API layer.
- [x] Application-approval transaction (atomic tenant+lease+rent_schedule creation) — `approve_application()` (migration `20260101000031`). Not security definer (unlike `create_organization()`/`accept_organization_invite()`): the caller already holds agent+ membership in the application's org, so every insert runs under the caller's own RLS rather than needing a privilege bypass — atomicity is the only thing this function adds. Deliberately does **not** hard-require `screening_status = 'passed'` before allowing approval — `DATABASE.md` §4 never states that as a rule, and encoding an unstated business policy as a hard DB constraint would be guessing at a product decision, not an engineering one.
- [x] API: `GET/POST /api/v1/applications`, `GET /api/v1/applications/:id` (no general PATCH — only the three state-transition actions below can mutate an application, so a blanket PATCH can't be used to bypass their rules), `POST /api/v1/applications/:id/consent`, `POST /api/v1/applications/:id/screen` (via a new mock-first `TenantScreeningProvider`/`MockTenantScreeningProvider`, `apps/admin/lib/providers/tenantScreening.ts`, matching ADR-014's vendor-agnostic pattern — no real screening vendor selected yet), `POST /api/v1/applications/:id/decide` (approve → `approve_application()` RPC; decline → simple update).
- [x] **V1 product-scope correction, 2026-08-01** (Mohammed, DECISIONS.md): PropertyVault V1 is not a tenant-screening platform — landlords/staff review applicants and documents directly and decide manually. Simplified the status model (`submitted`→display "New", added `reviewing`/`withdrawn`, migration `20260101000047`, expand-only — `screening`/`decided` and the screening_status/screening_consent_at columns kept intact and dormant, `TenantScreeningProvider` untouched, see ROADMAP.md). Added `notes` column + `POST /api/v1/applications/:id/notes` (also transitions submitted→reviewing) and `POST /api/v1/applications/:id/withdraw`. UI: removed the screening-consent/run-screening surface from `ApplicationActions`; kept POPIA consent, added a Notes panel and a Withdraw action; `ApplicationsTable`/detail badges now show the real decision (Approved/Declined) via a new `applicationDisplayPresentation()` helper instead of the generic "Decided" label. Approve still atomically creates tenant+lease via `approve_application()`, unchanged. Verified: real `supabase db reset` + full pgTAP (176/176 passed, no regressions), typecheck/lint/test (89/89) clean, real `next build` clean, demo-mode smoke test confirming no screening UI renders anywhere.
- **Exit criteria**: schema, RLS, atomic approval transaction, API, and the V1-simplified web UI done and execution-verified. Real screening-vendor integration explicitly deferred to V2 (ROADMAP.md), not started.

## M10 — Leases

- [x] Schema: `leases`, `lease_tenants`, `rent_schedules` (`DATABASE.md` §4, migration `20260101000030`, 2026-07-31). `lease_tenants`/`rent_schedules` follow the same "denormalized `org_id`"/"scoped through parent" RLS patterns already established for `property_owners`/`units`.
- [x] API: `GET/POST /api/v1/leases` (manual creation, `source: 'manual'` — the application-approval path never goes through this route), `GET/PATCH /api/v1/leases/:id`, `GET /api/v1/leases/:id/rent-schedule`.
- [ ] Lease-PDF-parse-to-prefill flow (`POST /api/v1/leases/:id/upload-and-parse`) — genuinely blocked on M12 (OCR/Document Intelligence), not started; building a stub that fakes OCR output would be worse than leaving it undone.
- [x] **Recurring `rent_schedules` generation, 2026-08-02** (closes TD-20's rent-schedule half — the Portfolio Intelligence/usage-rollup scheduling gaps TD-20 also named remain open, since they need their own trigger wiring, not just this generator). Migration `20260101000050`: `generate_rent_schedules_for_lease(lease_id, through)` idempotently fills in every missing monthly row for one active lease, anchored to the lease's own `start_date` (never chained off the previous row, which avoids Postgres's month-end date-drift bug for leases starting on the 29th–31st); `generate_rent_schedules_for_active_leases(through)` runs it across every org in one pass. Both `security definer`, `EXECUTE` revoked from `anon`/`authenticated`, granted only to `service_role` (same lockdown pattern as `resolve_whatsapp_sender()`). A real unique constraint (`lease_id, due_date`) makes duplicate prevention a DB guarantee, not just application logic. No proration is applied for mid-month starts — `ACCOUNTING.md`/`DATABASE.md` document no proration rule, and this matches `approve_application()`'s own existing full-amount-on-period-1 behaviour rather than inventing a new rule for periods 2+. Callable via `POST /api/v1/system/generate-rent-schedules` (super_admin session or `CRON_JOB_SECRET` bearer, for a scheduler with no user session) until `TASKS.md` M24 wires a real production cron trigger against this same endpoint — documented as the production-scheduler requirement, not left as dead code.
- [x] Tests: new `supabase/tests/recurring_rent_schedules.test.sql` (16 assertions) — first month, subsequent months, idempotent re-run (no duplicates), lease ending mid-horizon (stops strictly before `end_date`), open-ended lease, terminated lease (no-op), partially-paid existing row preserved untouched, bulk cross-org sweep with per-row org-isolation check, and `EXECUTE` privilege lockdown for both functions. Full regression suite: **192/192 pgTAP assertions across 14 files** on a genuine fresh `supabase db reset`. `apps/admin` `tsc --noEmit`/`eslint` on the changed files clean.
- [x] **Native Bearer-JWT authentication, 2026-08-02** (closes `TECHNICAL_DEBT_REGISTER.md` TD-28 — a cross-cutting API-layer fix landed here rather than under M21/M22 since it touches the one shared auth helper every route already uses, not any Android/iOS-specific code). `getServerSupabaseClient()` (`apps/admin/lib/supabase/server.ts`) now accepts `Authorization: Bearer <supabase-jwt>` (native callers) as well as the existing `@supabase/ssr` cookie session (the PWA) through a single abstraction — no route handler was edited. Verified live against the real local Supabase Auth server: a valid Bearer token authenticates and RLS-scopes reads/writes exactly like a cookie session; an invalid token still 401s; a valid token from a user with no membership in the target org still 403s via the unchanged `requireOrgRole()` checks; a real end-to-end `POST /api/v1/maintenance-tickets` and `POST /api/v1/device-push-tokens` both succeeded with a genuine Supabase-issued JWT and no cookie at all.
- [x] Tests: new `apps/admin/lib/supabase/__tests__/server.test.ts` (9 cases — 5 pure `extractBearerToken()` edge cases, 4 real integration tests against `supabase start`: valid token resolves the real user, invalid token resolves none, RLS-scoped read returns zero rows for a user with no membership rather than erroring or leaking, and the no-header case still falls through to the (unchanged) cookie path). Full monorepo-relevant regression: `apps/admin` `tsc --noEmit`/`eslint --max-warnings=0` clean, `vitest run` 123/123, real `next build` (all routes including the new system endpoint registered, no conflicts), 192/192 pgTAP unaffected (schema untouched).
- **Exit criteria**: schema, RLS, and manual/approval-sourced lease creation all done and execution-verified; PDF-parse flow correctly blocked on M12; recurring rent-schedule generation done and pgTAP-verified; native Bearer-JWT authentication done and verified against a real Supabase Auth server. Web UI for Leases itself was later done in M20; the Rent Due dashboard now receives accurate rows for every month of an active lease's life, not just its first; Android/iOS write flows are no longer blocked by the API auth layer (individual native screens still need their own client-side wiring, tracked under M21/M22).

## M11 — Documents

- [x] Extend `documents`, `document_categories`, `property_expected_categories`, `bills`, `payments`, `payment_matches`, `extraction_jobs`, `extraction_results`, `audit_events` from owner-scoped to org-scoped (migration `20260101000032`, 2026-07-31) — the largest single-migration blast radius so far (9 tables' RLS rewritten). `subscriptions`/`subscription_events` deliberately excluded (superseded by `organization_subscriptions`, a different question than an org_id add — correcting TD-02's original grouping).
- [x] **Corrected `DATABASE.md` §6** rather than implementing it literally: its documented `related_entity_type`/`related_entity_id` polymorphic redesign, checked against real application code before migrating (not after), would have regressed working, demoed features (`document_categories`' default+custom categories, `billing_year`/`billing_month` behind the Monthly Checklist, `checksum_sha256` dedup) that the redesign had no room for. Kept the proven shape, added only `org_id`. The polymorphic-relation generalization itself is deferred, not abandoned — no evidenced V1 need for it yet.
- [x] **This migration is the direct fix for what blocked M5's `DROP COLUMN properties.owner_user_id`** (`TECHNICAL_DEBT_REGISTER.md` TD-01): `property_expected_categories`' and `documents`' policies, previously keyed on `properties.owner_user_id`, now route through `properties.org_id`/`has_org_role()`. Re-verified live in a rolled-back transaction: `ALTER TABLE properties DROP COLUMN owner_user_id` now succeeds — TD-01's blocker is confirmed gone, though the actual drop is deliberately not executed in this migration (a separate, deliberate contract-step decision, not bundled into this one).
- [x] Two real bugs found by execution before this migration was committed, both fixed in place rather than shipped and patched after: (1) `owner_user_id NOT NULL` was never relaxed on 7 of these tables, which would have hard-blocked every new org-scoped insert (no meaningful owner to supply) — caught by writing the test suite, not by re-reading the migration; (2) `document_categories`' pre-existing CHECK constraint (`is_default or owner_user_id is not null`) was never updated when `org_id` was added, silently still blocking every org-scoped custom-category insert — caught the same way.
- [x] Tests: new `supabase/tests/documents_financials_isolation.test.sql` (14 assertions) — org-scoped inserts actually succeeding (not just old access being gone) across every cutover table, plus cross-org isolation on documents/categories/payment_matches. Full regression suite: 69/69 pgTAP assertions across 6 files. Full monorepo `pnpm typecheck`/`pnpm lint` unaffected (schema-only milestone, no app code changed — matches this milestone's own scope).
- [x] **Documents V1 vertical slice, 2026-08-01** (TASKS.md M20): `GET/POST /api/v1/documents` (real multipart upload — file parsed server-side, hashed, uploaded to Storage via the caller's own session-bound client, `documents` row inserted; a plain RLS-protected write, no service-role), `GET /api/v1/documents/:id` (+ short-lived signed URL), `GET /documents`/`GET /documents/:id`/`GET /documents/new` web UI. Storage bucket policies fixed to org-scoping alongside this (TD-21, resolved — see below). Bills/payments/payment-matching UI still not started (out of this pass's scope).
- [x] **OCR review workflow, 2026-08-01** (TASKS.md M20): `POST /api/v1/documents/:id/extract` generalizes M12's lease-`upload-and-parse` pattern (service-role for `extraction_jobs`/`extraction_results` only, after `requireOrgRole()` authorizes on the session client) to any bill/lease document; `POST /api/v1/documents/:id/review` records human confirmation (`reviewed_at`/`reviewed_by`, migration `20260101000048`) — display-only, never auto-corrects/auto-applies fields, matching `DOCUMENT_INTELLIGENCE.md`'s "always confirm before treating as final" rule.
- **Exit criteria**: schema/RLS cutover done for all 9 tables (TD-01 resolved); Documents upload/list/detail + OCR extract/review UI done and execution-verified (real `supabase db reset` + pgTAP 176/176, admin typecheck/lint/test 96/96, real `next build`, demo-mode smoke test). Bills/payments UI and native (mobile) upload flows remain open.

## M12 — OCR

- [x] Extended `DocumentIntelligenceProvider` to parse leases in addition to bills (2026-07-31): `'lease'` added to `DOCUMENT_TYPES`/`documents.document_type` (migration `20260101000033`), `FieldExtractionResult` gained lease-shaped optional fields (`tenantName`/`rentAmount`/`depositAmount`/`leaseStartDate`/`leaseEndDate`/`propertyAddress`) alongside the existing bill fields on the same type, and a new server-side `apps/admin/lib/providers/documentIntelligence.ts` (`MockDocumentIntelligenceProvider`) branches on `documentType` — the mobile app's existing mock (client-side, different runtime) is untouched.
- [x] Closed the `POST /api/v1/leases/:id/upload-and-parse` gap deferred from M10 — now built, using the extended provider. Returns extracted fields for client-side review only, never auto-applies them to the lease, per `DOCUMENT_INTELLIGENCE.md`'s "always confirm before treating as final" rule.
- [x] **Real bug found and fixed before shipping**: the route initially wrote to `extraction_jobs`/`extraction_results` using the caller's own session-bound client — but those tables have deliberately had no client INSERT/UPDATE policy since Phase 1 ("jobs are created and progressed only by the server-side processing pipeline"). Verified live that an agent's insert is rejected by RLS; fixed by using the service-role client for those two tables specifically (only after `requireOrgRole()` already authorized the caller), matching the established `getServiceRoleClient()` usage pattern. Re-verified the fix directly (service-role insert reaches the FK constraint, not an RLS rejection).
- [ ] Real OCR vendor selection remains an open decision (deferred, `DOCUMENT_INTELLIGENCE.md` §1) — mock provider carries M11/M12 until then; this is a cost/accuracy tradeoff for Mohammed, not an engineering call.
- **Exit criteria**: lease-extraction support and the upload-and-parse endpoint done and verified (full monorepo `pnpm typecheck`/`pnpm lint`, real `next build` with the new route registered, 33/33 migrations clean). Real vendor integration open pending Mohammed's decision.

## M13 — Maintenance

- [x] Schema: `maintenance_tickets`, `maintenance_photos`, `vendors`, `vendor_bills`, `inspections`, `inspection_items`, `inspection_photos` (migration `20260101000034`, 2026-07-31, `DATABASE.md` §5). `vendor_bills.paid_journal_entry_id` added without its FK constraint yet (matches the `owners.banking_ref`/M2 precedent) — `journal_entries` doesn't exist until M14; FK retrofit is M14's job.
- [x] Maintenance status state machine (To Do → In Progress → Pending Approval → Completed, with Pending Approval → In Progress as the rejection path) enforced server-side in `PATCH /api/v1/maintenance-tickets/:id` (`apps/admin/lib/operations.ts`'s `isValidMaintenanceTransition`) — RLS can only express "agent+ can write this row," not "is this a legal transition," matching `PERMISSIONS.md` layer 2.
- [x] Inspections: both-signed-or-refusal-logged completion rule enforced as a **hard DB CHECK constraint**, not just an API check — deliberately stronger than the maintenance state machine, since `TASKS.md` M14's deposit-release gate depends on this being genuinely true even against a direct/service-role write. Verified with 5 dedicated pgTAP assertions proving every edge case (neither signed, landlord-only, both-set-simultaneously rejected, both-signed accepted, refusal-logged accepted).
- [x] API: `GET/POST /api/v1/vendors`, `GET/PATCH /api/v1/vendors/:id`, `GET/POST /api/v1/maintenance-tickets`, `GET/PATCH /api/v1/maintenance-tickets/:id`, `GET/POST /api/v1/inspections`, `POST /api/v1/inspections/:id/items`, `POST /api/v1/inspections/:id/sign`, `POST /api/v1/inspections/:id/complete`. `vendor-bills`/`maintenance-tickets/:id/photos` API endpoints not built this pass — `vendor_bills`' approve flow is naturally paired with M14 (it writes to the same `paid_journal_entry_id` column M14 makes real), and photos need `documents` API endpoints (M11 TD-21) first.
- [x] Tests: new `supabase/tests/maintenance_inspections_isolation.test.sql` (13 assertions). Full regression suite: 82/82 pgTAP assertions across 7 files. Full monorepo `pnpm typecheck`/`pnpm lint` (7/7 packages — caught and fixed one real unused-import lint error before commit), real `next build` (8 new routes registered, no conflicts).
- [ ] Web UI: Maintenance Board, Vendors, Inspections — not started, consistent with every prior milestone's API-first sequencing.
- **Exit criteria**: schema, both state machines (one API-layer, one DB-layer, deliberately different strength matching what each depends on), and core API done and execution-verified. `vendor-bills`/photos API and Web UI open.

## M14 — Accounting (part 1: core ledger — done; part 2: subledgers + 4 posting operations — done; part 3: deposit release/interest/owner statements/tax pack — not started)

Given this milestone's own repeated flagging as the highest-risk single workstream in the project, it is deliberately built in increments rather than attempted in one pass, each verified to the same standard as every other milestone this session before moving to the next.

**Part 1 — core ledger (2026-07-31)**:

- [x] Schema: `chart_of_accounts`, `journal_entries`, `journal_lines`, `accounting_periods` (migration `20260101000035`, `DATABASE.md` §9).
- [x] **Real gap found and fixed before writing this migration**: `ACCOUNTING.md` §1 claimed immutability was enforced by "RLS has no update/delete policy... for any role, including elevated ones" — not actually true, since `service_role` has `BYPASSRLS = true` (verified this session), so RLS's presence or absence has zero effect on it. Fixed with `BEFORE UPDATE OR DELETE` triggers on `journal_entries`/`journal_lines` that unconditionally reject the operation — the real third enforcement layer. `ACCOUNTING.md` §1 corrected. Applied the identical fix to `audit_events` (migration `20260101000036`).
- [x] Posting primitives: `post_journal_entry()` (balance validation, period-lock check, atomic entry+lines insert) and `reverse_journal_entry()` (exact negation). `POST /api/v1/journal-entries/:id/reverse` wraps the latter; no free-form posting endpoint exists.
- [x] Period locking: `close_accounting_period()`/`reopen_accounting_period()`, accountant+-gated. Reopening's stated audit-write requirement not implemented — `audit_events.actor_type` has no value correctly describing an org accountant (`TECHNICAL_DEBT_REGISTER.md` TD-14).
- [x] Chart-of-accounts seeding wired atomically into `create_organization()` — 11 system accounts per new org.
- [x] API: `GET /api/v1/chart-of-accounts`, `GET /api/v1/journal-entries`, `POST /api/v1/journal-entries/:id/reverse`, `GET /api/v1/trial-balance`, `GET/POST /api/v1/accounting-periods`, `POST .../close`, `POST .../reopen`.
- [x] `supabase/tests/accounting_core.test.sql` (21 assertions) — proves immutability against the `postgres` superuser connection specifically (bypasses RLS the same way `service_role` does), plus balance/period-lock/reversal coverage.

**Part 2 — subledgers + 4 posting operations (2026-07-31)**:

- [x] Schema: `trust_ledgers`, `trust_ledger_entries` (with the same real-immutability trigger fix applied proactively, not discovered again), `bank_accounts`, `bank_transactions`, `invoices`, `expenses`, `owner_statements`, `tax_pack_exports` (migration `20260101000037`). All financial-write RLS gates on accountant+, **checked against `PERMISSIONS.md` §2's role table before writing any of it** (agent has no accounting-post rights at all — a real design question this resolved correctly before implementation, not after: deposit posting must be a separate accountant action, never bundled into `approve_application()` even though `ACCOUNTING.md` §3 describes "a lease going active" as its trigger).
- [x] Four typed posting operations (migration `20260101000038`), deliberately the only four unambiguously specified in `ACCOUNTING.md` §3 — `release_trust_deposit()`/`accrue_trust_interest()` explicitly **not** built this pass, both requiring an account-mapping decision `ACCOUNTING.md` doesn't specify (see below):
  - `record_expense()` — Dr [category] Expense (matched by name, falling back to Other Expense), Cr Accounts Payable/Bank.
  - `invoice_rent_schedule()` — Dr Accounts Receivable, Cr Rent Income; issues the invoice.
  - `confirm_bank_transaction_match()` — Dr Bank, Cr Accounts Receivable for the matched amount; **real bug found and fixed by testing the two-payment case, not assumed correct from testing one**: the paid-vs-partial decision must compare against the _cumulative_ amount matched across every transaction linked to a schedule, not just the current one — a second covering payment was initially compared against the full schedule amount in isolation and wrongly stayed `partial`. Fixed by adding `bank_transactions.matched_rent_schedule_id` (not in `DATABASE.md`'s original column list) to make that cumulative query possible.
  - `post_lease_deposit()` — Dr Trust Bank, Cr Tenant Deposits Held; opens the trust ledger. Accountant+ only, explicit, never automatic.
- [x] API: `GET/POST /api/v1/expenses`, `POST .../record`, `GET /api/v1/rent-schedules`, `POST /api/v1/rent-schedules/:id/invoice`, `GET/POST /api/v1/bank-accounts`, `GET/POST /api/v1/bank-transactions`, `POST .../confirm-match`, `GET /api/v1/trust-ledgers`, `POST /api/v1/leases/:id/post-deposit`.
- [x] `supabase/tests/accounting_posting_operations.test.sql` (20 assertions) — a full end-to-end flow (property → unit → tenant → lease-with-deposit → deposit posted → rent invoiced → partially paid → fully paid → expense recorded), including the `PERMISSIONS.md` role-split proof (an agent-only member is rejected by `post_lease_deposit()`) and the cumulative-payment bug above. Two real pgTAP-authoring bugs also found and fixed: fixture UUIDs using non-hex characters (same class of bug as M8's tenants tests), and `\gset`-captured variables not interpolating inside `$$...$$` blocks passed to `throws_ok`/`lives_ok` — abandoned `\gset` for this file in favor of the subquery pattern already proven across every other test file this session.
- Full regression suite: **123/123 pgTAP assertions across 9 files**. Full monorepo `pnpm typecheck`/`pnpm lint` (7/7 packages), real `next build` (9 new routes registered, no conflicts), 38/38 migrations clean on a fresh reset.

**Part 3**:

- [x] **Web UI, 2026-08-01** (TASKS.md M20, part of parts 1-2's existing API — no schema/RPC changes): `/accounting/bank-accounts` (list/create), `/accounting/bank-transactions` (list/create, KPI row, inline "Match" control against the org's pending/overdue rent_schedule rows, wrapping the existing confirm-only `confirm_bank_transaction_match()`), alongside the already-shipped Rent Due/Expenses/Trial Balance pages.
- [x] **`release_trust_deposit()`/`accrue_trust_interest()`, 2026-08-02** (migration `20260101000051`, closes `TECHNICAL_DEBT_REGISTER.md` TD-22's deposit half): the account-mapping decision that blocked this is resolved — two new system accounts, `4900 Deposit Deduction Income` and `5950 Trust Interest Expense`, backfilled onto every existing org and added to `seed_chart_of_accounts()`. `release_trust_deposit()` settles a lease's entire trust-ledger balance at move-out in one call (Dr Tenant Deposits Held / Cr Trust Bank for the total, plus Dr Business Bank / Cr Deposit Deduction Income for any deduction), gated on a completed `move_out`-type inspection (`ACCOUNTING.md` §4's evidenced release gate, checked in the service not just the UI), one-time/terminal via a new `trust_ledgers.status` column. `accrue_trust_interest()` posts simple daily-prorated interest at `organizations.deposit_interest_pct` (Dr Trust Interest Expense / Cr Tenant Deposits Held) — `ACCOUNTING.md` §4's computation/gating rule was already unambiguous, only the account mapping and unattended-scheduling gap (same missing cron infrastructure as TD-20) were open; scheduling stays open, computation does not — shipped as an explicit accountant-triggered action (`accrue_trust_interest_for_org()` for a same-session bulk convenience across one org), matching `post_lease_deposit()`'s own existing "explicit action" precedent. API: `POST /api/v1/trust-ledgers/:id/release`, `POST /api/v1/trust-ledgers/:id/accrue-interest`. **Real bug found and fixed by this pass's own test suite**: `SELECT ... FOR UPDATE` against a table with an accountant+-only "for all" write RLS policy silently requires satisfying that write policy just to lock the row, producing a misleading "not found" instead of the intended role-check error for an agent-only caller — removed `FOR UPDATE` from both functions (matching every other posting operation in this codebase, none of which use it either).
- [x] Tests: new `supabase/tests/trust_deposit_release_and_interest.test.sql` (21 assertions — release gate enforcement at both the no-inspection and scheduled-not-completed stages, amount validation, the PERMISSIONS.md role split for both functions, a full deduction+refund release with exact journal/ledger-entry assertions, the double-release guard, exact-amount interest accrual over a controlled 365-day window, the released-ledger-cannot-accrue-further guard, and the bulk wrapper's idempotent no-op). One pre-existing test (`accounting_core.test.sql`'s system-account count) updated from 11 to 13 to match the two new accounts. Full regression suite: **213/213 pgTAP assertions across 15 files** on a genuine fresh `supabase db reset`. Full monorepo `pnpm typecheck` (7/7 packages — pre-existing, unrelated `apps/mobile` demo-data type error not touched by this change, TD-11), `apps/admin` `eslint --max-warnings=0`/`vitest` (123/123) clean, real `next build` (2 new routes registered, no conflicts).
- [x] **Owner Statements, 2026-08-02** (migration `20260101000052`): `generate_owner_statements(org_id, period_start, period_end)` is the "month-scoped batch draft" `API_SPEC.md` §6 documents — for every property in the org, sums `payment`/`expense` journal_lines for the period, splits by `property_owners.ownership_pct` with `ACCOUNTING.md` §10's exact rounding-remainder-to-last-owner rule (verified: the sum of every co-owner's share equals the true total to the cent, never silently short/over), aggregates each owner's totals across every property they hold a stake in into ONE statement row per owner per period, and skips (never overwrites) any owner who already has an issued/paid statement for that period — `ACCOUNTING.md` §5's frozen-snapshot rule. `management_fee` uses a new simple flat `organizations.management_fee_pct`, mirroring the existing `deposit_interest_pct` precedent (no schedule/tiering invented — none is documented). `issue_owner_statement()` freezes a draft; `confirm_owner_statement_payout()` posts the `owner_payout` journal entry (Dr Owner Equity / Cr Business Bank) only once a statement is issued and matched to a real outgoing bank transaction, matching the existing confirm-only bank-matching principle (`ACCOUNTING.md` §8). API: `GET/POST` (list/detail only — no direct create, statements are generated not hand-entered) plus `POST /api/v1/owner-statements/draft`, `.../issue`, `.../confirm-payout`. Web UI: `/accounting/owner-statements` (list, generate-for-period action, KPI row) and `/accounting/owner-statements/:id` (detail, issue/confirm-payout actions) plus a `.../print` view — browser print-to-PDF (`window.print()`), not a server-rendered PDF library, a deliberate V1 scope choice to avoid a new dependency; `AppShell`'s chrome (both sidebars, mobile top bar) gained `print:hidden` so any `(dashboard)` page can double as a clean print view.
- [x] Tests: new `supabase/tests/owner_statements.test.sql` (20 assertions — a real two-owner/two-property portfolio proving the exact rounding-remainder invariant, idempotent-draft-regeneration, the frozen-snapshot rule once issued, and the full payout-confirmation journal entry) plus a new `components/tables/__tests__/OwnerStatementsTable.test.tsx` (2 cases). Full regression: **233/233 pgTAP assertions across 16 files** on a real `supabase db reset`. Full monorepo `pnpm typecheck` (6/6 non-mobile packages), `apps/admin` `eslint --max-warnings=0`/`vitest` (125/125) clean, real `next build` (8 new routes, no conflicts), and a real demo-mode smoke test (`next build && next start`, all three new pages 200 with real content, no server errors in the log).
- [x] **Tax Pack, 2026-08-02** (migration `20260101000053`): `compute_tax_pack(org_id, tax_year)` is a live, computed-on-demand report (never a stored table beyond the audit record, matching the Trial Balance's own pattern, `ACCOUNTING.md` §6) — sums `journal_lines` for the SA tax year (1 March–end of February, computed via `make_date(tax_year, 3, 1) - 1` so leap years resolve correctly with no special case), grouped per-property and per-account (`chart_of_accounts` IS the ledger's category taxonomy, since `record_expense()` already matches an expense's category to a same-named account — grouping by account is grouping by category, not a separate mechanism). No SARS classification beyond account name is invented — none is documented anywhere. `record_tax_pack_export()` writes the audit row (`tax_pack_exports`, unchanged minimal schema from M14 part 2) only when an export actually happens, not on every on-screen view. API: `GET /api/v1/tax-pack?org_id=&tax_year=` (JSON, for the on-screen summary) and `GET /api/v1/tax-pack/export?...` (CSV download — chosen over a server-rendered PDF to avoid a new dependency, same scope decision as Owner Statements; the download itself triggers the audit-record RPC, since downloading is exporting). Web UI: `/accounting/tax-pack` — a tax-year selector, income/expense/net KPI cards, a per-property/per-account table, a CSV download link, and the verbatim `ACCOUNTING.md` §7 disclaimer ("not tax advice... confirm treatment with SARS or a registered tax practitioner before filing").
- [x] Tests: new `supabase/tests/tax_pack.test.sql` (12 assertions — correct per-property/per-account attribution, a real out-of-year entry posted directly via `post_journal_entry()` at a controlled `entry_date` proven excluded — `journal_entries`' own immutability trigger correctly rejects the more naive "post then backdate" approach, confirming that enforcement works — role-gating for both functions, invalid-year rejection, the audit-record write, and cross-org isolation) plus a new `components/accounting/__tests__/TaxPackClient.test.tsx` (2 cases, mocked fetch). Full regression: **245/245 pgTAP assertions across 17 files** on a real `supabase db reset`. Full monorepo `pnpm typecheck` (6/6 non-mobile packages), `apps/admin` `eslint --max-warnings=0`/`vitest` (127/127) clean, real `next build` (3 new routes, no conflicts), and a real demo-mode smoke test (`next build && next start`, page 200 with real content, no server errors).
- [ ] **Still not started**: `calculateMatchScore`-based match _proposal_ (TD-22's remaining half — the V1 UI works around this by having staff pick the specific rent_schedule to match), unattended interest-accrual scheduling (bundled with TD-20's cron-infrastructure item).
- **Exit criteria for M14 overall**: the accounting engine (ledger, subledgers, posting operations, deposit release/interest, Owner Statements, Tax Pack) done and execution-verified, including web UI for all of it. Only the bank-match proposal step remains open — a genuinely independent, lower-risk item, not core to any launch-blocking money-handling flow.

## M15 — Notifications

- [x] Schema: `notifications`, `notification_preferences`, `device_push_tokens`, `announcements`, `announcement_reads` (migration `20260101000039`, 2026-07-31, `DATABASE.md` §7).
- [x] **Real gap found and fixed before shipping**: the announcements tenant-visibility RLS policy's first version used a raw subquery through `lease_tenants`/`leases`, both built agent+-only in M10 (no tenant-self RLS branch existed, since there was no tenant-facing read need at the time). The subquery silently returned zero rows for every tenant, failing 3 of 4 tenant-visibility tests on first run. Fixed with a `security definer` helper (`tenant_can_view_property_announcement()`), the same pattern `has_org_role()` itself uses for the identical "needs a cross-table read the caller has no direct RLS policy for" problem.
- [x] Tenant visibility for announcements is scoped correctly by property: a portfolio-wide announcement (`property_id null`) is visible to every tenant in the org; a property-scoped one only to tenants actually leasing that property — verified with a real two-property, two-tenant fixture, not assumed from a single-tenant case.
- [x] API: `GET/POST /api/v1/announcements`, `POST /api/v1/announcements/:id/acknowledge` (tenant), `GET /api/v1/notifications`, `POST /api/v1/notifications/:id/read`, `GET/PATCH /api/v1/notification-preferences`, plus `POST/DELETE /api/v1/device-push-tokens` (not in `API_SPEC.md`'s literal list, but push registration needs a real endpoint and RLS already fully supports it scoped to the caller).
- [x] Tests: new `supabase/tests/notifications_isolation.test.sql` (13 assertions). Full regression suite: 136/136 pgTAP assertions across 10 files. Full monorepo `pnpm typecheck`/`pnpm lint` (7/7 packages), real `next build` (6 new routes registered, no conflicts).
- **Exit criteria**: schema, RLS (including the tenant-visibility fix), and API done and execution-verified. Actual push/email/WhatsApp delivery of a notification (the sending side) is M16/M17/M21/M22's own work — this milestone is the data model and the read/preference/acknowledge API surface.

## M16 — Email

- [x] Schema: `email_messages`, `email_suppressions` (migration `20260101000040`, 2026-07-31, `DATABASE.md` §7). RLS: org-staff SELECT only, zero client write policy on either table (writes are server-only via `service_role`).
- [x] `EmailProvider` interface (`packages/types/src/email.ts`) + `MockEmailProvider` (`apps/admin/lib/providers/email.ts`, `EMAIL.md` §2) — always returns `status: 'queued'`, never simulates further progression, matching `EMAIL.md`'s rule that `email_messages.status` is read as delivery proof, not assumed.
- [ ] Real provider account — **external-service blocker**, not attempted by this session.
- [x] **Send-triggering wiring into product events, 2026-08-02** (closes TD-23's email half): `apps/admin/lib/emailDispatch.ts`'s `dispatchEmail()` is the one place every trigger site calls into — idempotent (one email per `(related_entity_type, related_entity_id, template_name)`, so a retried route call never double-sends), suppression-checked (`email_suppressions`), preference-gated for non-transactional categories only (`EMAIL.md` §1: transactional categories are never user-suppressible), and writes both the `email_messages` row and an `audit_events` row per send (`EMAIL.md` §5). Never blocks or fails the triggering request — a dispatch error is logged, not thrown, the same boundary `writeAuditEvent()` already uses. Wired into 5 real, already-existing trigger points: `POST /api/v1/rent-schedules/:id/invoice` (`invoice_issued`), `POST /api/v1/bank-transactions/:id/confirm-match` (`payment_recorded`), `POST /api/v1/owner-statements/:id/issue` (`owner_statement_ready`), `PATCH /api/v1/maintenance-tickets/:id` on a status change (`maintenance_update`, preference-gated), and `processBillingWebhookEvent()`'s `payment_failed` branch (`subscription_payment_issue`, to the org's principal). This is a deliberately smaller V1 subset of `EMAIL.md` §1's full template catalogue, matching the instruction to use only clearly-scoped, already-triggerable events rather than the whole table.
- [x] **Real bug found and fixed by this pass's own test suite**: `email_messages.related_entity_id` is a `uuid` column, not text — an early draft encoded extra context (e.g. a maintenance ticket's new status, so repeated distinct status transitions each get their own idempotency slot rather than being wrongly deduped as "already sent") directly into `related_entity_id` as a composite string, which fails with "invalid input syntax for type uuid" the moment a real ticket id is used. Fixed by moving that extra context into `related_entity_type` instead (a plain `text` column, unconstrained) and keeping `related_entity_id` a real, valid entity uuid throughout — caught by a real integration test against local Supabase, not by inspection.
- [x] Tests: new `apps/admin/lib/__tests__/emailDispatch.test.ts` (6 real integration tests against local Supabase — send + row write, idempotent no-duplicate-send, suppression skip, preference-gated denial, transactional-always-sends, no-address no-op). Full monorepo `pnpm typecheck` (6/6 non-mobile packages), `apps/admin` `eslint --max-warnings=0`/`vitest` (145/145) clean, real `next build` clean, 252/252 pgTAP unaffected (no schema change this pass).
- **Exit criteria**: mock-provider path and send-triggering wiring into 5 real product events are both complete and execution-verified. Only a live vendor account remains open (external-service blocker, cannot be provisioned by this session).

## M17 — WhatsApp

- [x] Schema: `whatsapp_messages`, `verified_phone_numbers`, `whatsapp_conversation_state` (migration `20260101000040`, 2026-07-31, `DATABASE.md` §7). `verified_phone_numbers`/`whatsapp_conversation_state` have RLS enabled with zero client policies (deny-all by design — resolution is server-side only).
- [x] Resolution algorithm (§1.2): `resolve_whatsapp_sender()` security-definer function implementing all 3 branches (0 matches = unauthenticated, 1 = resolved, 2+ = ambiguous), verified with a real two-owner-type-same-number fixture, not assumed. **Security fix required and applied before shipping** — see `DECISIONS.md` 2026-07-31 and `RISK_REGISTER.md`: `EXECUTE` was revoked from `anon`/`authenticated`, closing a cross-tenant phone-number-lookup vulnerability inherited from migration 024's default-privilege grant.
- [x] `WhatsAppProvider` interface (`packages/types/src/whatsapp.ts`) + `MockWhatsAppProvider` (`apps/admin/lib/providers/whatsapp.ts`, `WHATSAPP.md` §5) — deterministic synchronous responses; the timer-based queued→sent→delivered lifecycle simulation `WHATSAPP.md` §5 describes is deliberately not implemented since nothing consumes it yet (no webhook route or scheduled job wired in).
- [x] **Fixed trigger-list dispatcher, 2026-08-02** (closes TD-23 fully — both the email and WhatsApp halves): `apps/admin/lib/whatsappDispatch.ts`'s `dispatchWhatsApp()` is the _only_ call site permitted to send (`WHATSAPP.md` §2: "no code path may free-text an arbitrary message through it"). Deliberately wires only 3 of the enum's 16 values — `payment_accepted`, `owner_statement_available`, `maintenance_update_critical` — each backed by a real, already-existing synchronous trigger (bank-transaction payment confirmation, owner-statement issuance, a maintenance ticket's status change while `priority = 'urgent'`). The other 13 (`rent_overdue_material`, `lease_expiring_soon`, `rent_overdue_significant`, ...) all require a scheduled-detection job this codebase doesn't have (the same missing cron infrastructure as TD-20) — left correctly unwired rather than guessed at with an invented "check on every request" trigger. `account_security_event` has no real trigger point anywhere in this codebase either (Supabase Auth's own events aren't hooked). Idempotent (one message per `(related_entity_type, related_entity_id, template_name)`), preference-gated via `notification_preferences.whatsapp_enabled` (`WHATSAPP.md` §2's category mapping table), phone numbers taken from `tenants.phone`/`owners.phone` directly — **not** `verified_phone_numbers`, since that table is for _inbound_ identity resolution only (`WHATSAPP.md` §1.1: unverified numbers are explicitly valid for outbound contact). `PLATFORM_WHATSAPP_NUMBER` is a clearly-labeled `TO_BE_CONFIRMED` placeholder (no real number exists). Mirrors `dispatchEmail()`'s exact idempotency/preference/audit shape from the same pass.
- [x] Tests: new `apps/admin/lib/__tests__/whatsappDispatch.test.ts` (6 real integration tests against local Supabase — send + row write, idempotent no-duplicate-send, malformed-phone rejection, no-phone no-op, preference-gated denial, default-enabled-with-no-preference-row). Full monorepo `pnpm typecheck` (6/6 non-mobile packages), `apps/admin` `eslint --max-warnings=0`/`vitest` (151/151) clean, real `next build` clean, 252/252 pgTAP unaffected (no schema change this pass).
- [ ] OTP verification flow populating `verified_phone_numbers` — flagged as not yet designed (`WHATSAPP.md` Unresolved); not needed for outbound dispatch (see above), only for inbound resolution, which this pass didn't touch.
- [ ] Real BSP account and webhook signature verification — **external-service blocker**, not attempted by this session.
- **Exit criteria**: mock-provider path, resolution algorithm, and the outbound dispatcher for the 3 triggers with a real synchronous cause are all complete and execution-verified. The remaining 13 trigger types need either a scheduled-job infrastructure item (TD-20) or a design pass (`account_security_event`); OTP flow and a live BSP account remain open, external/design blockers not attempted.

## M18 — AI

- [x] Schema: `ai_conversations`, `ai_messages`, `portfolio_insights`, `usage_events`, `usage_snapshots` (migration `20260101000042`, `DATABASE.md` §7-8).
- [x] **`audit_events` TD-14 cutover, migration `20260101000043`**: schema rewritten to `DATABASE.md` §10's target shape (`actor_type` extended to `user|system|api|ai_assisted`, `target_type`/`target_id` renamed to `entity_type`/`entity_id`, `owner_user_id`/`metadata` dropped, `before`/`after`/`ip_address`/`ai_conversation_id`/`ai_message_id` added) — done now because `AI_ARCHITECTURE.md` §5's audit requirement genuinely cannot be represented on the old schema. Confirmed zero real writers existed before changing it (no `.from('audit_events')` call site anywhere in TS). `accounting_core.test.sql`'s own audit_events fixture updated to match (a real, expected blast-radius fix, not a regression). Wiring the two previously-deferred call sites (`reopen_accounting_period()`, `POST /api/v1/organizations`) back up is left as separate follow-on work, not bundled into this schema change.
- [x] Conversational Assistant: context-assembly function (`apps/admin/lib/ai.ts` `assembleOrgContext()`, session-scoped queries only, never service-role — `AI_ARCHITECTURE.md` §1.4), staged-changes confirm-before-apply flow (`POST /api/v1/ai/conversations`, `.../messages`, `POST /api/v1/ai/messages/:id/confirm`), `LLMProvider` interface + `MockLLMProvider` (`AI_ARCHITECTURE.md` §1, §3). Confirm re-enters the staged endpoint via a same-origin fetch forwarding the caller's own session cookie (realizing "in-process, as the acting user" within Next.js's route-handler model) — the staged `endpoint` is validated against a strict `/api/v1/...`-only pattern before being fetched, closing an SSRF vector a future prompt-injected LLM response could otherwise exploit.
- [x] Portfolio Intelligence: rules engine (non-LLM), all 5 evidenced rule types + severity thresholds (`AI_ARCHITECTURE.md` §2.2/§2.4), `reconcilePortfolioInsights()` (`apps/admin/lib/portfolioIntelligence.ts`) inserts newly-triggered insights, updates severity/message on ones still triggering, and auto-dismisses ones that no longer trigger. **Not wired to an actual schedule** — no cron/scheduled-function infrastructure exists in this codebase yet, the same gap `TECHNICAL_DEBT_REGISTER.md` TD-20 already documents for rent-schedule generation; this is the callable, tested rules engine §2.5 describes, not the Edge Function schedule itself.
- [x] `usage_events`/`usage_snapshots` metering (AI token usage recorded per conversation turn via service-role) + per-org AI usage cap enforcement (`checkAiUsageCap()`, checked before calling the LLM provider, per `AI_ARCHITECTURE.md` §4) — sums `usage_events` directly rather than reading `usage_snapshots`, since the rollup job that would populate snapshots doesn't exist yet (same TD-20-class gap). No plan has an `aiMonthlyTokenCap` configured yet (real cap numbers are a pricing decision, not invented here), so enforcement is real but currently a no-op until Mohammed sets one.
- [ ] LLM vendor selection remains an open decision (`AI_ARCHITECTURE.md` §3) — mock provider carries this milestone until then.
- [ ] Portfolio Intelligence's Edge Function schedule and triggered-on-write invocation (`AI_ARCHITECTURE.md` §2.5) — blocked on the same missing scheduling infrastructure as TD-20.
- Tests: new `supabase/tests/ai_and_usage_isolation.test.sql` (19 assertions — ai_conversations/ai_messages owner-only isolation, portfolio_insights/usage_events/usage_snapshots server-only-write + org-scoped-read, and the audit_events cutover's schema shape confirmed live via `information_schema`). New `apps/admin/lib/providers/__tests__/llm.test.ts` (4 assertions) and `apps/admin/lib/__tests__/ai.test.ts` (4 assertions, the SSRF-guard `isValidStagedEndpoint()`). Full regression suite: **167/167 pgTAP assertions across 12 files**. Full monorepo `pnpm typecheck`/`pnpm lint` (7/7 packages), `pnpm --filter admin test` (26/26), real `next build` (5 new routes registered: `/api/v1/ai/conversations`, `.../[id]/messages`, `/api/v1/ai/messages/[id]/confirm`, `/api/v1/insights`, `/api/v1/insights/[id]/dismiss`).
- **Exit criteria**: schema, context-assembly, staging/confirm pipeline, rules engine, and usage metering/cap-check are done and execution-verified. Actual scheduling (both the Portfolio Intelligence Edge Function and a real LLM vendor account) remain open, external/infrastructure blockers — not claimed as done.

## M19 — Super Admin

- [x] Independent platform-admin auth check — reused as-is (`SUPER_ADMIN.md` §0: "Reused from apps/admin as-is"), `requireRole()`/`getAdminSession()`/`isRoleAtLeast()` needed no logic changes, only the rename below. No separate `apps/web/app/(super-admin)/**` route group exists yet — `apps/web` itself doesn't exist until M20 renames `apps/admin`; this milestone's routes live at `apps/admin/app/api/v1/admin/**` and `apps/admin/app/(dashboard)/**`, which today's Super Admin portal already is.
- [x] `admin_users` → `platform_admin_users`, `is_admin()` → `is_platform_admin()` rename (migration `20260101000044`, deferred from M1/M3 per `DECISIONS.md` 2026-07-30). Also renamed `support_access_sessions.admin_user_id` → `platform_admin_id` to match `SUPER_ADMIN.md` §6's own documented column name — a real mismatch between the shipped column and the spec, closed in the same migration. Confirmed live post-reset: `platform_admin_users` exists, `admin_users` doesn't, `is_platform_admin()` exists. All ~9 `apps/admin` call sites and 2 pgTAP fixture files updated to match.
- [x] Dashboard metrics, client directory, billing/plan configuration, support-mode (`SUPER_ADMIN.md` §2-6): `apps/admin/lib/superAdmin.ts` (`listPlatformOrganizations()`, `getPlatformOrganizationDetail()`, `computePlatformMetrics()`, `updateOrganizationStatus()`) + `apps/admin/lib/audit.ts` (`writeAuditEvent()` — the first real writer of `audit_events` in the whole codebase, unblocked by M18's TD-14 cutover). 12 new API routes under `/api/v1/admin/**` matching `API_SPEC.md` §2's exact list: organizations list/detail, activate/suspend/archive, plan (covers price override + discount), credits, usage + usage/reset, support-sessions start/end, plans list/create. Every mutating route writes an `audit_events` row (`API_SPEC.md` §0).
- [x] `customers/page.tsx`/`CustomersTable.tsx`, `customers/[id]/page.tsx`, `subscriptions/page.tsx`/`SubscriptionsTable.tsx`, `overview/page.tsx` rebuilt to read organizations/`organization_subscriptions`/`plans` instead of individual `profiles`/the old per-user `subscriptions` table (`SUPER_ADMIN.md` §0's explicit "not reused" list). This resolves `TECHNICAL_DEBT_REGISTER.md` TD-16 / `RISK_REGISTER.md` R-21's confirmed live-mode bug for these specific files — the old `customers/page.tsx` query joined on `properties.owner_user_id`, which every property created through the real M6 API leaves null. Demo mode (`ADMIN_DEMO_MODE`) intentionally left untouched on all four pages — it's cosmetic-only mock data, not the real data path these fixes target. Buttons for the new actions (activate/suspend/archive/plan-change/credits) are now wired (Design phase + 2026-08-01, `OrganizationActionsPanel`); support-session start is deliberately not — see this section's Support-mode bullet below.
- [x] **Two real, unrelated bugs found and fixed while building this milestone's own read/write paths**, both in already-shipped schema: (1) `packages/types/src/enums.ts`'s `ORGANIZATION_STATUSES` was missing `'archived'`, even though the Postgres enum gained it in migration `20260101000025` (2026-07-31) — never caught because nothing had ever tried to use the value in TS until this milestone's archive action. (2) `plans` (migration `20260101000019`) declared both a column-level `unique` on `code` and a table-level `unique (code, version)` — the column-level one made plan _versioning_ (the documented design) impossible, since it blocks ever inserting a second row with an existing code regardless of version. Fixed via migration `20260101000046` (drops the erroneous constraint by its confirmed-live auto-generated name, `plans_code_key`). See `DECISIONS.md` 2026-08-01.
- [x] `admin_organization_counts(uuid[])` (migration `20260101000045`) — batched per-org properties/units/owners/tenants/staff counts for the client directory, avoiding N+1. Applied the `resolve_whatsapp_sender()` EXECUTE-grant lesson proactively: revoked `EXECUTE` from `anon`/`authenticated` in the same migration, before it was ever committed, with a pgTAP regression test proving it.
- [x] **Organization-level payment gateway abstraction, 2026-08-02** (migration `20260101000054`, `SUBSCRIPTIONS.md` "Organization-level web billing" section — a genuinely new capability item, not something SUPER_ADMIN.md's original scope list already named): `BillingGatewayProvider` (`packages/types/src/billing.ts`) is a vendor-agnostic interface (`createCustomer`/`createSubscription`/`getPaymentStatus`/`cancelSubscription`/`refundPayment`/`verifyWebhookSignature`/`parseWebhookEvent`) so a real South African gateway (PayFast/Yoco/Stitch) can be added later without rewriting the subscription business logic that calls it — `MockBillingGatewayProvider` is the only implementation (no real gateway account exists), deterministic per `idempotencyKey`, never returns a real-looking checkout URL. New `billing_events` table is the real idempotency guard: a `unique(provider_name, provider_event_id)` constraint makes a retried webhook delivery a no-op, not a double-processed payment — proven with a real replayed-webhook test, not just documented as a design intent. API: `POST .../billing/checkout`, `.../billing/cancel`, `GET .../billing/payments` (closes this row's own "payments-history... not built" note below — this is that endpoint, scoped to subscription_payments), `POST /api/v1/admin/subscription-payments/:id/refund`, `POST /api/v1/billing/webhook` (unauthenticated, signature-verified, the real gateway's own callback target). Web UI: a Billing panel on `/customers/:id` (payment history table, checkout/cancel actions, super_admin only). No real billing was activated — mock provider only, by design.
- [x] Tests: `apps/admin/lib/providers/__tests__/billing.test.ts` (6 cases) + `apps/admin/lib/__tests__/billing.test.ts` (6 real integration tests against local Supabase — checkout, webhook-driven payment/org status transitions, replayed-webhook idempotency verified against the real unique constraint, failed-payment handling, cancellation, missing-signature rejection) + `supabase/tests/billing_events_isolation.test.sql` (7 pgTAP assertions). Full regression: **252/252 pgTAP assertions across 18 files**. Full monorepo `pnpm typecheck` (6/6 non-mobile packages), `apps/admin` `eslint --max-warnings=0`/`vitest` (139/139) clean, real `next build` (4 new routes, no conflicts).
- [ ] Support-mode's "read-only by default, explicit escalation per write" _enforcement_ mechanism (`SUPER_ADMIN.md` §6, items 1-2) is still not built. The original reason ("no client-org-facing dashboard UI to browse during a support session") no longer holds — M20's `(dashboard)` route group exists now — but the enforcement itself is a real, non-trivial RLS/API-layer authorization mechanism (granting a platform admin viewer-equivalent read access into a specific target org's tables, something no policy in this schema does today) rather than a UI wiring task, and building a cross-tenant read-access pathway without an explicit go-ahead isn't a call to make unilaterally (`PERMISSIONS.md`'s tenant-isolation protections are never waived). The session lifecycle itself (start/end, reason requirement, `actions_taken` log shape) is built and tested and its API endpoints work; deliberately not wired into the UI yet either (2026-08-01, reviewed alongside the tenant portal work) — surfacing a "start support session" control before the scoping it implies actually exists would be misleading. Plan/price/discount change UI _was_ wired into `customers/[id]/page.tsx` this pass (`OrganizationActionsPanel`'s "Change plan" section) — that one's a straightforward UI-to-already-built-endpoint gap, not a new authorization mechanism, so it didn't need the same pause.
- [ ] Account-recovery workflow — explicitly flagged (`SUPER_ADMIN.md` §7.6) as needing its own identity-verification design before implementation; not attempted.
- [ ] Payments-history, audit-history, and resend-onboarding endpoints `SUPER_ADMIN.md` §4 suggests are not in `API_SPEC.md`'s ratified endpoint list (§2) — not built, matching this session's consistent discipline of building to the spec's actual closed list rather than every idea a supporting doc floats. Organization detail (`GET .../organizations/:orgId`) does include `recentPayments` inline, which covers the most common "view payments" need without a separate endpoint.
- [ ] Churn-rate metric — `SUPER_ADMIN.md` §7.2 explicitly flags it as needing a dedicated helper view; excluded from `computePlatformMetrics()` rather than approximated.
- Tests: new `supabase/tests/super_admin_schema.test.sql` (9 assertions — the rename, the EXECUTE-grant regression, both bug-fix regressions). Full regression suite: **176/176 pgTAP assertions across 13 files**. Full monorepo `pnpm typecheck`/`pnpm lint` (7/7 packages), `pnpm --filter admin test` (26/26, unchanged — all new logic this milestone is DB-dependent, covered by pgTAP instead of vitest). Real `next build` — 12 new admin routes + no conflicts. Runtime smoke check (`next start`, demo mode): `/overview`, `/customers`, `/customers/[id]`, `/subscriptions` all return 200 with sensible content; demo-mode branches unchanged as designed. Live-mode branches verified via typecheck + pgTAP + code review, not an end-to-end browser click-through against real org data (no such click-through exists for any admin page in this session; consistent scope boundary, not a gap specific to this milestone).
- **Exit criteria**: platform-admin rename, dashboard/directory/billing/support-session data layer and API surface, and the two real bug fixes are done and execution-verified. Support-mode's data-scoping enforcement, account recovery, and the three non-ratified endpoints remain open — correctly blocked on either M20 (client-facing UI to enforce against) or an explicit design pass, not guessed at.

## Design phase — 2026-08-01, between M19 and M20

Not a numbered milestone (cross-cutting groundwork Mohammed asked for after M19, before more UI
work). `DESIGN_REVIEW.md`: full comparison of `reference/propview-screenshots/` against two
Envato UI-kit references, reuse/modernize/simplify/improve determination per pattern, role-specific
experience definitions (Owner/Tenant/Staff/Super Admin). `DESIGN_SYSTEM.md`: rewritten from its
Phase-1/single-owner-era version into the component-level single source of truth (buttons, cards,
tables, forms, modals, alerts, empty/loading/error states, responsive rules) the design review
calls for. First real implementation slice: `Button`/`EmptyState`/`StatusBadge` components,
`ORGANIZATION_STATUS_PRESENTATION` (closing a real display bug the M19 rebuild introduced — see
`DECISIONS.md`), and `OrganizationActionsPanel` wiring M19's activate/suspend/archive/credits
endpoints into the organization detail page for the first time. `NATIVE_IOS_SPEC.md`/
`NATIVE_ANDROID_SPEC.md`: implementation-ready specifications (navigation, screens, component
mapping, HIG/Material 3 compliance, state management, offline, accessibility, animations,
notifications, deep links, biometric auth, tablet behaviour) — specification only, no `.swift`/
`.kt` source, since this environment has no Xcode/confirmed Android toolchain to verify against.

## M20 — Responsive Web

- [x] **P0 fix, 2026-08-02: production CSP was blocking hydration on every admin page since the first commit.** Found via the first real-browser check run against the app this session (a standalone `puppeteer-core` script, since Chrome DevTools MCP can't connect mid-session — see DECISIONS.md for the full tooling story): `/overview` rendered as a permanently frozen `loading.tsx` skeleton in a real browser, despite every past `curl`-based smoke test this session reporting success. Root cause: `next.config.ts`'s static `Content-Security-Policy` (`script-src 'self'`, no nonce/`'unsafe-inline'`) blocked every one of Next.js's own inline hydration `<script>` tags, in every browser, on every page, since commit `ce0f389`. Fixed with Next.js's documented nonce pattern in a migrated `proxy.ts` (Next 16 deprecated `middleware.ts`); `/login`/`/onboarding/create-organization` split into thin dynamic `page.tsx` + relocated client `*Form.tsx` (nonces require dynamic rendering, these two were the only static pages). Verified with the same real-browser tooling that caught it: `/overview` (light+dark), `/login`, `/dashboard` all fully hydrate with zero CSP console errors — screenshot-confirmed, not just HTTP 200. Full writeup in `DECISIONS.md` 2026-08-02.
- [x] **PWA redesign foundation, 2026-08-02** (`UI_REDESIGN_PLAN.md`): shared `AppShell` component (responsive icon-rail/full-sidebar/overlay-drawer per `DESIGN_SYSTEM.md`'s own long-unimplemented spec, real dark-mode activation via `next-themes` plus a System/Light/Dark toggle, `lucide-react` icons replacing hand-rolled SVGs) now backs all three route-group layouts. Two real, previously-invisible bugs found and fixed by the new real-browser audit tooling: (1) dark mode never activated anywhere (`tailwind.config.ts`'s `darkMode: 'class'` had no `.dark`-setting mechanism) — every `dark:` class written this session was correct and unreachable; (2) `DESIGN_SYSTEM.md`'s documented breakpoint scale (`md 1024/lg 1280`) was never actually configured, silently using Tailwind's stock `768/1024` instead. Verified with real screenshots (not just curl) at 1440/1100/390px, light and dark, plus a scripted drawer-open interaction — full writeup `DECISIONS.md`/`WORKLOG.md` 2026-08-02. Admin typecheck/lint/test (114/114) and a real production build all clean.
- [x] **Design foundation v2 + Owner Dashboard redesign, 2026-08-03** (`UI_REDESIGN_PLAN.md` 2026-08-03 entry): full premium-palette redesign against `reference/lovable-ui-reference` (audited alongside `reference/propview-screenshots`). `packages/ui/src/tokens.ts`'s `colorLight`/`colorDark` values replaced with precise sRGB conversions (not eyeballed) of the reference project's OKLCH palette — blue accent (`#106ADD` light / `#4A91F8` dark), soft near-white/near-black-blue surfaces, a 5-colour chart palette, new `shadow` (`card`/`lift`/`glow`) and expanded `radii` (`card`/`panel`) tokens — same key names throughout, so every pre-existing `text-light-textPrimary`-style class kept working unchanged. Plus Jakarta Sans (display) + Inter (body) added via self-hosted `next/font/google`. New shared components: `PageHeader`, `Panel`, plus `AdminMetricCard` gained optional `icon`/`href` props (backward-compatible). Owner Dashboard (`/dashboard`) rebuilt on top: real 6-month rent-collected-vs-expenses area chart (`recharts`, new dependency — justified by the explicit "strong data visualization" requirement, no existing lightweight chart component supported gradients/tooltips), a point-in-time occupancy donut (deliberately not a fabricated trend — no historical occupancy-snapshot table exists to compute one honestly), a real `audit_events`-backed recent-activity feed, and an icon-tile quick-actions grid. Verified: `apps/admin` typecheck/lint/vitest (153/153) clean, real `next build` clean, real-browser check (puppeteer + system Chrome) at 1440/1280/1024/768/390 light + 1440 dark — zero non-prefetch console/network errors beyond a pre-existing unrelated missing-favicon 404. One real bug found and fixed by the browser check: the money-flow chart's Y-axis labels were clipped by a margin value copied from the reference without adjusting for this chart's own tick width.
- [x] **List-page consistency pass, 2026-08-03** (`UI_REDESIGN_PLAN.md` 2026-08-03 entry): `/properties`, `/units`, `/owners`, `/tenants`, `/leases`, `/maintenance`, `/inspections`, `/documents` all adopted the shared `PageHeader` component in place of ad hoc `<h1>`/`<p>`/button headers; `AdminDataTable`'s own wrapper (both empty and populated states, used by all 18 table components in the codebase) upgraded to the new `rounded-card`/`shadow-card`/`bg-light-surfaceRaised` card language with a `bg-light-surfaceStrong` header row — one foundational change that carries every list page onto the new visual language at once, avoiding a double-border problem a per-page `Panel` wrapper would have caused (tried and reverted on Properties first). No data/API/schema changes. Verified: typecheck/lint clean, full `vitest` 153/153, real `next build` clean, real-browser check (puppeteer + system Chrome, demo mode) across all 8 pages at 1440px plus 768/390/dark spot checks — zero console/network errors beyond the pre-existing favicon 404, confirmed via an isolated single-navigation check.
- [x] **Detail-page consistency pass, 2026-08-03** (`UI_REDESIGN_PLAN.md` 2026-08-03 entry): `/properties/[id]`, `/properties/[id]/units/[unitId]`, `/owners/[id]`, `/tenants/[id]`, `/leases/[id]` adopted `PageHeader` (title/edit action) and `Panel` (key-facts `dl` block) in place of a bare `<h1>` + floating `dl`. Nested-table sections (Units/Maintenance on a property; Leases/Applications/Inspections on a unit) deliberately left as lightweight header rows, not `Panel`-wrapped, for the same reason `AdminDataTable` itself was upgraded rather than wrapped in the list-page batch. No data/API/schema changes. Verified: typecheck/lint clean, full `vitest` 153/153, real `next build` clean, real-browser check across all 5 pages light + Properties dark — zero console errors beyond the pre-existing favicon 404.
- [x] **Create/edit form consistency pass, 2026-08-03** (`UI_REDESIGN_PLAN.md` 2026-08-03 entry): `NewPropertyForm`, `UnitForm`, `OwnerForm`, `TenantForm`, `LeaseForm` — all 5 forms now use `PageHeader` for the title and `Panel className="max-w-xl"` to contain the form instead of floating directly on the page background. Field/input styling and validation/submit logic untouched. Closes out the Properties/Units/Owners/Tenants/Leases module group (list, detail, create, edit) before moving to Documents/OCR review. Verified: typecheck/lint clean, full `vitest` 153/153, real `next build` clean, real-browser check across 4 create-form routes light + Properties-new dark — zero console errors beyond the pre-existing favicon 404.
- [x] **Lovable UI donor-integration checkpoint, 2026-08-03** (`UI_INTEGRATION_PLAN.md`, branch `propertyvault/lovable-ui-integration`): strategy change from hand-building analogues to a controlled integration — `reference/lovable-ui-reference` audited for framework/routing/styling/licence compatibility (full findings table in the plan doc), then its strongest patterns adapted directly. `AppShell` gained a real desktop header it never had (breadcrumbs, notifications popover backed by real `notifications` rows, user menu with real Supabase sign-out). Owner Dashboard swapped a real "Expiring leases" stat in for "Vacant units". Properties gained a card-grid default view (grid/list toggle, real per-property income/outstanding/occupancy from units+leases+rent_schedules, placeholder icon in place of a hotlinked stock photo — no property-photo storage exists). Property detail gained a hero header (placeholder band, status pill, stat strip). Units gained a real status-tab filter + search. Tenants gained avatar-initial chips. New shared `Pill`/`Meter`/`Avatar`/`Delta` components plus `DropdownMenu`/`Popover` (adapted from shadcn, restyled onto existing tokens, `@radix-ui/react-dropdown-menu`+`@radix-ui/react-popover` the only two new dependencies). Deliberately not ported: `portfolioValue`/property valuations and a fabricated "Vault Intelligence" AI-insight banner (no backing data, explicitly banned by this instruction), Tenants' master-detail single-pane pattern (would break `/tenants/[id]` deep-linking), and Property detail's full tab-per-module layout (Documents/Accounting/Maintenance are real separate modules, not visual tabs) — all documented as deliberate decisions in the plan doc. `reference/` added to `.gitignore` (development reference material only, never a runtime dependency). Verified: typecheck/lint clean, full `vitest` 153/153, real `next build` clean, real-browser check across all 5 checkpoint pages at 1440 light+dark plus 768/390 — zero console errors beyond the pre-existing favicon 404.
- [x] **Documents and OCR review redesign, 2026-08-03** (`UI_REDESIGN_PLAN.md` 2026-08-03 entry): `/documents/[id]` detail page + `OcrPanel` + `DocumentUploadForm` all moved onto `PageHeader`/`Panel`. `OcrPanel` specifically: reviewed-state now a dot+label badge in the `Panel` header (matching `StatusBadge`'s convention) instead of a plain sentence; each field's OCR confidence score is a small neutral pill next to its value instead of parenthetical text — deliberately left uncoloured since no confidence threshold is documented anywhere to justify a red/amber/green semantic. No extraction/review API or auto-apply logic changed (`DOCUMENT_INTELLIGENCE.md`'s human-confirms-first rule untouched). Verified: typecheck/lint clean, full `vitest` 153/153 (including `OcrPanel.test.tsx`'s existing 5 cases unmodified and still green against the restyled markup), real `next build` clean, real-browser check on the document detail page (light+dark) and upload form (light) — zero console errors beyond the pre-existing favicon 404.
- [x] **`(dashboard)`/`(super-admin)` route groups now correctly named per `ARCHITECTURE.md`** — resolved 2026-08-01: the `next dev -p 3005` process holding the lock was confirmed and stopped (Mohammed's explicit instruction), then `apps/admin/app/(dashboard)` (Super Admin, M19) → `(super-admin)` and `apps/admin/app/(portal)` (client-org, this milestone) → `(dashboard)`, both plain `git mv` renames with zero logic changes. Layout uses `resolvePortalSession()` (org-membership auth), never `getAdminSession()` (platform-admin auth) — a different auth model entirely, per `PERMISSIONS.md`'s "never merge role systems" principle.
- [x] **Properties**: list, detail, create pages (`(dashboard)/properties/**`) — the first complete vertical slice (layout → list → detail → role-gated create form), proving the pattern the remaining modules below repeat. Reuses `AdminDataTable`/`Button`/`EmptyState` from the design-system pass, `mapPropertyRow`/`POST /api/v1/properties` from M5/M6 unchanged.
- [x] **Units**: org-wide list (`/units`, KPI row, matching `PROPVIEW_SCREENSHOT_AUDIT.md`'s evidenced top-level nav item), embedded per-property list, detail, create, and edit pages (`(dashboard)/properties/[id]/units/**`) — second vertical slice, 2026-08-01. Reuses `mapUnitRow`/`requireOrgRole` and the M6 Units API unchanged; added `UNIT_STATUS_PRESENTATION` (`packages/ui`) and a `PageLoading` skeleton + `loading.tsx` for the three data-fetching routes (a small improvement over Properties' own precedent, which predates the explicit per-module loading-state requirement). Role-gated create/edit (agent+ only, server-side redirect + API-layer re-check). Tests: `UnitsTable.test.tsx` (4 cases); needed a `vitest.config.ts` `@/`-alias fix, the first component test in this codebase to import an `@/`-aliased module. Verified: typecheck/lint/test (36/36) clean, real `next build` clean, runtime smoke test (`next start`, demo mode) across all 6 new/changed routes — 200s with response bodies grepped for real content.
- [x] **Tenants**: org-wide list (`/tenants`, matching `PROPVIEW_SCREENSHOT_AUDIT.md`'s LEASING nav section, not property-nested), detail, create, and edit pages (`(dashboard)/tenants/**`) — third vertical slice, 2026-08-01. Reuses `mapTenantRow`/`requireOrgRole` and the M8 Tenants API unchanged; form deliberately has no `status` field (`tenantSchema` excludes it from client input — server-set only). Added `TENANT_STATUS_PRESENTATION` (`packages/ui`), loading states for `/tenants`/`/tenants/[id]`. Role-gated create/edit (agent+ only). Tests: `TenantsTable.test.tsx` (3 cases). Verified: typecheck/lint/test (39/39) clean, real `next build` clean, runtime smoke test (`next start`, demo mode) across all 4 new routes — 200s with response bodies grepped for real content.
- [x] **Leases**: org-wide list (`/leases`, KPI row: Total/Active/Draft, joined against units→properties for context), embedded per-unit list, detail, and edit pages (`(dashboard)/leases/**`, `(dashboard)/properties/[id]/units/[unitId]/leases/new`) — fourth vertical slice, 2026-08-01. Reuses `mapLeaseRow`/`requireOrgRole` and the M10 Leases API unchanged. Create is always reached from a unit's own context (no unit-picker UI); no `status` field on create (server-set `draft`), no `rentFrequency` field (single-value enum, DB default). Added `LEASE_STATUS_PRESENTATION` (`packages/ui`), loading states for `/leases`/`/leases/[id]`. Role-gated create/edit (agent+ only). Tests: `LeasesTable.test.tsx` (4 cases). Verified: typecheck/lint/test (43/43) clean, real `next build` clean, runtime smoke test (`next start`, demo mode) across all 6 new/changed routes — 200s with response bodies grepped for real content.
- [x] **Maintenance**: org-wide board (`/maintenance`, KPI row per real `MAINTENANCE_STATUSES` value, grouped-by-status columns matching `PROPVIEW_SCREENSHOT_AUDIT.md`'s evidenced "Maintenance Board" — not full drag-and-drop, a confirmed V1 scope reduction), embedded per-property list, detail, and edit pages (`(dashboard)/maintenance/**`, `(dashboard)/properties/[id]/maintenance/new`) — fifth vertical slice, 2026-08-01. Reuses `mapMaintenanceTicketRow`/`requireOrgRole` and the M13 Maintenance Tickets API unchanged. Tickets are always reported from a property's own context; no unit/lease/tenant/vendor linkage fields (no picker UI exists yet, vendor assignment specifically has no Vendors module UI at all). Status transitions are not client-validated against `MAINTENANCE_TRANSITIONS` (server-only lib) — illegal transitions surface as a 409 through the form's existing error banner. Added `MAINTENANCE_STATUS_PRESENTATION`/`MAINTENANCE_PRIORITY_PRESENTATION` (`packages/ui`), loading states for `/maintenance`/`/maintenance/[id]`. Role-gated create/edit (agent+ only, same threshold PERMISSIONS.md's Maintenance column confirms). Tests: `MaintenanceTable.test.tsx` (2 cases), `MaintenanceBoard.test.tsx` (2 cases). Verified: typecheck/lint/test (47/47) clean, real `next build` clean, runtime smoke test (`next start`, demo mode) across all 6 new/changed routes — 200s with response bodies grepped for real content.
- [x] **Owners**: org-wide list (`/owners`, matching `PROPVIEW_SCREENSHOT_AUDIT.md`'s PORTFOLIO nav section), detail, create, and edit pages (`(dashboard)/owners/**`) — sixth vertical slice, 2026-08-01. Reuses `mapOwnerRow`/`requireOrgRole` and the M7 Owners API unchanged; no `status` field on the form (`ownerSchema` doesn't expose it). Small local status badge on `OwnersTable` rather than growing `packages/ui`'s `StatusPresentation` map for a plain inline `'active'|'inactive'` union with no other consumer. Role-gated create/edit (agent+ only). Tests: `OwnersTable.test.tsx` (3 cases). Property-ownership share attachment (`POST /api/v1/properties/:id/owners`) and owner statements not yet wired in — noted explicitly, not silently omitted. Verified: typecheck/lint/test (50/50) clean, real `next build` clean, runtime smoke test (`next start`, demo mode) across all 4 new routes — 200s with response bodies grepped for real content.
- [x] **Applications**: org-wide list (`/applications`, KPI row: Submitted/Screening/Decided, matching `PROPVIEW_SCREENSHOT_AUDIT.md`'s LEASING nav section), detail page with a workflow action panel, and a create page (`(dashboard)/applications/**`, `(dashboard)/properties/[id]/units/[unitId]/applications/new`) — seventh vertical slice, 2026-08-01. Reuses `mapApplicationRow`/`requireOrgRole` and the M9 Applications API unchanged. No generic edit form exists (API_SPEC.md §4 only exposes consent/screen/decide as distinct endpoints, no PATCH) — `ApplicationActions` is the edit surface instead, shaped around the real state machine (independent POPIA/screening consent, screening gated on consent, approve/decline gated on not-yet-decided). Approving calls `approve_application()`, which atomically creates the tenant+lease+first rent-schedule row. Added `APPLICATION_STATUS_PRESENTATION`/`APPLICATION_SCREENING_STATUS_PRESENTATION`/`APPLICATION_DECISION_PRESENTATION` (`packages/ui`). Tests: `ApplicationsTable.test.tsx` (3 cases), `ApplicationActions.test.tsx` (4 cases); needed a `next/navigation` `useRouter` mock, the first component test in this codebase to render something that calls `useRouter()` directly. Verified: typecheck/lint/test (57/57) clean, real `next build` clean, runtime smoke test (`next start`, demo mode) across all 3 new routes — 200s with response bodies grepped for real content, including the consent/screening action states.
- [x] **Inspections**: org-wide list (`/inspections`, KPI row: Scheduled/Awaiting signature/Completed, matching `PROPVIEW_SCREENSHOT_AUDIT.md`'s OPERATIONS nav section), detail page with a workflow action panel, and a create page (`(dashboard)/inspections/**`, `(dashboard)/properties/[id]/units/[unitId]/inspections/new`) — eighth vertical slice, 2026-08-01, closing out the CRUD/workflow-shaped modules before the remaining M20 scope shifts to genuinely different UI shapes. Reuses `mapInspectionRow`/`mapInspectionItemRow`/`requireOrgRole` and the M13 Inspections API unchanged. No generic edit form (API_SPEC.md §5 only exposes items/sign/complete) — `InspectionActions` is the edit surface, matching `ApplicationActions`' shape: inline item-adding, independent landlord/tenant signature capture (tenant signs OR logs a refusal reason), Complete button disabled until the API's own hasBothSignatures-or-refusal precondition is met. No `GET /api/v1/inspections/:id` endpoint exists at all — the detail page reads directly via RLS, same as every detail page this milestone regardless of whether a GET/:id route exists. Added `INSPECTION_STATUS_PRESENTATION`/`INSPECTION_CONDITION_RATING_PRESENTATION` (`packages/ui`). Tests: `InspectionsTable.test.tsx` (2 cases), `InspectionActions.test.tsx` (5 cases). Verified: typecheck/lint/test (64/64) clean, real `next build` clean, runtime smoke test (`next start`, demo mode) across all 3 new routes — 200s with response bodies grepped for real content.
- [x] **Accounting — Rent Due, Expenses, Trial Balance** (`(dashboard)/accounting/**`) — ninth vertical slice, 2026-08-01, first Accounting-domain UI. Deliberately scoped to the three screens with a straightforward, already-shipped API; bank matching, owner statements, and tax pack (each a distinct multi-step workflow, evidenced in `PROPVIEW_SCREENSHOT_AUDIT.md`'s FINANCE section) are explicitly not started. **Real role-gating correction**: every prior module used an agent+ write gate; Accounting's actual threshold is accountant+ (`PERMISSIONS.md`'s Accounting (post) column — agent gets none). Verified against `invoice_rent_schedule()`/`record_expense()` (migration `20260101000038`) directly, both call `has_org_role(org_id, 'accountant')` internally. Added `canWriteOrgRecords()`/`canPostAccountingRecords()` to `apps/admin/lib/orgSession.ts` (two explicit non-overlapping role-list checks, not a linear rank — agent/accountant are siblings per `has_org_role()`'s own comment), unit tested (`orgSession.test.ts`, 5 cases) since a wrong role list here would silently let agents post financial entries. Rent Due: direct `rent_schedules` read + status filter + "Issue invoice" action. Expenses: list/create/detail + "Record expense" action (paidImmediately copy verified against `record_expense()`'s actual Dr/Cr behavior). Trial Balance: read-only report (viewable by every role), ledger_class filter, live Balanced check — reproduces `GET /api/v1/trial-balance`'s aggregation query directly rather than calling its own API route over HTTP. Added `RENT_SCHEDULE_STATUS_PRESENTATION`/`EXPENSE_STATUS_PRESENTATION`/`INVOICE_STATUS_PRESENTATION` (`packages/ui`). Tests: `ExpensesTable.test.tsx` (2), `RentScheduleTable.test.tsx` (4). Verified: typecheck/lint/test (75/75) clean, real `next build` clean, runtime smoke test (`next start`, demo mode) across all 7 route/query-param combinations — 200s with response bodies grepped for real content.
- [x] **Notifications** (`(dashboard)/notifications/**`) — tenth vertical slice, 2026-08-01. A personal inbox, not org data (RLS's `notifications_select_own`/`notifications_update_own`/`notification_preferences_all_own` scope everything to the caller — no agent+/accountant+ role gate applies at all, the first module this milestone where that's true). `/notifications`: list with unread/read distinction, "Mark as read" (no create UI — notifications are always system-generated, no `POST /api/v1/notifications` exists). `/notifications/preferences`: one row per `NOTIFICATION_CATEGORIES` value × 3 channel toggles, each PATCHing immediately as a per-category upsert; categories with no row yet default every channel to checked, verified against the actual DB column defaults (migration `20260101000039`), not guessed. Tests: `NotificationsList.test.tsx` (3), `NotificationPreferencesForm.test.tsx` (3). Verified: typecheck/lint/test (81/81) clean, real `next build` clean, runtime smoke test across both routes — 200s with real content.
- [x] **Announcements** (`(dashboard)/announcements/**`) — eleventh vertical slice, 2026-08-01. Org-wide list + publish form, matching `PROPVIEW_SCREENSHOT_AUDIT.md`'s OPERATIONS nav section. List-and-create only, no detail/edit page — `API_SPEC.md` §5 has no PATCH for announcements at all, and the only other endpoint (`POST /:id/acknowledge`) is tenant-only (out of scope at the time — a tenant portal didn't exist yet; it does now, see this section's Tenant portal entry below). First slice publishes org-wide only (no property picker — no evidenced "target one property" UI pattern to copy yet). Role-gated create using the newly centralized `canWriteOrgRecords()` helper. Tests: `AnnouncementsTable.test.tsx` (3 cases). Verified: typecheck/lint/test (84/84) clean, real `next build` clean, runtime smoke test across both routes — 200s with real content.
- [x] **Documents + OCR review** (`(dashboard)/documents/**`) — twelfth vertical slice, 2026-08-01. First real Documents module implementation (M11 only did schema/RLS); details under `TASKS.md` M11.
- [x] **Payments — bank accounts + bank transaction matching** (`(dashboard)/accounting/bank-accounts/**`, `.../bank-transactions/**`) — thirteenth vertical slice, 2026-08-01. Web UI for the already-shipped M14-part-2 API; details under `TASKS.md` M14.
- [x] **Reports** (`/reports`) — fourteenth vertical slice, 2026-08-01, moved up from its original M25 slot. 4 evidenced report cards; details under `TASKS.md` M25.
- [x] **Owner Dashboard** (`/dashboard`) — fifteenth vertical slice, 2026-08-01. KPI row (Properties/Units occupied %/Cash left this month/Units available) + quick links, matching `PROPVIEW_SCREENSHOT_AUDIT.md`'s evidenced Dashboard. Building this surfaced and fixed a real pre-existing bug: no client-org user could ever reach their portal via `/login` (root `/` only checked platform-admin auth) — full root-cause and fix in `DECISIONS.md` 2026-08-01. Also brought `middleware.ts`'s protected-route list up to date with all 12 route segments added since it was last touched (a live UX gap, never a data-exposure one — every page independently enforces its own auth regardless). Verified: typecheck/lint/test (103/103) and real `next build` clean, demo-mode smoke test confirming `/` still resolves to `/overview` unchanged and `/dashboard` renders correctly.
- [x] **Tenant portal** (`(tenant)/**` — `/my-lease`, `/my-payments`, `/my-maintenance` + `/new`, `/notices`) — sixteenth vertical slice, 2026-08-01, V1 scope correction superseding every earlier "no tenant portal in V1" note in this codebase (full narrative `DECISIONS.md` 2026-08-01). New RLS in `supabase/migrations/20260101000049_tenant_portal_rls.sql` (`caller_tenant_ids()`/`caller_is_tenant_of_lease()`/`caller_is_tenant_of_unit()`/`caller_is_tenant_of_property()` — SECURITY DEFINER helpers, same pattern as `has_org_role()`) and `resolveTenantSession()` (`lib/tenantSession.ts`), a third independent identity system alongside org-staff/platform-admin sessions, plus a third branch on `/`'s routing. Real bug found and fixed while building the migration: a first-draft raw cross-table subquery caused `42P17` RLS recursion between `leases`/`lease_tenants` — pgTAP caught it (3 of 13 suites failing) before any commit; fixed with the SECURITY DEFINER wrapper functions, re-verified clean 176/176. Tenant-submitted maintenance tickets go through a new `POST /api/v1/tenant-portal/maintenance-tickets` route (org/property/unit/lease context derived server-side from the caller's own lease, never client-supplied) rather than the staff-only `/api/v1/maintenance-tickets` route. Documents remain staff/owner-only by default — tenant-visible only when explicitly tagged via the new `documents.lease_id` column, not a blanket property-scoped grant (owner-only paperwork stays invisible to tenants, `PERMISSIONS.md` §4). Tests: `NoticesList.test.tsx` (3), `TenantMaintenanceTicketForm.test.tsx` (2). Verified: full pgTAP (176/176), admin typecheck/lint/test (108/108), real `next build` clean, demo-mode smoke test across all 5 new routes — 200s with real rendered content (lease/unit/property names, rand-formatted balances, ticket summaries, notice acknowledgement state).
- [x] **UI consistency audit (priority 11), 2026-08-01**: checked every `(dashboard)`/`(super-admin)`/`(tenant)` page against the loading-state/shared-component conventions established across the vertical-slice pass. Found and fixed two real gaps predating the convention: `/properties` (the first vertical slice, before the explicit per-module loading-state requirement existed) and all six `(super-admin)` pages (the whole M19 milestone predates it) had no `loading.tsx` — added all seven. Table components (17/17), dark-mode class coverage, and Super Admin badge/health-indicator usage were already consistent — no other gaps found. Verified: typecheck/lint/test (112/112, unchanged) and real `next build` clean.
- [ ] Bank matching's proposal step (TD-22), Owner statements, Tax pack, AI Assistant chat UI, Portfolio Intelligence feed — not started, same one-module-at-a-time vertical-slice pattern as every module above.
- [ ] Simplified Portfolio Map UI (property list on a map, no GIS/heatmap layers — confirmed V1 scope).
- **Exit criteria**: not started as a full milestone; fifteen vertical slices done. Design system, role-experience groundwork, both permission gates (agent+ and accountant+), and the client-org login/routing path are no longer a blocker for starting the rest.

## M21 — Native iOS

- [x] Implementation-ready design specification (`NATIVE_IOS_SPEC.md`, 2026-08-01) — navigation architecture, screen hierarchy, component mapping, HIG compliance, state management, offline behaviour, accessibility, animations, notifications, deep links, biometric auth, tablet behaviour.
- [x] **Specification reconciled against shipped reality, 2026-09-01** (`NATIVE_IOS_SPEC.md` §16): the original spec pre-dates `apps/android` and several backend changes (`invoice_payments` ledger, migrations 158-162; the malware-scanning fail-closed upload gate). Added: an explicit scope-narrowing recommendation (§3/§4's V1 is ahead of what Android actually shipped — Owner Statements/Inspections/Approvals-as-a-screen/Tasks/AI Assistant should be post-V1 for iOS too, pending Mohammed's confirmation, not a unilateral cut); the exact session-refresh strategy to implement (matching `apps/android`'s just-verified `TokenAuthenticator`: one retry max, concurrent-401 refresh dedup, immediate sign-out-in-place on unrecoverable failure — not just on next cold launch); the malware-upload-gate UX contract (surface the server's own `error.message` verbatim, never invent wording or reference the scanning provider); biometric-lock parity confirmation (already correctly scoped, cross-checked against Android's shipped `BiometricGateViewModel`).
- [x] **Reviewable Swift source written, 2026-09-01** (`apps/ios/Sources/Proplyst/` — no Xcode project, `apps/ios/README.md` explains why and how to bring it into one): domain models + `Codable` DTOs (`Property`/`PropertyUnit`/`Tenant`/`Lease`/`MaintenanceTicket`/`PaymentReport`/`AppNotification`/`Announcement`/`AuthState`, every field mirroring `apps/android`'s equivalent exactly), the API error model (`APIError`, mirrors the server's `{error:{code,message}}` shape and Android's `WebApiErrorBody`), `APIClient` (a `URLSession` actor implementing the exact refresh-on-401 strategy above), `KeychainSessionStore` (`kSecAttrAccessibleWhenUnlockedThisDeviceOnly`), `AuthRepository` protocol + a complete deterministic mock, `BiometricAuthenticator`/`BiometricLockState` (`LocalAuthentication`, externally-driven scene-phase handling — deliberately NOT fetching a global lifecycle signal internally, the exact lesson learned live fixing an equivalent Android bug this same pass), repository protocols for every V1-scope domain, and a design-token file structure (§15, values deliberately left as placeholders pending the still-unbuilt codegen step, not invented). Not written: any SwiftUI `View`, any Xcode project file (real, `.pbxproj` is not hand-editable), any signing/provisioning. `IOS BUILD VERIFIED: NO` — no compiler available this session; explicitly not claimed.
- [ ] Xcode project scaffold, Swift/SwiftUI, Supabase Swift SDK, Keychain-backed auth (`MOBILE_ARCHITECTURE_DECISION.md`) — genuinely blocked on macOS/Xcode access, not on more preparation.
- [ ] Role-aware navigation shell (portal switcher), biometric re-auth gate for sensitive actions (`SECURITY.md` § Auth) — protocols/state machine ready (see above), SwiftUI wiring needs Xcode.
- [ ] Screens per `NATIVE_IOS_SPEC.md` §3/§4 (narrowed per §16.1), Maintenance flow first (explicit master-prompt priority).
- **Exit criteria**: specification done and execution-verified as a document (self-consistent, cross-referenced, grounded in the real M4-M19 API surface, now also reconciled against shipped Android/backend reality). The backend-contract Swift layer (domain/DTOs/error model/networking/auth/biometric/repository protocols) is written and reviewable, not compiler-verified. Actual Xcode build: not started; blocked on an environment with Xcode (requires macOS), not on more specification or more preparable source.

## M22 — Native Android

- [x] Implementation-ready design specification (`NATIVE_ANDROID_SPEC.md`, 2026-08-01) — mirrors M21's spec scope with Kotlin/Compose/Material-3-specific detail.
- [x] **Android Studio installed and toolchain verified, 2026-08-01** — full, dated, command-output-backed status table in `apps/android/README.md` "Toolchain status". Real gaps found and fixed: `cmdline-tools` was missing (installed), no AVD existed (created — `PropertyVault_Pixel7_API35`), the Android Studio bundled JBR (OpenJDK 25.0.2) is incompatible with Gradle 8.7's bundled Kotlin DSL compiler (a real, reproduced `IllegalArgumentException`) — Eclipse Temurin 21 LTS installed and wired via `~/.gradle/gradle.properties` (`org.gradle.java.home`, not a system-wide `JAVA_HOME`).
- [x] **Project foundation built** (`apps/android/`, real, separate project — `apps/mobile` untouched, never converted, per Mohammed's explicit instruction): Gradle wrapper (bootstrapped for real, not hand-crafted), root `settings.gradle.kts`/`build.gradle.kts`, version catalog (`gradle/libs.versions.toml`), `app` module (Kotlin 2.0.21, AGP 8.6.0, Compose BOM 2024.10.01, Material 3, Hilt, KSP, Room, WorkManager, Retrofit/OkHttp, `kotlinx.serialization`, `androidx.security-crypto`/`androidx.biometric` dependencies present), `AndroidManifest.xml`, Compose theme (light/dark `ColorScheme` generated from `packages/ui/src/tokens.ts`'s values, hand-transcribed pending the cross-platform token-export step `NATIVE_ANDROID_SPEC.md` §14/`NATIVE_IOS_SPEC.md` §15 flag as not yet built), navigation foundation (Navigation Compose, per-tab back stacks), DI (Hilt modules for network/database/repositories), network client (Retrofit + OkHttp, plain REST against Supabase Auth/PostgREST — see `README.md`'s "why not supabase-kt"), auth/session abstraction (`SessionManager` via `EncryptedSharedPreferences`, `AuthRepository` interface + real/mock implementations), offline database foundation (Room, a read-through cache for Properties), unit-test structure (JUnit4, MockK, `kotlinx-coroutines-test`) and instrumentation-test structure (AndroidX Test/Espresso).
- [x] **First vertical slice built and verified end-to-end, 2026-08-01**: Authentication shell (splash/session-restore, sign-in, sign-out plumbing) + Owner portal (bottom-nav shell, Dashboard placeholder, Properties list, Property detail) with loading/empty/error states and a cached-data-banner foundation. `PropertiesRepository`/`AuthRepository` both split into real (Postgrest/Supabase-Auth-backed) + mock (deterministic fixture) implementations per Mohammed's explicit instruction, never mixed. Real verification, not claimed without command output: `gradlew assembleDebug` — BUILD SUCCESSFUL, real 20.6MB APK; `gradlew testDebugUnitTest` — 7/7 passed; `gradlew lintDebug` — 0 errors, 55 warnings; installed and launched on `PropertyVault_Pixel7_API35` via `adb`, confirmed via `logcat` (`Displayed com.propertyvault.app/.MainActivity`, no crash) and real screenshots: sign-in screen, owner bottom-nav, Properties list (mock data), Property detail with back-navigation, light mode, dark mode.
- [x] **Two real bugs found and fixed** while getting the first build green (full narrative `DECISIONS.md` 2026-08-01): Android XML comments rejecting `--` (this session's comment style elsewhere), and a persistent unresolved-reference failure on `com.jakewharton.retrofit:retrofit2-kotlinx-serialization-converter` despite it resolving correctly on both compile/runtime classpaths — replaced with a small hand-rolled `Converter.Factory` rather than continuing to debug an opaque toolchain issue.
- [x] **Units vertical slice, 2026-08-01** (priority 12, "Android feature parity" — one module at a time, same pattern as Properties): view-only (`MOBILE_ARCHITECTURE_DECISION.md` §6 lists Units as view-only in the native scope), reached from Property Detail's new "View units" button rather than a new bottom-nav tab (units only ever make sense in a property's context, matching `apps/admin`'s own original build order). Data layer: `PropertyUnit` domain model (named to avoid colliding with Kotlin's own `Unit`), `UnitDto`/`UnitEntity`/`UnitDao` (Room cache keyed on `propertyId`, `replaceForProperty()` — deliberately not `PropertyDao`'s whole-table `replaceAll()`, since a Units read is always scoped to one property, not the org), `UnitsRepository` real (`PostgrestUnitsRepository`, same write-through-cache-then-fallback shape as `PostgrestPropertiesRepository`) + mock (`MockUnitsRepository`, 2 fixture units under the same `demo-property-1` id `MockPropertiesRepository` already uses, so the demo flow is coherent end to end) implementations, never mixed. `PropertyVaultDatabase` bumped to version 2 (`fallbackToDestructiveMigration()` — acceptable since this is a read-through cache, never a source of truth). Navigation: two new routes (`properties/{propertyId}/units`, `.../units/{unitId}`) added to the existing shared `NavHost` in `OwnerRootScreen`, not a new nested graph. Tests: `MockUnitsRepositoryTest` (4 cases), `UnitsListViewModelTest` (4 cases, same `StandardTestDispatcher`/`advanceUntilIdle()` pattern `PropertiesListViewModelTest` established).
- [x] **Device/emulator verification completed, 2026-08-01** (closing the gap this slice and the Tenants slice both disclosed as not-yet-done): Mohammed installed a current Android Studio; booted the pre-existing `PropertyVault_Pixel7_API35` AVD, installed the real debug APK, and drove the full flow via `adb` — sign in (mock auth) → Dashboard/Properties/Tenants bottom-nav tabs → Property Detail → "View units" → Units list → Unit Detail → Tenants list → Tenant Detail — confirmed via `adb logcat` (no crash, `Displayed com.propertyvault.app/.MainActivity`) and real screenshots at every step, in both light and dark mode (`adb shell cmd uimode night yes`). **Real bug caught on-device and fixed**: Unit Detail rendered `sizeSqm`/`marketRent` as raw `Double.toString()` output — "R10650.0" / "65.0 m²" — since Kotlin's default `Double` formatting always keeps a trailing `.0`. Fixed with `formatCurrency()`/`formatArea()` helpers in `UnitDetailScreen.kt` (whole numbers print without decimals, e.g. "R10,650" / "65 m²"); rebuilt, reinstalled, re-verified the fix on the same device. Re-ran `gradlew testDebugUnitTest assembleDebug` after the fix — BUILD SUCCESSFUL, 22/22 unit tests still passing.
- [x] **Tenants vertical slice, 2026-08-01** (priority 12, continuing the one-module-at-a-time pattern): org-wide list + detail, a new "Tenants" bottom-nav tab (third tab, alongside Dashboard/Properties — added now that there's a real module behind it, matching this codebase's discipline against stubbing dead tabs) rather than nested under a property, mirroring `apps/admin`'s own Tenants module shape (org-wide, not property-nested) rather than Units' per-property shape. Identical stack to Units: `Tenant` domain model (`idNumberRef` deliberately left out — a pointer into `encrypted_secrets` with no view-only-screen use yet), `TenantDto`/`TenantEntity`/`TenantDao` (whole-table cache, same as `PropertyDao` — an org-wide read, not property-scoped), `PostgrestTenantsRepository` + `MockTenantsRepository`, never mixed. `PropertyVaultDatabase` bumped to version 3 (still `fallbackToDestructiveMigration()`). Tests: `MockTenantsRepositoryTest` (3), `TenantsListViewModelTest` (4). Verified: real `gradlew testDebugUnitTest assembleDebug lintDebug` in one run — BUILD SUCCESSFUL, 22/22 unit tests across the whole module (6 new + 16 pre-existing, none broken), real ~20.8MB APK, lint 0 errors/55 warnings (unchanged baseline). Device/emulator verification (Tenants list + detail, both bottom-nav tab and click-through) completed in the same pass as the Units slice's own device verification below.
- [x] **Leases vertical slice, 2026-08-01** (priority 12, continuing the one-module-at-a-time pattern): unit-scoped (a lease only makes sense for a specific unit, same "no new bottom-nav tab" reasoning as Units), reached from Unit Detail's new "View leases" button. `Lease` domain model (`source`/`sourceDocumentId`/`sourceApplicationId` deliberately left out — internal provenance fields with no view-only-screen use, same reasoning as `Tenant.idNumberRef`), `LeaseDto`/`LeaseEntity`/`LeaseDao` (Room cache scoped to `unitId`, `replaceForUnit()` mirroring `UnitDao.replaceForProperty()`), `PostgrestLeasesRepository` + `MockLeasesRepository` (one fixture lease under `demo-unit-1`, the occupied unit in the Units fixture, so an occupied unit having an active lease is a coherent demo). `PropertyVaultDatabase` bumped to version 4. Extracted `formatCurrency()`/`formatArea()` out of `UnitDetailScreen` into a shared `ui/common/NumberFormatting.kt` (both Unit Detail and the new Lease Detail need identical Double-formatting, and this is exactly the class of logic that shouldn't be copy-pasted after the bug it already caused once). Tests: `MockLeasesRepositoryTest` (4), `LeasesListViewModelTest` (4). Verified: real `gradlew testDebugUnitTest assembleDebug lintDebug` — BUILD SUCCESSFUL, 30/30 unit tests (8 new + 22 pre-existing, none broken), real APK, lint 0 errors/55 warnings (unchanged). **Also device-verified in the same pass**: booted the AVD, installed the APK, drove Property -> Unit -> View leases -> Leases list -> Lease Detail by hand via `adb`, confirmed via `logcat` (no crash) and real screenshots in light and dark mode — the formatted rent/deposit values ("R10,650") render correctly, confirming the extracted shared formatter carried the earlier fix over cleanly.
- [x] **Maintenance vertical slice, 2026-08-01** (priority 12, continuing the one-module-at-a-time pattern): **view-only**, org-wide list + detail, a new "Maintenance" bottom-nav tab (fourth tab). `MaintenanceTicket` domain model (a deliberate subset — `unitId`/`leaseId`/`tenantId`/`submittedBy*`/`assignedVendorId`/`resolvedAt` all left out, no view-only-screen use yet), `MaintenanceTicketDto`/`Entity`/`Dao`, `PostgrestMaintenanceRepository` + `MockMaintenanceRepository`. `PropertyVaultDatabase` bumped to version 5. Tests: `MockMaintenanceRepositoryTest` (3), `MaintenanceListViewModelTest` (4). Verified: real `gradlew testDebugUnitTest assembleDebug lintDebug` — BUILD SUCCESSFUL, 37/37 unit tests, lint 0 errors/55 warnings (unchanged); device-verified (AVD, light+dark, no crash) same as every prior slice.
      **Real gap found while scoping this slice, not built around**: `MOBILE_ARCHITECTURE_DECISION.md` §6/§7 explicitly calls Maintenance ticket _submission_ out as the native-app write-path priority ("full flow both directions"). Investigated wiring it and found `apps/admin`'s API routes authenticated cookie-session-only (`getServerSupabaseClient()`) — they never read the `Authorization: Bearer <jwt>` header `API_SPEC.md` §0 itself specifies as the contract "so native mobile apps consume the same API surface as the web app." A native POST with only a Bearer header would get an unconditional 401, for every mutating admin route, not just this one. Filed as `TECHNICAL_DEBT_REGISTER.md` TD-28. **Resolved 2026-08-02** (M10's own entry) — `getServerSupabaseClient()` now accepts a Bearer token through the same shared helper every route already uses; ticket submission is no longer blocked at the API-auth layer. The remaining work is Android-side only: the actual Kotlin screen/ViewModel to POST a real ticket with the stored access token, not yet built.
- [x] **Ticket submission, tenant portal, deep links, and further Android V1 hardening —
      2026-08-17/08-18/09-01** (this checklist section was stale relative to `WORKLOG.md`'s own
      more recent entries; corrected here rather than trusted at face value, per this pass's own
      "verify TASKS.md's checkboxes against the actual repository" instruction). Built across
      three passes: tenant portal payment reporting (role-aware routing between `OWNER_ROOT`/
      `TENANT_ROOT`), owner payment review (confirm/reject), owner monthly summary, in-app
      notification centre + preferences, tenant Maintenance (incl. ticket submission and photo/
      file attachment), Documents, Notices (incl. read/unread tracking), automatic token refresh
      (`TokenAuthenticator`), App Links with deep-link-to-subscreen resume, real branding/
      launcher icon, release build config hardening (`RELEASE_*` BuildConfig fields, no
      accidental mock-data-in-release). **Auth/session hardening pass, 2026-09-01**: closed a
      real, previously-undisclosed gap — `AuthRepository.signOut()` existed at every layer but had
      zero UI call site (no Settings/Account screen existed at all); built `AccountScreen` (sign
      out, biometric-lock toggle, app version), wired `RootNavGraph` to react to `AuthState`
      becoming `Unauthenticated` from anywhere (not just while the splash screen happened to be
      showing), and fixed `TokenAuthenticator`'s unrecoverable-refresh-failure path to flip
      `AuthState` immediately (`forceSignOutLocally()`) rather than only on the next cold launch.
      **Biometric re-auth, previously declared but never wired, now built**: `BiometricPrompt`
      (`BIOMETRIC_STRONG or DEVICE_CREDENTIAL`, no custom PIN) gates app foreground-from-
      background when enabled in Account settings; never traps the user if hardware/enrollment
      becomes unavailable while backgrounded. `MainActivity` changed `ComponentActivity` →
      `FragmentActivity` (required to host `BiometricPrompt`; a safe superset, no existing API
      lost). Verified: real `gradlew testDebugUnitTest lintDebug assembleDebug assembleRelease
      bundleRelease` — **171/171 unit tests passing** (was 161 before this pass's own 10 new
      tests), lint 0 errors (59 warnings, unchanged class of pre-existing `GradleDependency`/
      `OldTargetApi` items plus one documented `StaticFieldLeak` suppression on the new
      `BiometricGateViewModel`, a confirmed lint false-positive explained inline). **Remaining,
      real, disclosed gaps**: adaptive tablet/foldable layout, push notifications (FCM — needs a
      Firebase project, an explicit external-setup decision, not attempted), the real App Links
      SHA-256 signing fingerprint (needs Mohammed's release keystore/Play App Signing
      enrollment), release signing itself (same blocker — `app-release.aab`/`app-release-
      unsigned.apk` both build, neither is signed), the cross-platform design-token codegen step,
      and a real Invoices/balance-ledger screen (Android's "Payments" tabs are the
      tenant-reported-claim workflow only, not the authoritative `invoice_payments`-backed ledger
      the web tenant portal now has — a real V1-level completeness gap, not a correctness bug: no
      local payment/balance arithmetic exists anywhere in the Android data layer, confirmed by
      direct search).
- [x] **Final Android V1 completion pass — authoritative invoice/payment ledger + My Lease,
      2026-09-01 (continued, 2)**. Closed the previously-disclosed Invoices/balance-ledger gap:
      added `GET /api/v1/invoices` and extended `GET /api/v1/invoices/:id` (both reusing the
      existing, already-trusted `loadInvoicesWithBalances()` — zero new/duplicated accounting
      logic; balance/paid/status computed server-side exactly as the web portal already relies
      on). Built the Android side natively: `Invoice`/`InvoiceDetail`/`InvoicePayment` domain
      models, real (`WebApiInvoicesRepository`) + mock repositories, owner/staff **Invoices** tab
      (list + detail: amount/paid/balance/status/line items/payment history/PDF open) with a
      **Record Payment** screen gated by `canRecordPayment()` (mirrors the server's own
      `accountant`/`manager`/`principal` role floor — UI-layer only, server `requireOrgRole` is
      the real, non-bypassable enforcement, unchanged), and a tenant **Invoices** tab (issued
      invoices only, RLS-scoped, no Record Payment control) kept explicitly distinct from the
      existing tenant **Report Payment** workflow (ledger truth vs. tenant self-report are
      different concepts, not merged). PDF viewing required new `FileProvider` infrastructure
      (authenticated download to app cache, then a `content://` share-URI) since invoice PDFs
      need this app's own bearer token, unlike pre-signed document URLs. Added a **My Lease**
      screen for tenants (property, unit, lease status, start/end date, rent) via a PostgREST
      embed on the existing RLS-protected `lease_tenants`/`leases` tables — no new backend
      endpoint. Investigated (not guessed) whether a tenancy switcher is needed: the backend's
      `caller_tenant_ids()` RLS helper has no per-request "active tenancy" scoping parameter
      anywhere in the API surface, so a real switcher would require new backend API work, out of
      this pass's "use the current backend contract" scope; Android instead shows the single
      most-likely-current tenancy (active lease first, else most recent), exactly mirroring web's
      own `resolveTenantSession()` tie-break rule. Added 22 new tests covering invoice mapping,
      server-authoritative balance (no local recomputation), record-payment role gating, PDF
      demo-mode failure (never a fake success), and My Lease loaded/empty/error states. Verified:
      real `gradlew testDebugUnitTest lintDebug assembleDebug` — **193/193 unit tests passing**
      (was 171), lint **0 errors, 60 warnings** (was 59; one new warning class, no regression).
      Full detail in `WORKLOG.md`'s "2026-09-01 (continued, 2)" entry. iOS untouched this pass
      (explicitly out of scope). Remaining gaps unchanged from the prior pass except the closed
      Invoices item: tablet/foldable layout, push notifications (FCM, needs a Firebase project),
      real App Links SHA-256 fingerprint, release signing, design-token codegen — all owner-action
      or POST-V1 items, not Android code gaps.
- **Exit criteria**: project foundation and every vertical slice through the tenant portal
  (Properties, Units, Tenants, Leases, Maintenance incl. submission/attachments, Documents,
  Notices, payment reporting/review, notifications, auth/session incl. biometric and sign-out, and
  now the authoritative Invoices/payment-ledger screen and My Lease) done and execution-verified
  (build, tests, lint; device/emulator install-and-launch confirmed as of the
  2026-08-01/08-17/08-18 passes — this pass's own emulator smoke test is tracked separately below,
  report only what actually ran). **ANDROID APPLICATION V1** (native code, all backend-supported
  V1 workflows) is functionally complete pending that final smoke-test confirmation. **PLAY STORE
  RELEASE READINESS** is a separate, still-BLOCKED concept: the disclosed owner-action gaps above
  (tablet layout classified POST-V1 enhancement — usable, not broken; push notifications; release
  signing; real App Links fingerprint; design-token codegen) remain open and are not Android code
  work.
- [x] **Proplyst Mobile Design System: Navy Deck redesign, 2026-09-02**. Full visual/navigation
  redesign onto the approved "1b Navy Deck" direction
  (`design/Proplyst mobile app design/design_handoff_proplyst_mobile/`) — replaces the old
  earth-tone palette and the 8-tab (owner)/6-tab (tenant) bottom nav with a 4-tab IA each
  (Owner: Home · Properties · Activity · More / Tenant: Home · Payments · Requests · Profile) and
  a floating white pill bottom nav (the one deliberate departure from the mockups' dark navy nav,
  per this pass's own explicit instruction). Design tokens, typography, and shapes rewritten as a
  proper `ProplystTheme` design system with System/Light/Dark appearance support (More/Profile >
  Appearance). Real bug fix found and corrected in the process: the old
  `Shapes.extraLarge = 999.dp` was the actual cause of the circular-clipped dialogs observed in the
  prior pass's emulator smoke test (mis-attributed at the time to the emulator's software
  renderer) — M3 dialogs use `shapes.extraLarge` as their container shape by default; now a real
  28dp per the design spec. Every previously-top-level screen remains fully reachable, nested one
  level deeper (Owner More, Property Detail's contextual links). Property photography wired to the
  real backend (`property_photos` table, existing `propertyPhotos.ts` cover-photo resolution/
  signing, previously used by web only) via best-effort JSON API enrichment on
  `GET /api/v1/properties` (list + detail) — signed URLs and real unit/occupancy counts, never
  fabricated. Login redesigned with real invalid/network error distinction and a working
  `sendPasswordReset()` flow (Supabase's own `/auth/v1/recover`); "Continue with Google" is a real
  UI boundary gated on `BuildConfig.GOOGLE_WEB_CLIENT_ID`, confirmed empty everywhere in this repo
  — **GOOGLE SIGN-IN: OWNER CONFIGURATION REQUIRED**, not implemented further per this pass's own
  "do not invent OAuth credentials" instruction. A development-only mock role selector
  (`MockAuthRepository`, email-prefix "tenant" → tenant fixture) lets the emulator exercise both
  portals without real credentials; compiled into every build but only ever wired live when
  `USE_MOCK_DATA` is true, itself hardcoded `false` in every release build. Business logic,
  repositories, API contracts, and auth/session/biometric architecture unchanged. Real, disclosed
  gaps found and left open (not fabricated screens): the Tenant Requests (maintenance) list and the
  Owner/tenant Activity feed still use their pre-existing plain-list visual treatment, not a
  restyled photo-thumbnail card (design handoff §"Tenant Requests" was not individually mocked and
  time-boxed this pass); a portfolio-wide occupancy % now shown on Owner Home is computed from the
  properties already fetched for that screen, not a separate backend aggregate. Real device/emulator
  visual pass caught and fixed three genuine defects before this pass's own final report: missing
  status-bar inset handling on every new navy header, a hardcoded occupancy placeholder that had
  real data available, and the floating nav silently going dark in dark mode (a real spec §22
  violation) — see WORKLOG.md's own entry for detail. See this pass's own final report for exact
  test/lint/build verification.

- [x] **Claude Design fidelity audit implementation, 2026-09-02 (continued)**. Implemented
  `design/New-design_handoff_proplyst_mobile/ANDROID_FIDELITY_AUDIT.md` in its suggested order:
  §0 globals (Plus Jakarta Sans bundled locally in `res/font/` — real ExtraBold; full §8 type
  token table; wordmark asset; dp-correct navy-header glow; audit-spec shadows; a custom
  `ProplystTextField` replacing Material's outlined fields), §1 Login (bottom-anchored hero,
  kind-specific banners incl. a real session-expired state and signed-out toast driven by actual
  auth transitions via a presentation-only `AuthEventStore`, 45%-disabled Sign in, badged Google
  boundary, full-navy forgot-sent state), §6 auth/biometric (approved navy lock screen with
  failed state and working "Use password instead" → returning-user sign-in with fingerprint
  shortcut, session intact; one-time biometric offer screen gated on a real system prompt;
  Settings › Security rebuild with inline unavailable/not-enrolled states, enroll intent, custom
  50×30 switch, bottom-sheet sign-out), §2 Owner Home, §3 Properties, §5 Tenant Home, §4 More,
  §11 floating-nav polish (11/600 labels, outlined icons). Security semantics unchanged;
  disclosed deviations: no BiometricPrompt negative-button label (device-credential fallback
  would be lost), greeting/avatar names limited to the stored email (no profile-name field),
  Properties "Attention" chip and per-property income figures omitted (no honest data source),
  tenant "Rent due <date>" uses the invoice's period label (no due-date field), one documents
  entry point instead of two fabricated named shortcuts. §10 screenshot acceptance completed on
  `PropertyVault_Pixel7_API35` against the matching `B-*.dc.html` mocks (login, dark/light owner
  home, properties, security, sign-out sheet, signed-out toast, tenant home); found and fixed one
  real bug in the process — the emulator-only `MockAuthRepository` dev fixture never persisted the
  sign-in email, so the avatar/account-row initial silently fell back to "•" in the smoke-test
  build (`SupabaseAuthRepository` already did this correctly). Final gate: 209/209 tests, 0 lint
  errors / 62 warnings, debug/release APK + AAB all build. Lock/offer screens implemented and
  unit-tested but not emulator-exercised (AVD has no enrolled fingerprint — NOT_ENROLLED state
  captured instead). See WORKLOG.md and the pass's final report for verification detail.

## M22a — Utilities, rates & levies, budgets (V1)

- [x] **Evidence-based gap audit, 2026-09-02**: `UTILITIES_RATES_BUDGET_GAP_AUDIT.md`. Confirmed
  what already existed (expenses, levy statement OCR ingestion, payment review, document
  categories) vs what was genuinely missing (utility meters/readings, rates/levy configuration,
  budgets) before writing any code.
- [x] **Implementation, 2026-09-02 (continued)**: `UTILITIES_RATES_BUDGET_IMPLEMENTATION.md`.
  Migrations 20260101000163-166: `recurring_property_costs` (effective-dated rates & taxes/levy),
  `utility_responsibility_settings`, `utility_meters`, `utility_readings`,
  `property_budgets`/`budget_category_lines`, `owner_financial_summary()`/`budget_vs_actual()`
  (both `SECURITY DEFINER`, both explicitly org-role gated — a real cross-org data-leak
  vulnerability was found and fixed in `budget_vs_actual()` before it ever shipped). Payment
  confirmation traced end-to-end and fixed: `confirm_payment_report()` now allocates through
  `record_invoice_payment()` (the existing authoritative ledger entry point) instead of only
  acknowledging the tenant's claim — idempotent, never double-allocates, refuses rather than
  silently downgrades when no invoice has been issued yet. Full pgTAP suite: 92 files / 1412
  tests, 0 failures (was 91/1403 before this pass). Backend: 8 new API routes, all typecheck +
  lint + `next build` clean. Web: property detail page "Finances" tab (rates & taxes, levy, water/
  electricity responsibility, monthly budget). Android: "Rent status" paid/unpaid tenant list
  (More → Rent status), server-authoritative from `rent_schedules.status`, never
  `payment_reports.status`; `FinancialSummaryRepository`/DI wiring for the dashboard extension
  below. Clean Kotlin compile on first attempt; full Android gate (tests/lint/debug+release
  build/AAB) run — see WORKLOG.md for exact results.
- [x] **Continuation pass, 2026-09-03**: `UTILITIES_RATES_BUDGET_IMPLEMENTATION.md` §"Continuation
  pass". New migration `20260101000167_owner_portfolio_financial_summary.sql` — a LIVE,
  portfolio-wide RPC (chosen over extending the stale-snapshot `owner_property_summaries`, with
  the reasoning recorded in the doc); pgTAP 11/11 including a cross-org `throws_ok`. Full pgTAP
  suite now 93 files / 1423 tests. Android: Owner Home dashboard extension (Operating costs/
  Budget/Net position/Needs Attention merge) now wired to that live RPC for every money figure,
  not just the new ones; four new screens shipped — Add Expense, Utility Capture, Utility History,
  Budget View — all reachable via More → Finances; Payment Review and Rent Status polished
  (method labels, reported-by-tenant-vs-staff, "Confirm payment received" wording, month
  selector). Web: `UnitFinancesPanel.tsx` (unit-level rates/levy/responsibility, the first pass's
  disclosed gap, now closed) and `PropertyUtilityMetersPanel.tsx` (create/list meters, record
  readings, view history — previously the web app had **zero** meter UI, which meant Android's
  own capture screen pointed nowhere; this was the highest-priority gap closed this pass), plus
  annual budget distribution on `PropertyFinancesPanel.tsx`. Alerts: `portfolioIntelligence.ts`
  gained `budget_exceeded`/`budget_approaching`/`unusual_utility_usage` rules in the existing
  Needs Attention engine (never a competing alert system), with the anomaly threshold logic
  extracted to a shared `lib/utilityAnomaly.ts` used by both the API route and the new rules — 4
  new real-Supabase integration tests, 9/9 passing. Android full suite 216/216, 0 lint errors.
- [ ] **Still deferred, disclosed in `UTILITIES_RATES_BUDGET_IMPLEMENTATION.md`**: vendor
  selection in Add Expense (Android always sends `vendorId: null`); annual budget progress on
  Android (web-only this pass); `budget_category_lines` UI (table/RLS exist, nothing reads/writes
  them); meter reset/rollover handling (a lower reading is stored as-is, by design).
- **Exit criteria**: substantially met for V1 scope — schema, payment-ledger fix, portfolio-wide
  live financial summary, the full Android owner workflow set (Home/Rent status/Payment review/
  Add expense/Utility capture/Utility history/Budget), and web setup (property+unit rates/levy/
  responsibility, meter management, monthly+annual budgets) are all built and tested. The four
  remaining items above are genuinely narrow, disclosed V1 scope trims, not blockers.

## M23 — Automated testing

- [ ] Full `TESTING.md` suite: unit, RLS/policy (highest priority), accounting invariants, integration, API contract, native (iOS/Android), E2E against seeded multi-org staging.
- [ ] CI split (`TESTING.md` §8): per-PR vs. nightly, wired into `.github/workflows/ci.yml`.
- **Exit criteria**: not started as a dedicated pass — individual test categories are written alongside their owning milestone per `TESTING.md`'s own structure; this milestone is the full-suite integration and CI-wiring checkpoint, not the first time any test gets written.

## M24 — Deployment

- [ ] Web CI/CD pipeline (staging auto-deploy, gated production promotion), Supabase migration pipeline (`DEPLOYMENT.md` §2-3).
- [ ] iOS pipeline (Xcode Cloud, TestFlight, gated App Store submission), Android pipeline (GitHub Actions + Fastlane, staged Play rollout) (`DEPLOYMENT.md` §4-5).
- [ ] Secrets management, rollback strategy, monitoring/alerting wired end-to-end (`DEPLOYMENT.md` §6-8).
- **Exit criteria**: not started; requires Vercel/Apple/Google account provisioning (external-service blockers).

## M25 — Engineering requirements

- [ ] Full security review pass against `SECURITY.md` — demo-mode bypass fix verified in production config, not just documented; rate limiting backing store wired; dependency-vulnerability scanning confirmed in CI. **Partial update, 2026-08-05**: the rate-limiting backing store itself is real and wired to 4 endpoints (`rate_limit_buckets`/`check_rate_limit()`, migration `20260101000058`) — `SECURITY.md` previously described it as unwired, corrected this date. **Update, 2026-08-06 (Stage 7)**: signin/signup/password-reset rate limiting closed for real (`TECHNICAL_DEBT_REGISTER.md` TD-31, resolved) — real server routes now sit between the client and Supabase Auth. Dependency-vulnerability scanning: closed, `pnpm audit --audit-level critical --prod` now runs in `.github/workflows/ci.yml`'s `verify` job (TD-42 tracks the known non-blocking findings). CSRF defense-in-depth added (`apps/admin/proxy.ts`'s `isTrustedOrigin()`), TOTP MFA shipped, real ClamAV upload scanning shipped (R-03 mitigated). Production-config verification: still blocked on M24 (no production environment exists yet to verify against) — everything above is real, tested code with no production target to verify it _in_ yet.
- [ ] `PRODUCT_SPEC.md` reconciled against actual shipped state (every module's V1/V2 marker and design-doc pointer still accurate).
- [x] **Reports module, 2026-08-01** (moved up to TASKS.md M20 as part of Mohammed's restated priority order): `/reports` — the 4 report cards `PROPVIEW_SCREENSHOT_AUDIT.md` evidences exactly (Income vs Expense Trend, Occupancy by Property, Tenant Payment Status, Maintenance by Status), each with a matching empty state + CTA. Income/expense is a month-bucketed sum of paid `rent_schedules`/recorded `expenses`, not a general-ledger (journal_lines) report — Trial Balance already covers that; this is the simpler evidenced "trend" view. Dependency-free `MiniLineChart`/`MiniBarChart` (already used by the Super Admin overview dashboard), no new charting library. Verified: admin typecheck/lint/test (103/103) and real `next build` clean, demo-mode smoke test confirming all 4 cards render.
- **Exit criteria**: not started; this is the launch-readiness checkpoint, not a discrete feature.

---

## Commercial-launch execution plan, 2026-08-05 — supersedes/extends M20-M25 scope

Mohammed's instruction this date reframed the objective: PropVault ships as a PWA-first commercial SaaS platform, with shared ownership, property-level permissions, and an owner portal now **mandatory for V1** (not deferred to V2, reversing the initial governance-architecture-review recommendation earlier the same session — see `DECISIONS.md` this date for the full reasoning). A full evidence-based execution plan covering all 11 of Mohammed's stated launch phases was researched, written, and approved via Plan Mode; it lives at the plan-file path recorded in `DECISIONS.md` this date and is the authoritative near-term roadmap alongside this file — M20-M25 above are not rewritten wholesale (their existing checklists remain accurate for what they cover), but the plan's Stage 1-8 structure is now the sequencing authority for everything property-permissions/shared-ownership/billing/governance-related, since it reorders work by real dependency (property-level RLS before owner portal, E2E infra started early rather than bolted on at the end) rather than the phase-numbered scope list alone.

**Stage 0 (cheap-now-expensive-later foundations) executed this date**: `property_ownership_history` table + trigger (migration `20260101000062`, 10/10 pgTAP passing); `writeAuditEvent()` wired into the expense-create, expense-record, journal-entry-reversal, and property-owner-ownership-change routes (bill/vendor-bill routes don't exist yet to wire — noted, not silently skipped); the existing 22-file pgTAP suite wired into CI as a new parallel job (`.github/workflows/ci.yml`); two stale `SECURITY.md` sections corrected (demo-mode bypass described as unresolved after it had actually shipped 2026-07-30; a nonexistent `apps/web` path reference). Auth-endpoint rate limiting was attempted and **not completed** — see `TECHNICAL_DEBT_REGISTER.md` TD-31 for why (client-direct GoTrue calls with no server hook point; a live-tested `config.toml` alternative did not produce the expected effect and was reverted rather than left in as non-functional configuration).

**Stage 1 (property-level permissions) foundation executed this date**: `property_access` table + `property_role` enum (Owner/Property Manager/Accountant/Maintenance Manager/Administrator/Read Only) + `has_property_access()`/`grant_property_access()`/`revoke_property_access()` (migration `20260101000063`). Purely additive at that point — no existing table's RLS policy was changed yet.

**Stage 1 cutover, table 1 of 8, executed the same date** (migration `20260101000064`): `properties` is now gated on `has_property_access()`. This was not a mechanical policy swap — three real bugs were found and fixed by testing before it was reported done (an `INSERT...RETURNING`/RLS timing failure that would have broken property creation in production, a late-joining-member visibility regression, and a platform support-mode read-access regression). Property creation now goes through a new `create_property()` RPC (matching `create_organization()`'s established pattern) — `properties` has no client-facing INSERT policy at all anymore. Full account, including the specific reproduction steps, in `WORKLOG.md` this date. 348/348 pgTAP passing (25 files).

**Remaining 6 tables cut over the same date** (migrations `20260101000065`-`20260101000070`): units, leases (+ lease_tenants, inherited with no policy changes of its own), documents, expenses, journal_lines (journal_entries itself deliberately left unchanged — no natural per-property scope), maintenance_tickets. 382/382 pgTAP passing (31 files). Full account of each table's specific design questions (leases has no direct property_id, expenses uses an accountant-flavored write gate, journal_lines required confirming `post_journal_entry()` is not security-definer before trusting the cutover) in `WORKLOG.md` this date.

**`owner_statements` (table 8) deliberately NOT cut over** — it's owner-scoped, not property-scoped, and a single statement can legitimately aggregate multiple properties; a naive per-property gate would be either meaningless or actively misleading. Correctly handling this needs the statement's own shape to become property-aware first, which is Phase 3 (Shared Ownership restructuring) work, not a Stage 1 mechanical cutover. See `WORKLOG.md` this date for the full reasoning.

**Stage 1 status: property-level access control now enforced on 7 of 8 originally-scoped tables.** Remaining before Stage 1 is fully done: the assignment UI (staff currently have no screen to narrow access — only the raw `grant_property_access()`/`revoke_property_access()` RPCs exist), and `owner_statements`' property-aware restructuring (folded into Phase 3). Both tracked as open follow-up, not silently assumed done.

**Stage 2 (Phase 3 — Shared Ownership data layer) executed the same date** (migration `20260101000071`): `generate_owner_statements()` now resolves ownership percentage from `property_ownership_history` as of each statement's `period_end`, not `property_owners`' current-state value — a statement for a past period now correctly reflects who owned what then, not today. Added `organizations.maintenance_reserve_pct` (deducted in `net_payable`, mirroring `management_fee_pct`) and partial-payout support: `amount_paid`/generated `outstanding_balance` on `owner_statements`, a new `owner_statement_payouts` table recording every payout event, and `confirm_owner_statement_payout()` now accepts an optional amount (defaults to full balance — every existing caller unaffected) and may be called more than once per statement. Two real bugs found and fixed by testing (function-overload ambiguity from `create or replace` not retiring an old signature; an enum-cast issue on a bare CASE expression) — full account in `WORKLOG.md` this date. 394/394 pgTAP passing (32 files); `apps/admin` typecheck/vitest clean after updating the `OwnerStatement` type and its three demo-mode mock-data call sites. `TECHNICAL_DEBT_REGISTER.md` TD-33 narrowed (math fixed, visibility gap remains, now filed under Phase 4/5).

**Stage 2 close-out, same date**: audited every report-adjacent route (`(dashboard)/reports`, `(dashboard)/dashboard`, `GET /api/v1/trial-balance`, `GET /api/v1/tax-pack` + `/export`) for property-access compliance. All confirmed to query through the RLS-scoped client (`getServerSupabaseClient()`, never service-role) with no security-definer RPC that would bypass `has_property_access()` — reports/dashboards already correctly respect property-level restriction, verified by direct code inspection, not assumed. One narrow gap found and filed, not silently fixed or ignored: `trial-balance`'s "Balanced" health check assumes a full-ledger view, which is misleading (not dangerous) for a partially-restricted viewer — `TECHNICAL_DEBT_REGISTER.md` TD-34, low priority while nobody uses property-level restriction yet (TD-32).

**Stage 2 is now complete.**

**Stage 3, Phase 5 (Owner Portal) executed the same date** (migrations `20260101000072`; new `(owner)` route group): a genuine co-owner with no org staff role can now sign in and see their own properties (with real ownership %, the one place in the app this renders at all), distribution history (outstanding/historical), documents, and maintenance activity — read-only, mirroring the `(tenant)` portal's session-resolution pattern exactly. Required revising every Stage 1 SELECT policy (a real gap: an owner with no `organization_members` row couldn't see their own data under the original AND-only design) — full account, including a route-collision build failure caught and fixed, in `WORKLOG.md` this date.

**Stage 3, Phase 7 (Cash Management) executed the same date** (migration `20260101000073`): `cash_receipts` table + `record_cash_receipt()`/`confirm_cash_receipt_deposit()`, wired into the _same_ accounting pipeline `confirm_bank_transaction_match()` already uses (not a parallel one) — including fixing that function's rent-schedule cumulative-total calculation to also sum cash payments, so a schedule covered by a mix of cash and bank payments correctly reaches "paid." API routes + owner-portal UI (a "Cash receipts" section on `/owner-portal/distributions`) ship in the same pass, not left as a DB-only capability.

**Stage 3, Phase 6 (Governance) executed the same date** (migration `20260101000074`): DB-trigger-based audit coverage (`owner_statements`/`cash_receipts`/`maintenance_tickets`) plus a new `/owner-portal/activity` page — filterable, real before/after diffs, closing the specific gap the earlier architecture review named ("today's Recent activity widget caps at 8 rows, no filtering"). `expenses`/`property_owners` remain on Stage 0's app-layer audit mechanism rather than double-logged — consolidation onto the trigger layer is named follow-up (`TECHNICAL_DEBT_REGISTER.md` TD-35), not silently left inconsistent.

**Stage 3 is now complete.** 425/425 pgTAP passing (35 files), 174/174 vitest, clean demo-mode `next build` throughout (no browser tool available this session to click through directly — disclosed, not claimed as a full manual QA pass).

**Stage 4 (Phase 2 — Commercial SaaS) executed the same date** (migrations `20260101000075`-`20260101000076`; new `apps/admin/lib/providers/payfast.ts`): real `PayFastBillingGatewayProvider` behind the existing vendor-agnostic interface (preferred automatically once `PAYFAST_MERCHANT_ID`/`_KEY`/`PAYFAST_PASSPHRASE` are configured, mock otherwise), the three real tiers (Starter R299/Professional R699/Business R1499) replacing the single placeholder plan, a 30-day trial clock (`organizations.trial_ends_at`), and the full lifecycle: `expire_trials_and_suspend_overdue()` (trial expiry → `suspended` directly; a real subscriber's failed charge → `suspended` after a 7-day grace period from a new `overdue_since` anchor) + `trials_expiring_soon()` reminders, both callable via `POST /api/v1/system/check-subscriptions` (mirrors `generate-rent-schedules`'s dual-auth pattern — no production cron wired to it yet, same M24-blocked posture as that endpoint). Self-serve checkout/cancel (`/api/v1/organizations/:orgId/billing/{checkout,cancel}`, principal-only) plus a new `/organization/billing` page (plan picker, trial countdown, cancel, payment history) — the org's own principal no longer needs staff to manage billing. A real design gap found and closed while wiring cancellation: nothing had ever populated `organization_subscriptions.provider_subscription_token` (PayFast's own recurring-billing handle), so cancellation would have required a caller to somehow already have PayFast's internal token — fixed by capturing it from the first successful payment's ITN and having `cancelOrgSubscription()` resolve it server-side, never accepted from a caller. Full account, including the disclosed lower-confidence areas (no live PayFast sandbox access this session; the Management API signature reconstructed from one concrete example rather than official docs; PayFast's `PENDING` ITN status deliberately unmapped rather than guessed), in `WORKLOG.md` this date.

**Stage 4 is now complete.** 440/440 pgTAP passing (37 files), 191/191 vitest (46 files), clean demo-mode `next build` with `/organization/billing` and both new API routes present in the route manifest. Not yet done, disclosed rather than dropped: a production scheduler calling `check-subscriptions` (blocked on the Stage 8 hosting decision), and an escalating dunning cadence beyond the current single-notice-per-event emails (a deliberate product-scope call for later, not an oversight).

**Stage 5 (vendor integrations + PWA tech) executed 2026-08-06**, vendor selection resolved first (AWS Textract / Resend / Meta Cloud API direct): real `ResendEmailProvider`, `MetaWhatsAppProvider`, `AWSTextractDocumentIntelligenceProvider` behind the existing vendor-agnostic interfaces, each preferred automatically once real credentials are configured (mock otherwise, same pattern as Stage 4's PayFast). Closed a real design gap found while wiring email: `SendEmailInput` never carried a rendered subject/body at all, so no real provider could ever have sent a correctly-worded email regardless of credentials — fixed. PWA installability + offline support shipped (`app/manifest.ts`, three icon routes, hand-written `public/sw.js`, `/offline` page) — verified live via real HTTP requests, not just build success. M20's three remaining slices (bank-match proposal scoring, AI Assistant chat, Portfolio Map) assessed and deliberately deferred — each is a genuinely separate, sizeable feature, not a residual wiring gap. Full account, including the disclosed unverified-live status of all three vendor integrations and the no-inbound-WhatsApp-webhook-route gap, in `WORKLOG.md` this date (`TECHNICAL_DEBT_REGISTER.md` TD-37/38/39).

**Stage 6 (Playwright E2E infrastructure) executed the same date**: `apps/admin/playwright.config.ts` + `e2e/` — runs against a real Next.js dev server backed by real local Supabase (never demo mode), per `TESTING.md` §7's own stated intent. `public-pages.spec.ts` (6 smoke checks) + `signup-and-onboarding.spec.ts` (real critical-path opening leg: seeded-user login → org creation → property creation, all through real UI against real API routes). **Three real bugs found and fixed by running this suite, not by inspection**: `CreateOrganizationForm.tsx` redirecting every new org's principal to a Super-Admin-only route that throws `FORBIDDEN` (the same bug class `LoginForm.tsx` already fixed once, missed here); `app/page.tsx` having no landing page at all for an authenticated user with zero org/tenant/owner identity (fell through to `/login`); and a `next.config.ts` `allowedDevOrigins` gap that was delaying hydration enough to cause test clicks to fall through to native browser form submission. Full account in `WORKLOG.md` this date. 7/7 Playwright passing, run three consecutive times to confirm stability, not a one-shot pass. Full multi-org cross-tenant-leakage coverage and the rest of the critical path (lease → rent → payment → owner statement) remain to be added (`TECHNICAL_DEBT_REGISTER.md` TD-40/41).

**Stage 5 and Stage 6 are now complete** for the scope executed this pass. 440/440 pgTAP (unaffected, no migrations this stage), 212/212 vitest (47 files), clean demo-mode `next build`, 7/7 Playwright.

**Stage 7 (security hardening, Phase 8) executed the same date**: the four remaining named Phase 8 gaps, three of which needed a real decision from Mohammed first (ClamAV / TOTP-for-V1 / build-the-server-proxy-routes-now, all given, all built for real rather than stubbed):

- **CSRF**: `SECURITY.md`'s `SameSite=Lax` claim verified live against the actual installed `@supabase/ssr` config; `apps/admin/proxy.ts` gained `isTrustedOrigin()` as real Origin/Referer defense-in-depth. A real same-origin-request-incorrectly-rejected bug in the first version was caught by the E2E suite (`request.nextUrl.origin` silently canonicalizes to `localhost` regardless of the real `Host` header) and fixed.
- **Dependency-vulnerability scanning**: `pnpm audit --audit-level critical --prod` now gates `.github/workflows/ci.yml`'s `verify` job. 10 real findings catalogued, all non-blocking today (9 transitive devDependency-tooling chains, 1 dev-only-exploitable critical) — `TECHNICAL_DEBT_REGISTER.md` TD-42.
- **Auth rate limiting, TD-31 closed for real**: `/api/v1/auth/{signin,signup,password-reset}` are now real server routes `LoginForm.tsx`/`RegisterForm.tsx`/`ForgotPasswordForm.tsx` call instead of the Supabase Auth SDK directly, giving `rateLimitOrRespond()` an actual hook point for the first time. Verified live: 11 rapid wrong-password attempts against the real route trip a real 429 on the 11th.
- **TOTP MFA (V1 scope, per Mohammed's decision)**: real Supabase Auth native MFA API, no custom TOTP implementation — enrollment (`/settings`), login-time challenge (inline on `/login`), unenrollment. Verified with a from-scratch RFC 6238 generator cross-checked against RFC 4226's own published test vectors, then a real Playwright test completing a genuine enroll → sign-out → challenge → sign-in-again round trip against real Supabase Auth (no MFA-specific mocking anywhere in that test). Two real bugs caught by running it: `/settings` requiring an org membership the test's first draft didn't create, and the rate-limit test's IP-keyed bucket poisoning every other browser-driven test sharing the same machine IP.
- **Real ClamAV upload scanning (per Mohammed's decision)**: `apps/admin/lib/providers/malwareScan.ts` speaks clamd's INSTREAM protocol directly, wired into both real upload routes, verified against a genuine local ClamAV instance (`docker-compose.yml`) including real EICAR-test-file detection -- not a mocked assertion. Fails closed (refuses the upload) if a _configured_ scanner errors, unlike this codebase's usual fail-open posture for other external services, since malware scanning is access-control-adjacent, not mere throttling. `RISK_REGISTER.md` R-03 moved from Open/High to Mitigated/Medium.

Full account, including the two real bugs the CSRF fix and the MFA E2E test each caught, in `WORKLOG.md` this date. Phase 8 (security hardening) complete for the scope executed this pass. 440/440 pgTAP (unaffected), 248/248 vitest (52 files, +13 new), clean demo-mode `next build` with all 7 new `/api/v1/auth/*` routes present, 9/9 Playwright (+2 new specs), run twice consecutively to confirm stability. Not done, disclosed: no production ClamAV target yet (Stage 8 decision), MFA has no backup codes/org policy/remember-device (`TECHNICAL_DEBT_REGISTER.md` TD-44), content-sniffing/magic-byte upload verification remains open.

**Stage 7, Phase 9 (UI polish) executed 2026-08-06**: Stage 7 also covers Phase 9 per the plan's own scope — not yet done when Phase 8 above was first reported. Closed `TECHNICAL_DEBT_REGISTER.md` TD-27 (24 inline role-gate expressions across 23 files replaced with `canWriteOrgRecords()`, zero functional change). Dispatched a read-only audit of every admin-app page for missing `PageHeader`/`Panel` treatment; found 13 real gaps (6 Super Admin pages never touched by the redesign, 4 Accounting/Operations detail pages missed from an earlier batch, 7 shared form components) out of 31 candidates (18 correctly exempt — print-only, custom hero headers, or thin wrappers whose form component already had it). Fixed all 13. Full account in `WORKLOG.md` this date.

**Stage 7 is now complete** (Phase 8 + Phase 9, the plan's full stated scope). 440/440 pgTAP (unaffected, no migrations), 248/248 vitest (unaffected — this pass touched only presentational markup, no test-covered logic), `tsc --noEmit`/`eslint` clean, clean demo-mode `next build` (exit 0). Not done, disclosed: no dedicated Playwright/visual pass against the 13 newly-fixed pages specifically (no E2E coverage exists for the Super Admin section at all yet — a pre-existing gap, not new to this change).

**Property compliance workflow (core slice) executed 2026-08-12** (migration `20260101000097`): registered conduct rules / body corporate rules / CSOS rules / welcome packs as versioned, acknowledgeable `documents` rows (`property_rules` → `property_rule_versions`, one active per rule, activation atomically supersedes the prior version and assigns fresh compliance requirements to every active tenancy without ever mutating a historical acknowledgement); tenant acknowledgement (`compliance_requirements`/`compliance_acknowledgements`, append-only evidence, viewed≠acknowledged, atomic+idempotent); occupants (`lease_occupants`, non-login household members, distinct from co-tenants which already existed); body corporate/managing agent contacts (`property_management_contacts`); levy statements (`levy_statements`/`levy_statement_line_items`, OCR-assisted via a disclosed heuristic line parser on top of the existing `extractText()` pipeline, human review required, never auto-posted to accounting). Tenant portal `/compliance` + acknowledgement detail page; owner/staff property-detail "Compliance" + "Management" tabs. Full account, including what was deliberately deferred (accounting posting, levy-statement review UI, occupants UI, a generic approval-request engine, most notification events, Playwright E2E), in `WORKLOG.md` this date. 648/648 pgTAP (+34 new), 435/435 vitest (+6 new), clean demo-mode `next build`.

## Sequencing notes

This order is Mohammed's explicit, restated instruction (2026-07-30), followed as given rather than re-derived from dependency analysis alone. Where the literal order creates a dependency wrinkle, it's noted inline rather than silently resolved by reordering: M13 (Maintenance) now precedes M14 (Accounting), which actually _simplifies_ the deposit-release gate versus the previous ordering (inspections exist before the trust-accounting code that checks them, so no stub-then-unstub step is needed). M20-M22 (Responsive Web / Native iOS / Native Android) coming after all backend/API milestones means every screen built in those milestones has a real API to call against from day one — no UI is ever built ahead of the data model it depends on.

This file is the authoritative "what's next" reference — `WORKLOG.md` records what was actually done and when; `KNOWN_BUGS.md` records what's broken; `DECISIONS.md` records why the order changed from the previous 16-milestone structure; `PRODUCT_SPEC.md` §8 points here for the milestone-level detail behind its one-line roadmap summary.
