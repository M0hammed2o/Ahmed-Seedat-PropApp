# PropertyVault PWA — Version 1 Completion Plan

**Audited 2026-08-03** against the current `propertyvault/lovable-ui-integration` branch state (not prior reports — `TECHNICAL_DEBT_REGISTER.md` was cross-checked, not trusted blindly; two of its entries turned out to already be resolved and are corrected below). Evidence for every "missing" claim: a `Grep`/file-read that found nothing, not an assumption.

## How to read this

Severity: **Blocker** (breaks a mandatory end-to-end workflow or a real security boundary) · **Pilot** (needed before real accounts/data) · **Polish** (quality-of-life) · **Deferred** (genuinely V2) · **External** (blocked on a credential/decision only I can't supply).

| # | Module | Current state | Missing | Backend dep | PWA work | Verification | Severity | External cred? |
|---|---|---|---|---|---|---|---|---|
| 1 | **Org status enforcement** | `has_org_role()` (migration `20260101000021`) checks membership + role only. `organizations.status` is never read by any RLS policy. Suspend/Activate/Archive buttons exist and work (`OrganizationActionsPanel`) — they update the row, with zero access effect. Confirmed live by reading the function source. | Status check inside `has_org_role()` (or an equivalent gate every RLS policy already routes through). Product decision already implicit in `SUPER_ADMIN.md`: suspended/archived = no write, and archived = no read either (trial/active/overdue = full access — overdue is a billing state, not an access lock, per `ORGANIZATION_STATUS_PRESENTATION`). | New migration | None (pure RLS) | pgTAP: suspended org member fails write, archived member fails read, active/trial/overdue unaffected | **Blocker** | No |
| 2 | **Tax Pack demo-mode 401** | `TaxPackClient.tsx` fetches `/api/v1/tax-pack` unconditionally, no demo branch — shows "Sign in required" in demo mode (found in prior session, not yet fixed). | Demo-mode short-circuit returning fixture data, matching every other page's `ADMIN_DEMO_MODE` pattern. | None | `TaxPackClient.tsx` | Real-browser check in demo mode | **Blocker** (explicitly named in this instruction) | No |
| 3 | **Invite acceptance UI** | `POST /api/v1/organizations/[orgId]/invites` and `.../invites/accept` exist. No page renders an accept-invite screen — an invited staff member has no way to accept via the web. | None | New page `/invitations/[token]` or query-param based | Confirm/decline UI, real submit to existing endpoint | Manual invite→accept→membership-active flow against local Supabase | **Blocker** (workflow #3 in the mandatory list is unachievable without it) | No |
| 4 | **Password reset / forgot password** | No route, no form, anywhere. Supabase Auth supports it natively (`resetPasswordForEmail` / recovery link). | None (Supabase Auth feature) | `/login` "Forgot password?" link → `/forgot-password` → `/reset-password` | Real Supabase Auth call, no mock | Manual flow against local Supabase Auth | **Blocker** (named explicitly: "forgot password; password reset") | No |
| 5 | **Not-found / permission-denied pages** | `app/error.tsx`/`app/global-error.tsx` exist (from the earlier CSP fix). No `not-found.tsx` anywhere (falls to Next's unbranded default). No dedicated permission-denied state — pages that gate access currently just render a plain sentence inline (e.g. Tax Pack's "Only accountant... roles"). | Root `not-found.tsx`; a small shared `PermissionDenied` component for the inline-sentence call sites | None | 2 small files | Visual check, 404 route, one gated page | **Pilot** | No |
| 6 | **Search/filter coverage** | ~~Only Properties and Units have a search box~~ **Closed 2026-08-03**: all 12 remaining lists now have client-side search (Owners, Tenants, Leases, Applications, Documents, Maintenance, Inspections, Rent Due, Expenses, Bank Transactions, Notifications, Super Admin Organizations/Subscriptions). Built as a shared `useListSearch` hook + `SearchBar` component (extracted rather than 12 copies of `UnitsFilterClient`'s markup, since a Server Component page can't pass a render-prop function to a Client Component — functions aren't serializable across that boundary) with one thin `*FilterClient` wrapper per list. | — | None for client-side; `pg_trgm` indexes still deferred (TD-19, correctly — no query would use them yet) | 12 `*FilterClient` components + `lib/useListSearch.ts` + `components/ui/SearchBar.tsx` | Typecheck/lint/full vitest (155/155) clean; real-browser check in demo mode confirmed narrow-to-empty-state and clear-restores-list on Owners, Tenants, Applications, Notifications, Maintenance, Rent Due | **Pilot** | No |
| 7 | **User settings** | **Closed 2026-08-03**: `/settings` (org staff) — display name (`profiles.display_name`, `PATCH /api/v1/profile`), email/password via real `supabase.auth.updateUser()` calls, link to existing `/notifications/preferences`. Shared `AccountSettingsForm` also used by #11. | — | — | Typecheck/lint/vitest clean, real-browser render check (demo mode) | **Pilot** | No |
| 8 | **Organization settings** | **Closed 2026-08-03**: schema check found `legal_name`/`trading_name`/`cipc_reg_no`/`vat_no`/`sars_tax_no`/`popia_information_officer`/`invoice_prefix`/`deposit_interest_pct`/`ffc_number`/`ffc_issued`/`ffc_expires` all already exist (no banking/contact fields on `organizations` — none found). `/organization/settings` (manager+, hidden entirely below that per PERMISSIONS.md), `GET`/`PATCH /api/v1/organizations/:orgId` (manager+ enforced via `has_org_role`, RLS is ground truth via `organizations_update_manager_plus`). | — | — | Typecheck/lint/vitest clean, real-browser render check (demo mode) | **Pilot** | No |
| 9 | **Lease templates** | **Closed 2026-08-03**: new `lease_templates` table (migration 20260101000056) reusing the *existing* `documents` storage bucket/RLS convention (no new bucket) at `{org_id}/lease-templates/...`. Upload/rename/archive/set-default at `/organization/lease-templates` (manager+); `set_default_lease_template()` RPC makes default-swap atomic (partial unique index `lease_templates_one_default_per_org` backs the invariant at the DB level regardless). Replace = upload with `supersedesId`, which archives (never mutates/deletes) the old row — a lease already created against it is unaffected. Picker added to `LeaseForm.tsx` (create mode): reference-only (opens a signed URL), doesn't pre-fill fields. | — | — | New `lease_templates_isolation.test.sql` (9 pgTAP assertions: cross-org isolation, manager-only write, atomic default-swap), typecheck/lint/vitest clean, real-browser check | **Pilot** | No |
| 10 | **Tenant "My Documents"** | **Closed 2026-08-03**: `documents_select_tenant_self` (table RLS) already existed, but its storage-bucket counterpart didn't — `documents_bucket_select_org_member` only grants org *members*, always false for a tenant, so a tenant-visible row's file itself would 403 on `createSignedUrl()`. Added `documents_bucket_select_tenant_self` (migration 20260101000056) to close that gap — found and fixed while building this, not assumed away. `/my-documents` lists tenant-visible docs with signed download links; `DocumentUploadForm.tsx` gained an optional "tag to a lease" field so staff can actually produce one. | — | — | SQL-level verification via `docker exec` (tenant sees only the lease-tagged object, not the owner-only one; outsider sees zero), typecheck/lint/vitest clean, real-browser check | **Pilot** | No |
| 11 | **Tenant Profile** | **Closed 2026-08-03**: `tenants.sql`'s own comment flagged this as deliberately deferred "for whichever native-app milestone builds the tenant portal" — that milestone is now real, so `tenants_update_self` RLS (migration 20260101000056) closes it; `tenants_isolation.test.sql`'s existing "self-edit did not apply" assertion flipped to match (same pattern as TD-17's org-status flip). `/profile` = shared `AccountSettingsForm` (name/email/password) + new `TenantContactForm` (email/phone only, via `PATCH /api/v1/tenant-portal/profile` — full_name/status stay staff-managed by API-layer whitelist, matching PERMISSIONS.md §5 layer 2). | — | — | pgTAP (`tenants_isolation.test.sql`, updated assertion passes), typecheck/lint/vitest clean, real-browser check | **Pilot** | No |
| 12 | **Support-session enforcement** (TD-25) | **Closed 2026-08-03**: `has_org_role()` extended with a viewer-only OR branch granting read access when the caller holds an active `support_access_sessions` row for that org (migration 20260101000057) — ignores `organizations.status` entirely (archived orgs stay compliance-readable, unlike the real-membership branch). Audited every `has_org_role(..., 'viewer')` call site in the schema before broadening it: found and fixed the one write policy gated at that floor (`portfolio_insights_dismiss_org`, rewritten to check real membership directly) and a second real gap (`organizations_select_member` predated `has_org_role()` and had neither the TD-17 archived exclusion nor this new grant — switched onto `has_org_role(id, 'viewer')`). `resolvePortalSession()` synthesizes a viewer membership for support-session orgs so the existing `(dashboard)` pages work unmodified; persistent `SupportModeBanner` (non-dismissible, "End session" button) renders via a new `AppShell` `banner` slot. "Start support session" wired on the customer detail page (reason required, ≥10 chars). Explicit per-write escalation (SUPER_ADMIN.md §6 item 2) is NOT built — a materially separate mechanism, disclosed as out of scope, not silently claimed. | — | — | New `support_session_access.test.sql` (11 adversarial pgTAP assertions: no session = zero access, active session = viewer read only, cross-org isolation, write denial incl. the portfolio_insights exception, archived-org compliance access, session-end revokes immediately), typecheck/lint/vitest/pgTAP (281/281) clean, real-browser check | **Pilot** | No |
| 13 | **Usage metering UI** (Super Admin) | **Closed 2026-08-03**: `usage_snapshots` stays empty for every org until the rollup job (TD-20) exists, so `getPlatformOrganizationDetail()`/`GET /api/v1/admin/organizations/:orgId/usage` now also sum `usage_events` live for the current calendar month (same approach `lib/ai.ts`'s `checkAiUsageCap()` already used) — new `UsagePanel` on the customer detail page. | — | — | Typecheck/lint/vitest clean | **Pilot** | No |
| 14 | **Audit log viewer** (Super Admin) | **Closed 2026-08-03**: `getPlatformOrganizationDetail()` now also fetches the org's most recent 20 `audit_events` rows; new `AuditLogPanel` renders them on the customer detail page (`audit_events_select_org_member`'s RLS also auto-extends via #12's `has_org_role()` change, so a support-mode admin sees this too). No standalone paginated/filterable page — real follow-on work if usage grows past a detail-page panel's scope, disclosed not silently scoped down. | — | — | Typecheck/lint/vitest clean | **Pilot** | No |
| 15 | **2FA (owner)** | Not built, not referenced anywhere. | Supabase Auth MFA (TOTP) | Settings toggle + challenge step in login | Manual enroll+challenge flow | **Deferred** — instruction says "if it remains part of the agreed V1 scope"; no prior decision on record confirming it is. Flagging as a scope question, not silently building or silently dropping it. | Business decision |
| 16 | **Google Sign-In** | **Code built 2026-08-03** (PRODUCT DECISION 1, `DECISIONS.md`/`AUTHENTICATION.md`): `OAuthButtons.tsx` + `/auth/callback` (PKCE `?code=` exchange) implement Google and Apple OAuth end-to-end, `supabase/config.toml`'s `[auth.external.google]`/`[auth.external.apple]` wired with `env()` references. **Still external-blocked**: no real Google Cloud OAuth client / Apple Services ID exists in this environment, so the live provider round trip has never been click-tested — tracked as `TECHNICAL_DEBT_REGISTER.md` TD-29, not silently claimed as done. | Supabase Auth Google OAuth provider | Sign-in button | Manual OAuth flow | **External** — requires a real Google Cloud OAuth client ID/secret, cannot be built against nothing; code path itself is no longer missing | Yes — OAuth credentials |
| 17 | **Staff property restrictions** | No property-assignment model exists — every org member with `agent`+ role sees the whole org's data; there is no "restrict to these properties" concept in the schema at all. | New join table (`staff_property_assignments`) + RLS predicate on every property-scoped table | Assignment UI (org settings or per-staff), filtered list views | pgTAP: restricted staff sees only assigned properties | **Deferred** — this is a genuinely new authorization model, not a missing screen; the instruction says "where configured," implying it's optional/conditional, not a hard V1 requirement | No |
| 18 | **Meter readings** | Not built, not referenced anywhere. Instruction itself says "optional... where enabled by the owner." | New table + toggle in org settings | Small form on tenant portal + owner-side view | Manual flow | **Deferred** (explicitly optional in the instruction) | No |
| 19 | **Rate limiting** (TD-10) | **Closed 2026-08-03**: `check_rate_limit()` Postgres function (migration 20260101000058) — single-row-per-key fixed-window counter, one UPSERT so concurrent callers can't race past the limit; `lib/rateLimit.ts` wraps it (fails open on an infra error — this is throttling, not access control). Wired to the two real unauthenticated/auth-adjacent server routes that actually exist in this codebase: `POST /api/v1/billing/webhook` (by source IP) and `POST /api/v1/organizations/invites/accept` (by user id — token-guessing defense in depth). **Not** wired to `/login`: no server-side login route exists anywhere in this app (`LoginForm.tsx` calls `supabase.auth.signInWithPassword()` directly client-side) — Supabase Auth's own native rate limiting applies there, outside this codebase's control. Disclosed, not silently claimed as covered. | — | — | New `rate_limiting.test.sql` (7 pgTAP assertions: under-limit allowed, over-limit rejected and stays rejected, independent keys don't share a bucket, window-elapsed reset), typecheck/lint/vitest/pgTAP clean | **Pilot** | No |
| 20 | **Upload malware/content scanning** (TD-09) | MIME allow-listing only. | Needs a real scanning provider (ClamAV self-hosted, or a vendor API) to do anything beyond MIME checking | — | — | **External** — no scanning engine available in this environment; MIME allow-listing is the honest current ceiling | Yes — scanning provider/infra |
| 21 | **Bank-transaction match-proposal step** (TD-22) | Confirm-only flow works (caller already knows which `rent_schedule` to match). No `calculateMatchScore`-based suggestion UI. | Scoring function (amount+date proximity against candidate rent_schedules) | Suggestion list in the existing match UI | Real match against 2+ candidates | **Polish** | No |
| 22 | **Unattended cron** (TD-20/TD-23) — rent-schedule generation, WhatsApp reminder types, usage/metrics rollups | Every job is a tested, callable endpoint. Nothing calls them on a schedule. | Needs a real scheduler (hosting-platform cron, e.g. Vercel Cron/Supabase Edge Functions scheduled trigger) | — | — | **External** — no hosting platform chosen yet (this is a deployment-target decision, `TASKS.md` M24) | Yes — hosting/scheduler choice |
| 23 | **RLS isolation test execution** (TD-05) | **Closed**: Docker/local Supabase confirmed available this session; `supabase db reset` + full pgTAP has been run after every migration throughout this phase (281/281 passing as of the #12/#19 batch, across 21 test files). | — | — | — | **Pilot** | No |
| 24 | **Corrections to `TECHNICAL_DEBT_REGISTER.md`** | TD-27(2) claims Owner Statements/Tax Pack/Bank Transactions have "no UI at all yet" — **false**, all three were built in the earlier functional-completion pass this session and redesigned in the Lovable-integration pass. Register entry is stale. | — | — | Update `TECHNICAL_DEBT_REGISTER.md` to close TD-27(2) | — | **Polish** (docs only) | No |

## Status (2026-08-03, mid-phase)

Closed and verified: **#1** (org status RLS), **#2** (Tax Pack demo fix), **#3** (invite
acceptance), **#4** (password reset — surfaced and fixed two additional real bugs along the way:
a CSP `connect-src` gap blocking all client-side Supabase calls against local Supabase, and a
missing PKCE `exchangeCodeForSession()` call), **#5** (not-found + PermissionDenied), **#6**
(search/filter coverage — all 12 remaining lists), **#7** (user settings), **#8** (organization
settings), **#9** (lease templates), **#10** (tenant My Documents — also closed a real
tenant-storage-access gap found while building it), **#11** (tenant profile), **#12**
(support-session enforcement — also closed two real pre-existing gaps found while auditing
`has_org_role()`'s call sites), **#13** (usage metering UI), **#14** (audit log viewer), **#19**
(rate limiting — disclosed limitation: `/login` itself has no server route to gate), **#23** (RLS
isolation tests — run continuously throughout this phase via real local Supabase, 281/281 passing
as of this batch), **#24** (TD-27 correction). Each closed with a focused commit, full
verification (typecheck/lint/full vitest/pgTAP where relevant/real-browser), and updated tracking
docs.

Still open: none of the **Pilot** items (5–14, 19, 23). **#15–18, 20, 22** remain Deferred/External
as originally classified — genuinely out of this phase's scope (business decisions, external
credentials, or a hosting-platform choice not yet made). The 22 mandatory end-to-end workflows:
#1–2 (sign in, create org), #3 (invite staff — now provable), and #20–21 (suspend/restore,
cross-org isolation — now provable via the pgTAP suite) are covered by this phase's work; the
remainder are covered by earlier sessions' functional-completion work. A full walk-through of all
22 in one pass, and the closing `PWA_V1_READINESS_REPORT.md`, are the last two items of this
phase.

**Post-phase addendum, 2026-08-03**: `PWA_V1_READINESS_REPORT.md` §2, written immediately after this
plan closed, surfaced a gap this plan's own 24-item list never asked about — no web self-service
signup path existed anywhere in `apps/admin` (this list was scoped to already-existing-account
feature pages, not "can a brand-new customer get in the door at all"). Mohammed's instruction closed
it the same day as two explicit product decisions (`DECISIONS.md` 2026-08-03: web account creation
in scope for V1, tenants link via secure invitations rather than re-entering data) — full build
record in `WORKLOG.md` 2026-08-03 ("Web account creation + tenant activation-code system") and
design in `AUTHENTICATION.md`. This closes the gap `PWA_V1_READINESS_REPORT.md`'s workflow #1
("Sign up (web)") flagged as `Blocked`.

## Execution order for this phase

1. **Blockers first** (1–4): org-status RLS enforcement, Tax Pack demo fix, invite-acceptance page, password reset.
2. **Pilot items** (5–14, 19, 23) in the order listed — each is independently shippable, batched sensibly (e.g. all search/filter additions as one verification batch).
3. **Deferred/External** (15–18, 20, 22): documented here, not built blind. 2FA and staff-property-restriction are flagged as scope questions rather than assumed in either direction.
4. Update `TECHNICAL_DEBT_REGISTER.md` corrections (24) alongside whichever batch touches those modules.

Not re-auditing Owner Dashboard, Properties, Units, Owners, Tenants, Maintenance, Accounting's existing pages, Reports, Notifications, Announcements, or Tenant Portal's existing pages — per instruction, these are approved and only extended (search, settings links) not rebuilt.
