# Technical Debt Register

Distinct from `RISK_REGISTER.md`: a **risk** is something that might go wrong. **Technical debt** here is a deliberate, known simplification or incomplete cutover — a specific, scoped gap between the current state and the target state, taken on knowingly rather than discovered as a surprise. Every item has an owning milestone where it gets paid down; nothing here is open-ended.

| ID    | Debt item                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | Why it exists                                                                                                                                                                                                                                                                                                                                              | Paydown milestone                                                                                                                                                                                                                                                                      | Interest (cost of leaving it)                                                                                                                                                                                                                                                                                                      |
| ----- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| TD-01 | `properties.owner_user_id` still exists (now nullable, not dropped as originally reported) alongside `org_id` (required, RLS-enforced). **Revised 2026-07-30**: a real `supabase start` run proved the column cannot even be dropped yet — `document_categories.sql`/`documents.sql`'s RLS policies (original PropVault-era migrations, still owner_user_id-scoped) reference `properties.owner_user_id` via cross-table subqueries, which Postgres blocks a DROP COLUMN on. A real dependency, not a style choice — TD-02 must close first. | Originally the deliberate expand/contract pattern (`DATABASE.md` §14); now also a genuine cross-table RLS dependency discovered by execution, not just self-imposed sequencing. | `TASKS.md` M5/M11 — the column drop is now blocked on TD-02, not independently schedulable. | Low today (column is inert, unused by application code); rises once TD-02 is tackled, since that is when the drop actually becomes possible. |
| TD-02 | `documents`/`bills`/`payments`/`payment_matches`/`extraction_jobs`/`subscriptions`/`audit_events` all still carry `owner_user_id`, not yet cut over to `org_id`. **Now confirmed to also be blocking TD-01's column drop**, not just parallel debt. | Same reasoning as TD-01 — sequenced with or immediately after the Properties cutover rather than done piecemeal. | `TASKS.md` M5 (bundled with the Properties cutover per its own note); `documents` specifically also gates `TASKS.md` M11. | Same as TD-01 — low now, rises before real data exists; also now blocks TD-01's cleanup specifically. |
| TD-03 | `admin_users`/`is_admin()` not yet renamed to `platform_admin_users`/`is_platform_admin()`, despite every architecture doc referring to the target names.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | Deferred deliberately (ADR-007) to avoid touching live, working `apps/admin` auth code ahead of the Super Admin rebuild that opens those files anyway.                                                                                                                                                                                                     | `TASKS.md` M19 (Super Admin)                                                                                                                                                                                                                                                           | Low — purely cosmetic naming mismatch between docs and live schema, already caveated consistently across every referencing document (2026-07-30 review).                                                                                                                                                                           |
| TD-04 | ~~Demo-mode auth bypass~~ — **paid down 2026-07-30**: dual-gated fix implemented and build-verified across `apps/admin` and `apps/mobile`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | Was carried forward unresolved from the PropVault era.                                                                                                                                                                                                                                                                                                     | `TASKS.md` M2 (Authentication) — closed                                                                                                                                                                                                                                                | Now Low (see R-01 in `RISK_REGISTER.md`, downgraded Critical→Medium). Remaining item is the CI production-deploy assertion, which needs a real deploy pipeline to assert against (M24).                                                                                                                                            |
| TD-05 | RLS isolation test suite is written as a pattern (one file, `rls_isolation.test.sql`) but not extended to the new multi-tenant schema, and not executed at all (no Docker in this sandbox).                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | Sandbox limitation, not a design choice — carried forward from the same limitation noted in the PropVault-era `KNOWN_BUGS.md`.                                                                                                                                                                                                                             | `TASKS.md` M3/M23                                                                                                                                                                                                                                                                      | **High** — this is the test category `TESTING.md` §2 itself calls "non-negotiable" for a multi-tenant system; also tracked as R-02 (Critical).                                                                                                                                                                                     |
| TD-06 | `packages/types/src/property.ts` (`Property` interface) does not yet expose `orgId`, even though the database column exists.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | Deliberately deferred alongside TD-01 — adding it to the shared type now would force every consuming call site to handle it before the schema cutover is ready, which is exactly the "big-bang" risk the expand/contract pattern avoids.                                                                                                                   | `TASKS.md` M5                                                                                                                                                                                                                                                                          | Low — purely a TS/DB drift, caught immediately by `pnpm typecheck` the moment it matters (nothing currently reads a non-existent field).                                                                                                                                                                                           |
| TD-07 | `usage_events`/`usage_snapshots` tables are designed (`DATABASE.md` §7) but no subsystem actually writes to them yet — every "per-org usage" metric in `SUPER_ADMIN.md`/`AI_ARCHITECTURE.md` is schema-ready but data-empty until each subsystem (email, WhatsApp, OCR, AI, storage) is built and instrumented.                                                                                                                                                                                                                                                                                                                                                      | Schema-first design, matching the pattern used throughout this codebase (design the table, wire the writer when the owning feature is built).                                                                                                                                                                                                              | Distributed — each subsystem's own milestone (M11 storage, M16 email, M17 WhatsApp, M18 AI) instruments its own `usage_events` writes.                                                                                                                                                 | Low — no metric silently lies; it's simply empty/zero until instrumented, not wrong.                                                                                                                                                                                                                                               |
| TD-08 | `accounting_periods` (period locking) is designed (`DATABASE.md` §9, `ACCOUNTING.md` §9) but not built — nothing currently stops a backdated post in the eventual implementation until this lands.                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | Identified and designed in this review pass; implementation is naturally part of the accounting engine build, not before it.                                                                                                                                                                                                                               | `TASKS.md` M14 (Accounting)                                                                                                                                                                                                                                                            | Medium once the accounting engine exists and has real usage — zero today since no accounting engine is built yet either.                                                                                                                                                                                                           |
| TD-09 | Malware/content-scanning on file uploads is specified (`SECURITY.md`, this review) but not implemented — the only current upload safeguard is MIME-type allow-listing.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | Identified in this review; implementation is Documents-module work.                                                                                                                                                                                                                                                                                        | `TASKS.md` M11 (Documents)                                                                                                                                                                                                                                                             | Medium-High once real uploads from real (potentially untrusted) tenant/vendor users occur — zero today, no upload pipeline exists yet. Also tracked as R-03.                                                                                                                                                                       |
| TD-10 | Rate limiting has a scaffolded constant set (`packages/config` `RATE_LIMITS`) with no wired backing store.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | Carried forward unresolved from the original `SECURITY.md`.                                                                                                                                                                                                                                                                                                | `TASKS.md` M2 (Authentication) or M24 (Deployment) — not yet definitively assigned, flagged here so it isn't lost between milestones.                                                                                                                                                  | Medium — auth/webhook endpoints are unprotected against brute-force at the infrastructure level until this lands.                                                                                                                                                                                                                  |
| TD-11 | `apps/mobile` (Expo) remains in the repository, superseded in architecture (`MOBILE_ARCHITECTURE_DECISION.md`) but not deleted, and still has one known unresolved defect (jest-expo/Windows test-runner bug).                                                                                                                                                                                                                                                                                                                                                                                                                                                       | Retained deliberately as living reference documentation for native screen/field requirements until `apps/ios`/`apps/android` reach parity.                                                                                                                                                                                                                 | Implicit — no dated retirement milestone exists yet; should be added once M21/M22 are substantially complete.                                                                                                                                                                          | Low — never deployed to app stores, doesn't affect production; mild ongoing cost of a stale codebase lingering in the monorepo (CI still runs its (passing) lint/typecheck).                                                                                                                                                       |
| TD-12 | `AI_ARCHITECTURE.md`'s evidenced "AI-assisted unit setup" feature (natural-language → generated units, `PROPVIEW_SCREENSHOT_AUDIT.md` IMG_7998) has no architecture document covering it — flagged in `PRODUCT_SPEC.md` §5 as needing its own design pass.                                                                                                                                                                                                                                                                                                                                                                                                           | Discovered during `PRODUCT_SPEC.md` compilation; genuinely not designed anywhere yet, unlike the Assistant/Portfolio Intelligence which are fully specified.                                                                                                                                                                                               | Not yet assigned — needs a design pass before `TASKS.md` M6 (Units) can build it, or M6 ships without this specific evidenced feature and it's added later.                                                                                                                            | Low today (nothing depends on it yet); should be resolved before M6 claims completion if this feature is meant to be in V1.                                                                                                                                                                                                        |
| TD-14 | `audit_events`' live schema (migration `20260101000013`: `owner_user_id not null`, `target_type`/`target_id`, `actor_type enum(customer\|admin\|system)`) does not match `DATABASE.md` §10's target shape (`org_id` nullable, `entity_type`/`entity_id`, `before`/`after`, extended `actor_type` incl. `ai_assisted`, `ai_conversation_id`/`ai_message_id`) — discovered 2026-07-30 while wiring the org-signup endpoint, which therefore does **not** write an audit row yet, despite `API_SPEC.md` §0's "every mutating endpoint audits" rule.                                                                                                                     | Same `owner_user_id`-era table as `properties`, never updated when the org-scoped design was written; not caught by the earlier architecture review because the review checked the _design_ doc against _itself_, not against the live migration.                                                                                                          | `TASKS.md` M5 (bundled with the other `owner_user_id` cutovers, per that milestone's own note) — or split out sooner if more endpoints need audit-writing before M5 lands.                                                                                                             | Medium and rising — every new mutating endpoint built before this closes either skips audit logging (as `POST /api/v1/organizations` does now) or writes against a schema that's about to change underneath it. Flag every such endpoint here as it's built, don't let this silently accumulate.                                   |
| TD-15 | `apps/mobile/app/(onboarding)/add-first-property.tsx` assumes a user can create a property immediately after signup — true in the single-owner model, no longer true now that a property needs an organization. Mobile has no create-organization screen (only web does).                                                                                                                                                                                                                                                                                                                                                                                            | Discovered 2026-07-30 while cutting `properties` over to org-scoping (M5) — the schema/data-layer fix is mechanical, but "what should mobile onboarding actually do now" is a real product/UX decision, not a mechanical follow-on.                                                                                                                        | Not yet assigned — candidate: M21 (Native iOS) once the native onboarding flow is designed for real, or a stopgap web-redirect if mobile ships before then.                                                                                                                            | Low today (mobile is a legacy Expo reference app, not deployed); rises to Medium once native onboarding is actually being built and this decision can no longer be deferred.                                                                                                                                                       |
| TD-16 | **Confirmed real conflict, not hypothetical — corrected 2026-07-31**: `apps/admin/app/(dashboard)/customers/page.tsx`'s pre-existing uncommitted `getCustomers()` (dated "launch-readiness audit, 2026-07-22" per its own comment — an N+1-query fix) does `supabase.from('properties').select('owner_user_id').in('owner_user_id', ids)`. **This row previously claimed the M5 migration "drops `properties.owner_user_id` entirely" — that contradicted TD-01 directly above it (which correctly says the column is only relaxed to nullable, never dropped) and was never reconciled until now.** The real, still-valid consequence: the query doesn't throw, it silently omits every property created *after* the org-scoped cutover (including all properties created through the real M6 `POST /api/v1/properties` route, which never sets `owner_user_id`) — arguably worse than a crash, since it fails silently. `processing/page.tsx`, `subscriptions/page.tsx`, `adminMockData.ts` are the same file family, not individually re-checked for the identical issue. | Discovered 2026-07-30 while tracing M5's blast radius. Left unfixed deliberately — this specific code is itself pre-existing uncommitted work (present before Phase 7 implementation began), and editing it risks conflicting with whatever else that in-progress change was doing, per the git-safety instruction to preserve unrelated uncommitted work. | Not yet assigned — **needs Mohammed's decision**: (a) commit the pre-existing `customers/page.tsx` change first so it's a clean, understood baseline to fix on top of, or (b) explicitly authorize editing it now despite being uncommitted, or (c) something else. Not safe to guess. | **Medium-High, not just Medium** — this is not a latent/theoretical staleness, it is a confirmed broken query the moment `ADMIN_DEMO_MODE` is off against a migrated database. Currently masked because demo mode is the only thing exercised (CI, and this whole sandbox, has never run against a live post-M5 Supabase project). |
| TD-13 | No accounting-data export path (CSV/standard format) to third-party accounting software exists in the design.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | Accepted consequence of ADR-004 (in-house engine, no integration) — an export path is a different, smaller feature than an integration, not yet separately designed.                                                                                                                                                                                       | Not yet assigned (V2 candidate per `RISK_REGISTER.md` R-08).                                                                                                                                                                                                                           | Low — no client has asked for this yet; revisit if it becomes a real blocker to closing a deal.                                                                                                                                                                                                                                    |

| TD-17 | **New, 2026-07-31**: `organizations.status` (`trial\|active\|overdue\|suspended\|cancelled\|archived`) is not checked by any RLS policy anywhere. `has_org_role()` only checks `organization_members.status = 'active'`; nothing checks the organization's own status. Confirmed via a real pgTAP assertion (`multi_tenant_isolation.test.sql`): an archived org's own principal still passes `has_org_role(org_id, 'viewer')`. Suspending or archiving an org today has zero effect on that org's own staff's data access. | Never actually decided, not a deliberate simplification like the rest of this register — `SUPER_ADMIN.md` describes what "archive"/"suspend" mean for billing/dashboard visibility but never states what they mean for the org's own member access. Found while verifying the M1-M5 foundation end-to-end per Mohammed's instruction, not while building a specific feature. | Needs a product decision before a milestone assignment is meaningful: should a `suspended` org's members lose write access? Read access? Should `cancelled`/`archived` be read-only, fully locked, or unaffected (billing-only)? Once decided, likely a small `has_org_role()` change — probably `TASKS.md` M4 or M19 (Super Admin), whichever lands first. | Low today (no billing enforcement exists yet to even trigger a suspension) but rises sharply once subscription enforcement (`TASKS.md` M9/M19) goes live — a suspended-for-non-payment org would keep working exactly as before, silently defeating the point of suspending it. Also tracked as R-22 in `RISK_REGISTER.md`. |

| TD-18 | **New, 2026-07-31**: `encrypted_secrets` (`DATABASE.md` §11) exists as a schema-only table (migration `20260101000027`) — `id`, `ciphertext bytea`, `created_at`, RLS locked to service-role only. The application-layer encrypt-before-insert pipeline (key management, `SECURITY.md`) is not built. Nothing currently writes to this table; `tenants.id_number_ref`/`owners.banking_ref` are both nullable and unpopulated. | Built ahead of any caller specifically so `tenants` (M8) and `owners` (already-existing `banking_ref`, retrofitted with a real FK in the same migration) have a schema matching `DATABASE.md` exactly — implementing the full encryption pipeline before any real feature needs to write a secret would be speculative, unverifiable work (no way to test key management without a concrete usage flow driving it). | The first feature that actually needs to capture a tenant ID number or owner banking details (candidate: M8's Web UI, or a dedicated Banking/Compliance milestone) — building the pipeline alongside its first real caller, not before. | Zero today (unused table, no PII at risk). Rises to Medium the moment any UI/API tries to write a plaintext value into `id_number_ref`/`banking_ref` directly instead of routing through the (not-yet-built) encryption pipeline — flag this explicitly if it's ever attempted as a shortcut. |
| TD-19 | Trigram (`pg_trgm`) search indexes specified in `DATABASE.md` §13 (Properties, Tenants, Owners, Vendors, Documents, Audit Log) are not implemented anywhere, including for tables that already exist (`properties`, `owners`, and now `tenants`). | Identified in the 2026-07-30 Production Readiness Review as a fix, but never actually built — no evidenced search-box UI has been implemented yet for any of these tables to require it, and adding an index with no query using it would be unverifiable/speculative. | Whichever milestone first builds a real search box against one of these tables (Properties/Owners/Tenants list UI, none built yet) — add the specific `gin_trgm_ops` index that UI's actual query needs, not all of them preemptively. | Low today (no search queries exist to be slow); becomes a real performance risk only once list views with search exist and have production-scale row counts. |

| TD-20 | **New, 2026-07-31**: `approve_application()` (`TASKS.md` M9/M10) generates only the first `rent_schedules` row for a new lease (the period it starts in). Every subsequent period's due-date row is not generated by anything — there is no cron/scheduled-function infrastructure anywhere in this codebase yet. | Generating an unbounded series upfront (for a lease that may have no `end_date`) inside a single atomic transaction was rejected as the wrong design — that's a recurring-job problem, not a one-time-creation problem, and inventing ad-hoc scheduling logic inside `approve_application()` would conflate two different concerns. | A real scheduled-function/cron milestone — candidate `TASKS.md` M24 (Deployment) once the hosting platform's scheduling primitive is chosen, or its own small milestone if rent-due dashboards are needed before M24. Needs: "for every active lease, if the next period's `rent_schedules` row doesn't exist yet, create it" run on a monthly cadence. | High once any org has a real active lease running past its first month — the Rent Due dashboard (`DATABASE.md` §4) would silently show nothing for month 2 onward. Zero today (no production leases exist). |

## How this register is used

Every item above is either (a) already assigned to a specific `TASKS.md` milestone, in which case it's paid down as a normal part of that milestone's exit criteria, or (b) flagged as "not yet assigned," in which case it must get a milestone assignment before the phase of work it would block is considered complete. Nothing on this list is meant to persist indefinitely — a technical debt item with no paydown plan is really just an unaddressed risk wearing a more comfortable name, and should be re-filed in `RISK_REGISTER.md` if it's ever found drifting that way.
