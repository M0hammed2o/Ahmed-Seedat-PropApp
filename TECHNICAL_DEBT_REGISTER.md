# Technical Debt Register

Distinct from `RISK_REGISTER.md`: a **risk** is something that might go wrong. **Technical debt** here is a deliberate, known simplification or incomplete cutover — a specific, scoped gap between the current state and the target state, taken on knowingly rather than discovered as a surprise. Every item has an owning milestone where it gets paid down; nothing here is open-ended.

| ID    | Debt item                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | Why it exists                                                                                                                                                                                                                                                                                                                | Paydown milestone                                                                                                                                                                                                                                                                        | Interest (cost of leaving it)                                                                                                                                                                                             |
| ----- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| TD-01 | `properties.owner_user_id` still exists (nullable) alongside `org_id` (required, RLS-enforced). **Blocker resolved 2026-07-31, drop not yet executed**: migration `20260101000032` (M11) rewrote `document_categories`/`property_expected_categories`/`documents`' policies to route through `properties.org_id`/`has_org_role()` instead of `properties.owner_user_id` — the actual cross-table dependency that blocked M5's `DROP COLUMN` attempt. Re-verified live (rolled-back transaction): the drop now succeeds. Not executed as part of M11 on purpose — the drop is a distinct, deliberate contract-step decision (irreversible-in-spirit even pre-launch), not something to bundle silently into an RLS migration.                                                                                                                                                                                                                                                                                                 | Originally the deliberate expand/contract pattern (`DATABASE.md` §14); the cross-table RLS dependency was a genuine blocker discovered by execution (2026-07-30), now removed by execution (2026-07-31).                                                                                                                     | A short, dedicated follow-up migration whenever the column drop itself is wanted — no longer blocked, just not yet done.                                                                                                                                                                 | Low — column is inert, unused by application code, and no longer blocks anything.                                                                                                                                         |
| TD-02 | ~~`documents`/`bills`/`payments`/`payment_matches`/`extraction_jobs`/`audit_events` still carry `owner_user_id`, not yet cut over to `org_id`~~ — **paid down 2026-07-31** (`TASKS.md` M11, migration `20260101000032`): all six now have `org_id` (RLS-enforced) with `owner_user_id` relaxed to nullable, matching the `properties` pattern. `subscriptions`/`subscription_events` were incorrectly grouped into this item originally — they're superseded by `organization_subscriptions`/`subscription_payments`, not merely mis-scoped, and were deliberately excluded from the M11 migration (a retire-vs-rescope question, not an org_id-add question).                                                                                                                                                                                                                                                                                                                                                               | Same reasoning as TD-01 — sequenced with the Properties cutover; completed once M11's schema work landed.                                                                                                                                                                                                                    | Closed. `subscriptions`/`subscription_events`' actual disposition (retire outright once nothing reads them, vs. formally deprecate) is not yet its own tracked item — should be, once M19 (Super Admin, the only remaining plausible reader) is built and it's clear nothing needs them. | None remaining for the six cut-over tables. `subscriptions`/`subscription_events` carry no urgency (both already functionally dead weight, superseded since M1).                                                          |
| TD-03 | ~~`admin_users`/`is_admin()` not yet renamed~~ — **paid down 2026-08-01** (`TASKS.md` M19, migration `20260101000044`): renamed to `platform_admin_users`/`is_platform_admin()`, plus `support_access_sessions.admin_user_id` → `platform_admin_id` (matching `SUPER_ADMIN.md` §6's own documented column name, a mismatch found in the same pass). All call sites and pgTAP fixtures updated; confirmed live post-reset.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | Was deferred deliberately (ADR-007) to avoid touching live, working `apps/admin` auth code ahead of the Super Admin rebuild that opens those files anyway.                                                                                                                                                                   | Closed.                                                                                                                                                                                                                                                                                  | None remaining.                                                                                                                                                                                                           |
| TD-04 | ~~Demo-mode auth bypass~~ — **paid down 2026-07-30**: dual-gated fix implemented and build-verified across `apps/admin` and `apps/mobile`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | Was carried forward unresolved from the PropVault era.                                                                                                                                                                                                                                                                       | `TASKS.md` M2 (Authentication) — closed                                                                                                                                                                                                                                                  | Now Low (see R-01 in `RISK_REGISTER.md`, downgraded Critical→Medium). Remaining item is the CI production-deploy assertion, which needs a real deploy pipeline to assert against (M24).                                   |
| TD-05 | RLS isolation test suite is written as a pattern (one file, `rls_isolation.test.sql`) but not extended to the new multi-tenant schema, and not executed at all (no Docker in this sandbox).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | Sandbox limitation, not a design choice — carried forward from the same limitation noted in the PropVault-era `KNOWN_BUGS.md`.                                                                                                                                                                                               | `TASKS.md` M3/M23                                                                                                                                                                                                                                                                        | **High** — this is the test category `TESTING.md` §2 itself calls "non-negotiable" for a multi-tenant system; also tracked as R-02 (Critical).                                                                            |
| TD-06 | `packages/types/src/property.ts` (`Property` interface) does not yet expose `orgId`, even though the database column exists.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | Deliberately deferred alongside TD-01 — adding it to the shared type now would force every consuming call site to handle it before the schema cutover is ready, which is exactly the "big-bang" risk the expand/contract pattern avoids.                                                                                     | `TASKS.md` M5                                                                                                                                                                                                                                                                            | Low — purely a TS/DB drift, caught immediately by `pnpm typecheck` the moment it matters (nothing currently reads a non-existent field).                                                                                  |
| TD-07 | `usage_events`/`usage_snapshots` tables are designed (`DATABASE.md` §7) but no subsystem actually writes to them yet — every "per-org usage" metric in `SUPER_ADMIN.md`/`AI_ARCHITECTURE.md` is schema-ready but data-empty until each subsystem (email, WhatsApp, OCR, AI, storage) is built and instrumented.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | Schema-first design, matching the pattern used throughout this codebase (design the table, wire the writer when the owning feature is built).                                                                                                                                                                                | Distributed — each subsystem's own milestone (M11 storage, M16 email, M17 WhatsApp, M18 AI) instruments its own `usage_events` writes.                                                                                                                                                   | Low — no metric silently lies; it's simply empty/zero until instrumented, not wrong.                                                                                                                                      |
| TD-08 | `accounting_periods` (period locking) is designed (`DATABASE.md` §9, `ACCOUNTING.md` §9) but not built — nothing currently stops a backdated post in the eventual implementation until this lands.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | Identified and designed in this review pass; implementation is naturally part of the accounting engine build, not before it.                                                                                                                                                                                                 | `TASKS.md` M14 (Accounting)                                                                                                                                                                                                                                                              | Medium once the accounting engine exists and has real usage — zero today since no accounting engine is built yet either.                                                                                                  |
| TD-09 | Malware/content-scanning on file uploads is specified (`SECURITY.md`, this review) but not implemented — the only current upload safeguard is MIME-type allow-listing.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | Identified in this review; implementation is Documents-module work.                                                                                                                                                                                                                                                          | `TASKS.md` M11 (Documents)                                                                                                                                                                                                                                                               | Medium-High once real uploads from real (potentially untrusted) tenant/vendor users occur — zero today, no upload pipeline exists yet. Also tracked as R-03.                                                              |
| TD-10 | Rate limiting has a scaffolded constant set (`packages/config` `RATE_LIMITS`) with no wired backing store.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | Carried forward unresolved from the original `SECURITY.md`.                                                                                                                                                                                                                                                                  | `TASKS.md` M2 (Authentication) or M24 (Deployment) — not yet definitively assigned, flagged here so it isn't lost between milestones.                                                                                                                                                    | Medium — auth/webhook endpoints are unprotected against brute-force at the infrastructure level until this lands.                                                                                                         |
| TD-11 | `apps/mobile` (Expo) remains in the repository, superseded in architecture (`MOBILE_ARCHITECTURE_DECISION.md`) but not deleted, and still has one known unresolved defect (jest-expo/Windows test-runner bug).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | Retained deliberately as living reference documentation for native screen/field requirements until `apps/ios`/`apps/android` reach parity.                                                                                                                                                                                   | Implicit — no dated retirement milestone exists yet; should be added once M21/M22 are substantially complete.                                                                                                                                                                            | Low — never deployed to app stores, doesn't affect production; mild ongoing cost of a stale codebase lingering in the monorepo (CI still runs its (passing) lint/typecheck).                                              |
| TD-12 | `AI_ARCHITECTURE.md`'s evidenced "AI-assisted unit setup" feature (natural-language → generated units, `PROPVIEW_SCREENSHOT_AUDIT.md` IMG_7998) has no architecture document covering it — flagged in `PRODUCT_SPEC.md` §5 as needing its own design pass.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | Discovered during `PRODUCT_SPEC.md` compilation; genuinely not designed anywhere yet, unlike the Assistant/Portfolio Intelligence which are fully specified.                                                                                                                                                                 | Not yet assigned — needs a design pass before `TASKS.md` M6 (Units) can build it, or M6 ships without this specific evidenced feature and it's added later.                                                                                                                              | Low today (nothing depends on it yet); should be resolved before M6 claims completion if this feature is meant to be in V1.                                                                                               |
| TD-14 | ~~`audit_events`' live schema didn't match `DATABASE.md` §10's target shape~~ — **schema paid down 2026-08-01** (`TASKS.md` M18, migration `20260101000043`): `actor_type` extended to `user\|system\|api\|ai_assisted`, `target_type`/`target_id` renamed to `entity_type`/`entity_id`, `owner_user_id`/`metadata` dropped, `before`/`after`/`ip_address`/`ai_conversation_id`/`ai_message_id` added. Forced now because `AI_ARCHITECTURE.md` §5's audit requirement genuinely could not be represented on the old schema. **Remaining, narrower item**: the two call sites that were already documented as deliberately abstaining from writing an audit row pending this exact migration — `reopen_accounting_period()` (`TASKS.md` M14) and `POST /api/v1/organizations` (`TASKS.md` M5) — are now unblocked but not yet wired up; each needs its own small change, deliberately not bundled into the schema migration itself to avoid re-opening and re-verifying M5/M14's already-shipped route code in the same pass. | Same `owner_user_id`-era table as `properties`, never updated when the org-scoped design was written; not caught by the earlier architecture review because the review checked the _design_ doc against _itself_, not against the live migration.                                                                            | Schema: closed. Wiring `reopen_accounting_period()`/`POST /api/v1/organizations`'s actual audit writes: not yet assigned — small, mechanical, safe to do in either's own next touch.                                                                                                     | Low now that the schema is correct — the only remaining cost is the two endpoints still not writing an audit row, unchanged from before (no new endpoints have been silently added to the blocked list since 2026-07-31). |
| TD-15 | `apps/mobile/app/(onboarding)/add-first-property.tsx` assumes a user can create a property immediately after signup — true in the single-owner model, no longer true now that a property needs an organization. Mobile has no create-organization screen (only web does).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | Discovered 2026-07-30 while cutting `properties` over to org-scoping (M5) — the schema/data-layer fix is mechanical, but "what should mobile onboarding actually do now" is a real product/UX decision, not a mechanical follow-on.                                                                                          | Not yet assigned — candidate: M21 (Native iOS) once the native onboarding flow is designed for real, or a stopgap web-redirect if mobile ships before then.                                                                                                                              | Low today (mobile is a legacy Expo reference app, not deployed); rises to Medium once native onboarding is actually being built and this decision can no longer be deferred.                                              |
| TD-16 | ~~`customers/page.tsx`'s `getCustomers()` queried `properties.owner_user_id`, silently omitting every property created through the real M6 API~~ — **`customers/page.tsx` and `subscriptions/page.tsx` resolved 2026-08-01** (`TASKS.md` M19): both explicitly rebuilt as part of M19's own spec-mandated scope (`SUPER_ADMIN.md` §0), which is the authorization this row was waiting on — M19's own instructions to rebuild these exact files superseded the earlier "needs Mohammed's decision before editing" caution. See `RISK_REGISTER.md` R-21. **`processing/page.tsx` and `adminMockData.ts` (same file family) remain unaddressed** — not individually re-checked for the identical `owner_user_id` issue, and still pre-existing uncommitted work outside M19's scope.                                                                                                                                                                                                                                           | Discovered 2026-07-30 while tracing M5's blast radius. `customers`/`subscriptions` fixed because M19 explicitly mandated rebuilding them; `processing.tsx`/`adminMockData.ts` were never in M19's scope, so the original caution (editing pre-existing uncommitted work needs explicit authorization) still applies to them. | `processing.tsx`: not yet assigned — needs the same check `customers.tsx` got (does it reference `owner_user_id` the same broken way?) and, if so, the same Mohammed-authorization question TD-16 originally posed.                                                                      | Low for `customers`/`subscriptions` (fixed). Unknown-but-unverified for `processing.tsx` — flag before deploying `apps/admin` against a live post-M5 database.                                                            |
| TD-13 | No accounting-data export path (CSV/standard format) to third-party accounting software exists in the design.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | Accepted consequence of ADR-004 (in-house engine, no integration) — an export path is a different, smaller feature than an integration, not yet separately designed.                                                                                                                                                         | Not yet assigned (V2 candidate per `RISK_REGISTER.md` R-08).                                                                                                                                                                                                                             | Low — no client has asked for this yet; revisit if it becomes a real blocker to closing a deal.                                                                                                                           |

| TD-17 | **Resolved 2026-08-03** (migration `20260101000055`, `PWA_V1_COMPLETION_PLAN.md` #1): `has_org_role()` now joins `organizations` and branches on `status` — `archived` denies all access (even viewer), `suspended`/`cancelled` force viewer-equivalent (read-only) regardless of stored role, `trial`/`active`/`overdue` unaffected. Decided as an ordinary access-control implementation detail (inferable from `SUPER_ADMIN.md`'s own "archived orgs retain full data... never a hard delete" language and universal SaaS convention), not deferred as a business decision. Single change point — no RLS policy or API route needed editing, every call site already goes through `has_org_role()`. `multi_tenant_isolation.test.sql`'s previously-documented "CURRENT BEHAVIOR (not a guarantee)" assertion is flipped per its own instruction, plus 2 new assertions for the suspended case. Verified: real `supabase db reset` + full pgTAP (254/254 across 18 files) + `apps/admin` vitest (153/153, no regressions). | Never actually decided, not a deliberate simplification like the rest of this register — `SUPER_ADMIN.md` describes what "archive"/"suspend" mean for billing/dashboard visibility but never states what they mean for the org's own member access. Found while verifying the M1-M5 foundation end-to-end per Mohammed's instruction, not while building a specific feature. | Closed. | None remaining — Super Admin's suspend/activate/archive routes all use `getServiceRoleClient()` (RLS-bypassing), so enforcement only affects the org's own members, exactly as intended; verified no circular lockout (Super Admin can always act regardless of an org's current status). Also closes `RISK_REGISTER.md` R-22. |

| TD-18 | **New, 2026-07-31**: `encrypted_secrets` (`DATABASE.md` §11) exists as a schema-only table (migration `20260101000027`) — `id`, `ciphertext bytea`, `created_at`, RLS locked to service-role only. The application-layer encrypt-before-insert pipeline (key management, `SECURITY.md`) is not built. Nothing currently writes to this table; `tenants.id_number_ref`/`owners.banking_ref` are both nullable and unpopulated. | Built ahead of any caller specifically so `tenants` (M8) and `owners` (already-existing `banking_ref`, retrofitted with a real FK in the same migration) have a schema matching `DATABASE.md` exactly — implementing the full encryption pipeline before any real feature needs to write a secret would be speculative, unverifiable work (no way to test key management without a concrete usage flow driving it). | The first feature that actually needs to capture a tenant ID number or owner banking details (candidate: M8's Web UI, or a dedicated Banking/Compliance milestone) — building the pipeline alongside its first real caller, not before. | Zero today (unused table, no PII at risk). Rises to Medium the moment any UI/API tries to write a plaintext value into `id_number_ref`/`banking_ref` directly instead of routing through the (not-yet-built) encryption pipeline — flag this explicitly if it's ever attempted as a shortcut. |
| TD-19 | Trigram (`pg_trgm`) search indexes specified in `DATABASE.md` §13 (Properties, Tenants, Owners, Vendors, Documents, Audit Log) are not implemented anywhere, including for tables that already exist (`properties`, `owners`, and now `tenants`). | Identified in the 2026-07-30 Production Readiness Review as a fix, but never actually built — no evidenced search-box UI has been implemented yet for any of these tables to require it, and adding an index with no query using it would be unverifiable/speculative. | Whichever milestone first builds a real search box against one of these tables (Properties/Owners/Tenants list UI, none built yet) — add the specific `gin_trgm_ops` index that UI's actual query needs, not all of them preemptively. | Low today (no search queries exist to be slow); becomes a real performance risk only once list views with search exist and have production-scale row counts. |

| TD-20 | **Rent-schedule half resolved 2026-08-02** (migration `20260101000050`, `TASKS.md` M10): `generate_rent_schedules_for_lease()`/`generate_rent_schedules_for_active_leases()` now fill in every subsequent period for an active lease, idempotently, callable via `POST /api/v1/system/generate-rent-schedules`. Verified live: real `supabase db reset` + 192/192 pgTAP. **Still open**: no production scheduler actually calls that endpoint on a cadence yet (needs a real cron trigger, `TASKS.md` M24, once a hosting platform is chosen) — the generation logic itself is no longer the gap, only its automatic triggering is. **Also still open, same root cause, 2026-08-01**: Portfolio Intelligence's scheduled Edge Function evaluation (`AI_ARCHITECTURE.md` §2.5) and the `usage_snapshots` rollup job (§4) are both callable, tested functions (`reconcilePortfolioInsights()`; usage-cap enforcement currently sums `usage_events` directly instead) with nothing invoking them on a schedule. | Generating an unbounded series upfront (for a lease that may have no `end_date`) inside a single atomic transaction was rejected as the wrong design — that's a recurring-job problem, not a one-time-creation problem, and inventing ad-hoc scheduling logic inside `approve_application()` would conflate two different concerns. | A real scheduled-function/cron milestone — candidate `TASKS.md` M24 (Deployment) once the hosting platform's scheduling primitive is chosen: wire an actual timer against the now-existing `/api/v1/system/generate-rent-schedules` endpoint (and the Portfolio Intelligence/usage-rollup jobs the same way once each has its own callable endpoint). | Low today for rent schedules specifically — the generator is idempotent and safe to call manually (Super Admin) or via any interim scheduler in the meantime; the only remaining risk is forgetting to wire the real cron trigger before relying on it unattended in production. Portfolio Intelligence/usage-rollup: unchanged, zero today (no production usage exists to schedule against yet). |

| TD-21 | **(1) Resolved 2026-08-01** (migration `20260101000048`): `documents` bucket storage.objects policies now check `has_org_role((storage.foldername(name))[1]::uuid, ...)` (viewer/agent, matching the `documents` table's own thresholds) against a `{org_id}/{property_id}/{uuid}{ext}` path, built alongside the real web upload flow (`POST /api/v1/documents`, TASKS.md M11/M20). Verified live: real `supabase db reset` + full pgTAP (176/176). **(2) Still open**: no real (non-demo) mobile document upload flow exists — only the web (`apps/admin`) path was built this pass; the equivalent native mobile/Android/iOS upload flow remains future work. | (1) Built alongside the first real Documents web UI, the natural moment TD-21 itself named ("whichever milestone first builds real document upload"). (2) Native upload flows depend on their own app's storage-client wiring, out of scope for a web-first pass. | (2) M21/M22 (native apps), whenever their own document-upload screens are built — same `{org_id}/{property_id}/{uuid}{ext}` convention and policies already apply, no further RLS work needed, just client code. | (1) Resolved — real isolation now enforced. (2) Zero (no native upload code exists yet to be insecure). |

| TD-22 | **Deposit release/interest resolved 2026-08-02** (migration `20260101000051`, `TASKS.md` M14 part 3): `release_trust_deposit()` settles a trust ledger's entire balance at move-out (Dr Tenant Deposits Held / Cr Trust Bank for the total, plus Dr Business Bank / Cr new `4900 Deposit Deduction Income` for any deduction portion), gated on a completed `move_out` inspection (ACCOUNTING.md §4), one-time/terminal via a new `trust_ledgers.status`. `accrue_trust_interest()` posts simple daily-prorated interest at `organizations.deposit_interest_pct` (Dr new `5950 Trust Interest Expense` / Cr Tenant Deposits Held) as an explicit accountant-triggered action (not an unattended cron job — no scheduler infrastructure exists yet, same gap as TD-20) — `accrue_trust_interest_for_org()` offers a same-session bulk convenience across one org's active ledgers. `POST /api/v1/trust-ledgers/:id/release` and `.../accrue-interest` added. The two new system accounts (`4900`, `5950`) are the account-mapping decision this item was originally blocked on — decided and documented inline rather than left implicit, backfilled onto every existing org. Verified: real `supabase db reset` + new `trust_deposit_release_and_interest.test.sql` (21 assertions) + full regression 213/213 pgTAP across 15 files; a real RLS/FOR UPDATE interaction bug was found and fixed by this test suite (see migration's own comment) before it shipped. **Still not built**: a `calculateMatchScore`-based _proposal_ step for bank transaction matching (`confirm_bank_transaction_match()` still requires the caller to already know which `rent_schedule` to match against, not the evidenced two-step propose-then-confirm flow). Unattended interest-accrual scheduling remains open, bundled with TD-20's cron-infrastructure item. | The two release/interest functions originally needed an accounting-account mapping `ACCOUNTING.md` didn't specify — resolved by adding two clearly-labeled new system accounts rather than leaving it unmapped, since `ACCOUNTING.md` §4 already gave an unambiguous computation/gating rule, just not the GL account pairing. The match-proposal step remains deprioritized — genuinely independent, smaller, non-money-handling work. | Match-proposal: schedulable independently, low priority. Unattended interest-accrual scheduling: bundled with TD-20's eventual cron milestone. | Deposit-release/interest: low — verified correct against a real test suite; the only remaining gap is manual-trigger-only interest (an accountant must remember to run it), not a computation or safety gap. Match-proposal: low — the confirm-only flow works, it's just less automated than evidenced. |

| TD-23 | **Fully resolved 2026-08-02** (`apps/admin/lib/emailDispatch.ts` + `apps/admin/lib/whatsappDispatch.ts`, `TASKS.md` M16/M17): email dispatches 5 real product events; WhatsApp dispatches 3 (`payment_accepted`, `owner_statement_available`, `maintenance_update_critical`), deliberately a smaller subset since 13 of the 16 `WhatsAppNotificationType` values require a scheduled-detection job (rent overdue, lease expiring, ...) this codebase still doesn't have — same root gap as TD-20, not solved here, correctly left unwired rather than guessed at. Both dispatchers are idempotent, preference-gated, and verified with real integration tests against local Supabase (12 total, including a genuine uuid-column-misuse bug the email tests caught before it shipped). | Both channels needed the same missing piece (something calling the already-built provider/schema layer) — closing both in the same functional-completion pass, one smaller-scoped than the other per each channel's own documented restraint (`EMAIL.md`: comprehensive/default; `WHATSAPP.md`: narrow/urgent-only). | Closed for the triggers with a real synchronous cause. The 13 remaining WhatsApp trigger types and any time-based email reminders (e.g. lease-expiring emails) are blocked on real scheduled-job infrastructure — bundle with TD-20's eventual cron milestone, don't re-solve independently per channel. | Zero today for both channels' wired triggers (still zero real deliveries until real vendor accounts exist — mock providers only). The 13 unwired WhatsApp types and any lease/rent-overdue email reminders remain a real product gap once a scheduler exists and staff expect them to fire automatically — flag explicitly if that expectation forms before the scheduler is built. |

| TD-24 | **New, 2026-08-01**: `apps/admin/lib/superAdmin.ts`'s `computePlatformMetrics()` (the Overview dashboard, `TASKS.md` M19) computes every platform-wide metric live, on every page load, rather than reading `platform_metrics_snapshots` the way `DATABASE.md`'s own architecture review (line ~374) describes ("a live count(_)/sum(_) across every org's rows on every dashboard load is a real, avoidable cost... at 'tens of thousands of organisations' scale"). No `platform_metrics_snapshots` table was created. | Deliberate: the snapshot optimization solves a scale problem this system doesn't have yet (near-zero real organizations, pre-launch) — building the table now, with no scheduler to keep it fresh (same root cause as TD-20), would reproduce exactly TD-20's anti-pattern (schema with no real caller) for an optimization nothing currently needs. Computing live today is not actually costly. | Whichever milestone first needs the optimization for real — almost certainly bundled with TD-20's eventual cron/scheduled-function infrastructure, since `platform_metrics_snapshots` would need the same scheduler `usage_snapshots`/`portfolio_insights` do. Revisit if real org counts grow enough that live computation becomes measurably slow. | Zero today. Rises with real production org-count growth — watch for it, don't guess a threshold now. |
| TD-25 | **New, 2026-08-01**: Support-mode's "read-only by default, explicit escalation per write" enforcement (`SUPER_ADMIN.md` §6, items 1-2) has no concrete caller. `support_access_sessions` start/end (`TASKS.md` M19) is built and tested, but there is no client-org-facing dashboard UI anywhere in this codebase yet for a platform admin to actually browse during an open session — that surface is `apps/web`'s client-facing routes (M20, not started). | Building the scoping/escalation-logging mechanism now, with nothing to wire it into, would be speculative infrastructure with no real caller to verify it against — the same judgment already applied in TD-18 (encrypted-secrets pipeline) and TD-21 (real document-upload flow). | `TASKS.md` M20 (Responsive Web), the first milestone that gives a platform admin something to browse during a support session — build the scoping/escalation logic alongside the client-facing routes it would apply to, not before. | Zero today (no client-facing UI exists to escalate against). Rises to a real gap the moment M20 ships client-org pages without this enforcement wired in — flag explicitly if M20 is ever marked complete without it. |

| TD-26 | **New, 2026-08-01**: `apps/android`'s first vertical slice (`TASKS.md` M22) has three specified-but-not-built gaps: (1) `androidx.biometric`/`androidx.security-crypto` dependencies are present and a biometric re-entry gate is scaffolded, but not wired to an actual `BiometricPrompt` call — sign-in is currently password-only end to end. (2) Deep links (`NATIVE_ANDROID_SPEC.md` §11) are not implemented — no `intent-filter`/Navigation Compose deep-link handling exists in `AndroidManifest.xml` or `RootNavGraph.kt`. (3) Push notifications are not implemented — no FCM dependency, no `PushEmailProvider`-equivalent Android sender registration. Related, cross-platform: the Compose theme's colours/type/shape (`ui/theme/*.kt`) are hand-transcribed from `packages/ui/src/tokens.ts` rather than generated, so the two can silently drift — `NATIVE_IOS_SPEC.md` §15 and `NATIVE_ANDROID_SPEC.md` §14 both flag the same not-yet-built token-export codegen step. | This vertical slice deliberately scoped to Auth shell (password only) + Owner portal (Dashboard/Properties list/detail read-only), per Mohammed's explicit instruction not to build every module simultaneously — biometric, deep links, and push notifications are all real `NATIVE_ANDROID_SPEC.md` requirements, just not part of this first slice's proof-of-toolchain scope. | Biometric: next slice that touches Auth again, or a dedicated security-hardening pass before any real credential is at risk. Deep links: whichever slice first needs one (candidate: notification-triggered navigation, once push exists). Push notifications: needs its own milestone once a real FCM project/APNs equivalent is provisioned — same "don't build against nothing real" judgment as TD-23's notification-dispatch wiring. Token codegen: a small, standalone follow-up, not owned by any single feature milestone — candidate whenever the third platform (iOS) starts implementation and the drift risk becomes real instead of theoretical. | Biometric: Low today (no real user credentials at stake in a mock/demo build) — rises to Medium the moment this app handles a real login. Deep links/push: zero today (no notification-sending backend calls Android at all yet — same root gap as TD-23, just from the Android-receiving side). Token codegen: Low — a manual-transcription mismatch would be a visual bug, not a security/data issue, and was diffed by eye against `packages/ui/src/tokens.ts` during this slice's dark-mode verification. |

| TD-27 | (1) **Resolved 2026-08-06** (Stage 7 Phase 9 UI polish pass): all 24 remaining inline copies of `role !== 'viewer' && role !== 'accountant'`, across 23 page files (grew from the original 8-file M20 scope as later milestones reused the same unwrapped expression), now call the existing `canWriteOrgRecords()` (`apps/admin/lib/orgSession.ts`) instead. Each file's exact existing `Boolean(...)` wrapper/null-guard/variable name (`membership.role` vs. `activeOrg.role`) preserved unchanged — pure mechanical substitution, no behavior change. Verified via grep: zero remaining inline copies outside `canWriteOrgRecords()`'s own definition. (2) ~~The Accounting module covers only Rent Due/Expenses/Trial Balance — bank transaction matching, owner statements, and the tax pack all have no UI at all yet~~ — **stale, corrected 2026-08-03** (`PWA_V1_COMPLETION_PLAN.md` audit): all three now have real UI — Bank Transactions (`/accounting/bank-transactions`, inline match control), Owner Statements (`/accounting/owner-statements`, generate action), Tax Pack (`/accounting/tax-pack`, year picker + CSV export), all built in this session's earlier functional-completion pass and since redesigned. The only remaining accounting-UI gap is TD-22's smaller, already-tracked item (a `calculateMatchScore`-based propose-then-confirm suggestion step, not "no UI"). | (1) Deferred once as low-risk-low-urgency, then completed as part of a broader, dedicated Phase 9 UI-consistency pass rather than left open indefinitely — grouping it with the other consistency-pass fixes meant a single `tsc`/`eslint`/`vitest`/`build` verification pass covered all of it. (2) N/A — resolved. | (1) Closed. (2) Closed. | (1) None remaining — resolved, zero functional change, verified clean by grep plus full typecheck/lint/vitest/build. (2) None remaining. |

| TD-28 | **Resolved 2026-08-02** (`TASKS.md` M10, no migration — `apps/admin/lib/supabase/server.ts` only): `getServerSupabaseClient()` now checks for `Authorization: Bearer <jwt>` first (via `next/headers` `headers()`) and, when present, builds a client whose every REST/RPC call carries that JWT (`global.headers.Authorization`), with `auth.getUser()` overridden to default to the same token — every existing route handler's unchanged `await supabase.auth.getUser()` call works for both caller types with zero per-route edits. Falls back to the original `@supabase/ssr` cookie path when no bearer header is present (verbatim-unchanged code, now in an `else` branch). Verified live against the real local Supabase Auth server (not mocked): valid token resolves the real user and RLS-scopes `.from()` reads to it; invalid/garbage token returns no user; cross-org and role-restricted writes still 403 via the unchanged `requireOrgRole()` checks; missing-token/cookie-less requests still 401, matching pre-change behaviour exactly. `apps/admin/lib/supabase/__tests__/server.test.ts` (9 cases, 4 of them real integration tests against `supabase start`) plus a live `POST /api/v1/maintenance-tickets` and `POST /api/v1/device-push-tokens` run with a real Supabase-issued JWT, both succeeding end to end. | Every admin API route was originally built and verified only against the web app's own cookie-based session — the web client was always the only caller exercised, so this gap had no caller to surface it until a native write path was attempted. Fixed at the single shared auth-resolution point (`getServerSupabaseClient()`) rather than per-route, so every existing and future route gains Bearer support automatically. | Closed for the auth-resolution mechanism itself. Individual native (Android/iOS) screens still need their own client-side wiring to actually send the header and call these now-unblocked write endpoints — that remains each screen's own future work (`TASKS.md` M21/M22), not an auth-layer gap anymore. | Low — the mechanism is verified correct against a real Supabase Auth server; remaining risk is ordinary "did the native client wire the header correctly" per-screen work, not a security gap in the shared resolver. |

| TD-29 | **New, 2026-08-03**: Google/Apple OAuth are implemented end-to-end (`OAuthButtons.tsx`, `/auth/callback`, `supabase/config.toml` `[auth.external.google]`/`[auth.external.apple]`, both `enabled=false` with `env()` references) but have never completed a real provider round trip — no Google Cloud OAuth client, no Apple Services ID/key/team ID exist yet, both requiring Mohammed's own developer accounts. Email/password registration and the tenant-activation flow _were_ verified end-to-end against a real local Supabase instance (real email round trip via Mailpit, real PKCE exchange); only the two OAuth providers remain unverified live. | Cannot be closed without credentials only Mohammed can provision (Google Cloud Console project, Apple Developer Program enrollment) — inventing placeholder production credentials would violate the standing "never invent production client IDs/secrets" instruction. | Whenever Mohammed provisions real Google/Apple OAuth credentials — set the four `.env.example`-documented variables, flip `enabled=true` in `supabase/config.toml`, and run one real sign-in per provider against local Supabase before considering V1 auth complete. | Medium — the code path is real and structurally identical to the verified email/password path (same `/auth/callback`, same identity-linking rules), but "the code should work" is not the same as "a real provider round trip was observed," per this project's own evidence rule. |
| TD-30 | **New, 2026-08-03**: the `tenant_invitation` email/WhatsApp dispatch events (`emailDispatch.ts`/`whatsappDispatch.ts`, wired into `POST /api/v1/tenants/:id/invitations`) use the same mock providers as every other channel in this codebase (TD-23) — no real delivery has ever been sent for this specific template. Manual activation-code delivery (staff reads the code aloud/copies it) needs no provider and works today regardless. | Same root cause as TD-23 — no real email/WhatsApp vendor account exists yet for _any_ template; this is one more template on an already-tracked gap, not a new one. | Closes automatically alongside TD-23 once a real email/WhatsApp vendor is selected and credentials provisioned — no template-specific work needed beyond what TD-23 already requires. | Zero today (mock-only, matches every other dispatch event). Rises alongside TD-23 once staff expect real delivery for any channel. |
| TD-31 | **Resolved 2026-08-06** (Stage 7, commercial-launch execution plan): `LoginForm.tsx`/`RegisterForm.tsx`/`ForgotPasswordForm.tsx` now call new server routes (`apps/admin/app/api/v1/auth/{signin,signup,password-reset}`) instead of the Supabase Auth (GoTrue) client SDK directly -- `rateLimitOrRespond()` (migration `20260101000058`) now has a real hook point and is actually enforced, keyed on IP and (for signin) email independently. Verified live: a real Playwright test (`e2e/auth-security.spec.ts`) makes 11 rapid wrong-password requests against the real route and confirms the 11th is rejected with 429. The self-hosted-GoTrue-config approach originally attempted (see prior text of this row) remains empirically disproven, not revisited. | N/A -- closed. | N/A -- closed. | None remaining for rate limiting specifically. |

| TD-32 | **New, 2026-08-05** (Stage 1 cutover): `has_property_access()`/`grant_property_access()`/`revoke_property_access()` (migration `20260101000063`) and the resulting RLS cutover on 7 of 8 scoped tables (`20260101000064`-`20260101000070`) are fully functional but have no UI at all — narrowing a staff member's access to specific properties currently requires calling the RPC directly (e.g. via `supabase.rpc('revoke_property_access', ...)` from a console/script), not a screen in the app. | Deliberate scope cut for Stage 1: the plan's own phasing puts the assignment UI under Phase 4/5 (property-level permissions UI, owner portal), built on top of this foundation rather than in the same pass as the schema/RLS work. | Phase 4/5 of the commercial-launch execution plan — not yet assigned a milestone number in this file. | Medium — the access-control mechanism itself is real and tested, but until a UI exists no customer can actually use it to restrict a coworker's access; today's zero-regression backfill/triggers mean everyone still sees everything, matching pre-cutover behavior, so this is a missing feature, not an open vulnerability. |
| TD-33 | **Narrowed, 2026-08-05** (Stage 2, `20260101000071`): `owner_statements` was NOT gated on `has_property_access()` — it's owner-scoped, not property-scoped, and a single statement can legitimately aggregate rent/expenses across every property that owner holds in the org. A staff member with no property_access at all on any of an owner's properties can still see that owner's full statement today. **Stage 2 fixed the statement's math (ownership % now correctly resolved as of the period, via `property_ownership_history`), not its visibility** — this line item is specifically about the remaining visibility gap, unchanged. | Genuine structural mismatch, not an oversight — a per-property gate on a table with no single property_id would need the statement's own shape to change further (splitting or annotating per-property contributions within a multi-property statement, e.g. a per-property breakdown table), beyond what Stage 2's reserve/partial-payout/history-awareness work covered. | Phase 4/5 of the commercial-launch execution plan (property-level permissions UI / owner portal) — property-scoped statement visibility is really an owner-portal-readiness question, not a data-layer one at this point. | Medium — same as before: narrower in practice while most orgs don't yet use property-level restriction (TD-32), becomes a real gap the moment a customer starts using it. |

| TD-34 | **New, 2026-08-05** (Stage 2 close-out, reports/dashboards audit): `GET /api/v1/trial-balance`'s "Balanced" health check (`totalDebit == totalCredit` across whatever `journal_lines` rows the caller can see) assumes it's looking at the _whole_ org ledger — its own code comment says a `false` result means "the immutability/balance invariant was violated somewhere... investigate." That assumption breaks the moment property-level restriction is actually used (TD-32): a user with access to only some of an org's properties will structurally see an unbalanced partial ledger (e.g. a property-tagged expense line without visibility into the org-level bank account line it offsets), which is completely expected, not a data-integrity bug — but the UI/route gives no indication of that, so a restricted accountant would reasonably read "Balanced: false" as something broken. | Found by auditing every report-adjacent route for property-access compliance (reports/dashboard pages, trial-balance, tax-pack) — all confirmed to correctly respect `has_property_access()` automatically via RLS (none use the service-role client or a security-definer RPC that would bypass it), except this one pre-existing UX assumption that a full ledger view is guaranteed. | Not yet assigned — likely a small fix (e.g. only compute/show "Balanced" when the caller has org-wide, unrestricted property access; or an explicit caveat in the response/UI when it doesn't) once property-level restriction is actually in use by a real customer (currently nobody is, per TD-32). | Low today (no customer uses property-level restriction yet, so this is unreachable in practice); becomes a real, if minor, support-confusion risk the first time someone does. |

| TD-35 | **New, 2026-08-05** (Stage 3 governance close-out): `expenses` and `property_owners` are audited at the _app layer_ only (Stage 0's `writeAuditEvent()` calls in specific API routes), while `owner_statements`/`cash_receipts`/`maintenance_tickets` are audited at the _DB-trigger_ layer (this date, more robust — catches every future mutation path automatically, not just the ones a route handler calls). Two different audit mechanisms for the same governance promise, not yet consolidated onto one. | Deliberate sequencing, not an oversight: adding a trigger to `expenses`/`property_owners` now, on top of their existing app-layer calls, would double-log every action (two `audit_events` rows per real change) — a data-quality regression, not an improvement. Removing the app-layer calls in the same pass as adding triggers was judged more change than this specific migration should carry. | Not yet assigned — a small, well-understood follow-up: add the trigger to both tables, remove the now-redundant `writeAuditEvent()` calls in the four Stage-0 routes, verify no duplicate rows. | Low — both tables ARE audited today, correctly, just via two different mechanisms; the only real cost is minor code-path inconsistency, not a coverage gap. |

| TD-36 | **New, 2026-08-05** (Stage 4, commercial-launch execution plan): `PayFastBillingGatewayProvider` (`apps/admin/lib/providers/payfast.ts`) is implemented end-to-end and unit-tested against independently-reimplemented signature logic, but has never completed a live round trip against PayFast's own sandbox or production servers — no real PayFast merchant account exists in this environment (same class of gap as TD-29's Google/Apple OAuth). The Subscriptions/Refunds Management API methods (`cancelSubscription`/`refundPayment`) carry the least confidence of the three call groups — their signature construction was reconstructed from one concrete working example, not official docs, unlike checkout/ITN which was cross-checked against 3+ independent sources. Separately, PayFast's `PENDING` ITN status (e.g. an EFT payment awaiting bank confirmation) is deliberately unmapped in `parseWebhookEvent()` — it throws rather than guessing whether a pending payment should count as success or failure. | Cannot be closed without credentials only Mohammed can provision (a PayFast merchant account, sandbox or live) — inventing placeholder production credentials would violate the standing "never invent production client IDs/secrets" instruction, same reasoning as TD-29. `PENDING`'s real-world frequency/shape needs a live sandbox to observe before it can be mapped correctly rather than guessed. | Whenever Mohammed provisions real PayFast sandbox credentials — set `PAYFAST_MERCHANT_ID`/`PAYFAST_MERCHANT_KEY`/`PAYFAST_PASSPHRASE`, run one real checkout→ITN→activate round trip, one real cancellation, and one real refund against the sandbox before considering Stage 4 billing fully verified. | Medium — the checkout/ITN path (the one every real subscriber's payment flows through) is cross-checked from multiple sources and has the safety property that a failed/rejected signature check fails closed (no false "paid"/"cancelled" state); the Management API path (cancel/refund) is lower-confidence and should be the first thing manually verified once credentials exist. |

| TD-37 | **New, 2026-08-06** (Stage 5, commercial-launch execution plan): `ResendEmailProvider` (`apps/admin/lib/providers/email.ts`) is real and unit-tested, but has never sent a real email -- no Resend account/API key exists in this environment (same class of gap as TD-29/TD-36). A real, separate gap found and fixed in the same pass, not just an unverified-live caveat: `SendEmailInput` had no `subject`/`bodyText` fields at all -- `dispatchEmail()` computed a subject internally but never passed it to `provider.send()`, so no real provider could ever have sent a correctly-worded email even with credentials. Fixed by adding both fields (rendered by `emailDispatch.ts`'s `TEMPLATE_SUBJECTS`/new `TEMPLATE_BODY`) and threading them through. Also: `input.attachments` (documentId -> file bytes) is deliberately NOT resolved by this provider (would require DB/Storage access from a provider class, a boundary no other provider in this codebase crosses) -- confirmed via grep that zero real callers pass attachments today, so this is a disclosed, currently-unreachable gap. | Cannot be closed without credentials only Mohammed can provision (a Resend account + verified sending domain). | Whenever Mohammed provisions real Resend credentials -- set `RESEND_API_KEY`/`RESEND_FROM_ADDRESS`, send one real email, confirm delivery. | Medium — the send path itself is straightforward and unit-tested against the real Resend SDK's actual response shape; the un-exercised risk is entirely "has this ever talked to the real API," not algorithmic complexity. |
| TD-38 | **New, 2026-08-06** (Stage 5): `MetaWhatsAppProvider` (`apps/admin/lib/providers/whatsapp.ts`) is real for the outbound send path (the only one with a real caller, `whatsappDispatch.ts`) but has never sent a real WhatsApp message -- no Meta Business/WhatsApp Business Platform account exists (same class of gap as TD-36/TD-37). Separately, and more fundamentally: `verifyWebhookSignature`/`parseInboundEvent`/`parseStatusCallback` have **no inbound webhook route calling them at all** -- confirmed via grep before writing the provider that this codebase has never had one (only outbound dispatch exists). WHATSAPP.md §1.2's phone-resolution/ambiguous-match flow and the webhook route itself remain undesigned-and-unbuilt scope beyond "swap the mock for a real provider" -- not invented here. | Outbound: cannot be closed without real Meta credentials. Inbound: a genuinely separate, larger feature (a new API route + the resolution algorithm WHATSAPP.md §1.2 already specifies) that was out of scope for "add a real provider behind the existing interface." | Outbound: whenever Mohammed provisions Meta Cloud API credentials (`WHATSAPP_ACCESS_TOKEN`/`WHATSAPP_PHONE_NUMBER_ID`/`WHATSAPP_WEBHOOK_SECRET`), send one real template message. Inbound: a dedicated milestone to build the webhook route + resolution flow -- not yet assigned. | Outbound: Medium, same reasoning as TD-37. Inbound: Low today (unreachable code, no route exists to call it) — rises the moment a real webhook route is built without also verifying these three methods against real Meta payloads first. |
| TD-39 | **New, 2026-08-06** (Stage 5): `AWSTextractDocumentIntelligenceProvider` (`apps/admin/lib/providers/documentIntelligence.ts`) is real and unit-tested against a reconstructed response shape, but has never run against live Textract -- no AWS account/IAM credentials exist (same class of gap). Two disclosed design/confidence caveats, not silently assumed safe: (1) uses Textract's synchronous `AnalyzeExpenseCommand`/`AnalyzeDocumentCommand`(QUERIES)/`DetectDocumentTextCommand` APIs, capped at a 5MB inline-bytes limit (documented AWS constraint) -- a document over that size throws a clear error rather than being silently truncated; the async S3-based flow needed for larger/many-page documents is not implemented. (2) The bill-extraction field mapping (`AMOUNT_DUE`/`TOTAL`/`ACCOUNT_NUMBER`/... normalized `Type.Text` values) was reconstructed from AWS documentation/training knowledge, not verified against a live response -- multiple candidate aliases are checked per field specifically because of that uncertainty. `classify()` is an explicit keyword heuristic on raw OCR text, not a real document-type classifier (Textract has no such feature). | Cannot be closed without credentials only Mohammed can provision (an AWS account + IAM user/role scoped to Textract). | Whenever Mohammed provisions real AWS credentials -- set `AWS_ACCESS_KEY_ID`/`AWS_SECRET_ACCESS_KEY`/`AWS_TEXTRACT_REGION`, run one real bill and one real lease document through `extractFields()`, confirm the field-name mapping actually matches what Textract returns live. | Medium-High — this is the least-confident of the three new vendor integrations (bill field-name mapping specifically), and the most likely to need a real-response-driven correction once credentials exist. |
| TD-40 | **New, 2026-08-06** (Stage 6, commercial-launch execution plan): the new Playwright suite (`apps/admin/playwright.config.ts`) runs serially (`fullyParallel: false`, `workers: 1`) against a single shared local Supabase instance -- no per-worker tenant/org isolation exists, so parallel workers could race on shared state. Fine at the current suite size (7 tests, ~45s total) but won't scale as more E2E tests are added. | Deliberate, disclosed tradeoff for this pass: building real per-worker isolation (e.g. a dedicated Postgres schema or org-prefix convention per worker) is a genuinely separate piece of test infrastructure, not a config flag. | Whenever suite runtime actually becomes a problem (candidate: once TD-41's multi-org leakage tests and the rest of TESTING.md §7's critical path are added) -- design per-worker isolation then, against real measured pain, not speculatively now. | Low today (suite is fast and small); rises as more E2E coverage is added without addressing this. |
| TD-41 | **New, 2026-08-06** (Stage 6): the real-backend E2E suite covers one org's signup -> org creation -> property creation only. TESTING.md §7's own stated design explicitly wants cross-tenant leakage checks woven into the same suite ("acting as Org A's principal... assert Org B's seeded data never appears") -- not yet built; the current suite's "properties list shows only what this org owns" check is a weak single-org signal, not real two-org leakage coverage. Also: no staging environment exists yet (Stage 8 not started) -- this suite runs against local Supabase instead, a disclosed substitution TESTING.md §7 itself doesn't anticipate. | Building a real two-org seeded fixture (and the assertions that go with it) is a meaningfully larger addition than the single-org critical path this pass delivered -- scoped out rather than rushed. | Next E2E milestone -- add a second seeded org + principal in the same test file, assert neither can see the other's properties/tenants/financials through the UI, the same check `rls_isolation.test.sql` already does at the DB layer, exercised through the full stack this time. | Medium — RLS itself is independently pgTAP-tested and verified (440/440), so this isn't a coverage gap in the enforcement mechanism, only in proving it end-to-end through the real UI the way TESTING.md §7 specifically asks for. |

| TD-42 | **New, 2026-08-06** (Stage 7, commercial-launch execution plan, `SECURITY.md` "Dependency auditing"): a real `pnpm audit` run found 10 findings total (4 moderate, 5 high, 1 critical) — 9 of them (all but the critical one) are transitive chains through devDependency build tooling only: `brace-expansion` via `eslint`'s own `minimatch`/`@typescript-eslint` chain (4 findings, a glob-expansion DoS), `vite`/`esbuild`'s dev-server (path traversal / `server.fs.deny` bypass, both dev-server-only exposure), `postcss`'s sourcemap handling (build-time only). The 1 critical finding (a vitest UI-server arbitrary-file-read) exists only in devDependencies and is exploitable only by running `vitest --ui` and exposing it externally — nothing in this repo's scripts or CI ever does that. None of the 10 are reachable in the actual deployed Next.js production runtime; `pnpm audit --audit-level critical --prod` (what CI now runs) exits 0 today. | Every one of the 9 non-critical findings needs an upstream `eslint`/`vite`/`vitest`/`postcss` release to actually fix — not a version bump this repo can make unilaterally without risking breaking the toolchain on an unrelated major-version jump. The 1 critical finding is real but dev-only and not applicable to how this repo actually uses vitest. | Re-run `pnpm audit` (full, not `--prod`) periodically as part of routine maintenance and take the first available compatible upgrade when one exists; re-check whenever `eslint-plugin-import`/`typescript-eslint`/`vite`/`vitest`/`postcss` majors are next bumped for any other reason. | Low — none of the 10 findings are reachable by an actual production request against the deployed app; CI (TASKS.md M25) now gates on the tier that would be (`critical`, `--prod`), so a genuinely dangerous new finding still blocks a merge. |
| TD-43 | **New, 2026-08-06** (Stage 7, R-03): `ClamAVMalwareScanProvider` (`apps/admin/lib/providers/malwareScan.ts`) is real and verified against a genuine local ClamAV instance (`docker-compose.yml`, EICAR test file correctly detected, clean files correctly passed, both single- and multi-chunk streaming verified). Two disclosed, real findings from that verification: (1) production deployment of the ClamAV service itself (which host, how the app reaches it) is entirely a Stage 8 (production deployment) decision, not yet made -- this is real, tested code with nowhere production-real to run yet. (2) Probed live this session: ClamAV only recognizes the EICAR test signature when the scanned stream is (almost) exactly the 68-byte test string alone -- as little as 100 bytes of padding before or after it and detection silently stops. Confirmed this is EICAR's own documented behaviour (deliberate anti-false-positive design, not a chunking bug in this provider) and does not apply to ClamAV's real malware-signature database, which uses full-content matching -- but it does mean there is no safe test string available to verify multi-chunk _detection_ the way EICAR verifies single-chunk detection; only multi-chunk streaming-of-clean-content is covered. | (1) No hosting target chosen yet (same blocker as every other Stage 8 item). (2) A structural limitation of the one safe, standard test signature (EICAR) available for this kind of testing -- there's no equivalent "large multi-chunk test malware file" industry standard to reach for instead. | (1) Whenever Stage 8's hosting target is chosen, provision a real ClamAV instance (or managed equivalent) reachable from the deployed app and set `CLAMAV_HOST`/`CLAMAV_PORT` in that environment's real config. (2) Not actionable further without inventing a real malware sample (never appropriate to add to a test suite) -- accepted as a permanent testing limitation, not a todo. | (1) Medium -- the code is ready and verified, but zero real uploads are scanned in any environment until a production ClamAV target exists. (2) Low -- the underlying multi-chunk streaming mechanism is verified with clean content; the untested slice is specifically "does a real malware signature split across a chunk boundary still match," which is standard TCP-stream reassembly behaviour clamd's own INSTREAM protocol is documented to handle, not something specific to this provider. |
| TD-44 | **New, 2026-08-06** (Stage 7, TOTP MFA): the shipped MFA feature (enrollment, login-time challenge, unenrollment -- `apps/admin/app/api/v1/auth/mfa/*`, verified end-to-end via a real Playwright test generating actual RFC-6238-correct TOTP codes) deliberately does not include: (1) backup/recovery codes -- a user who loses their authenticator app and has no other enrolled factor has no self-service way back into their account (today's only recourse would be a support/admin-assisted path, which itself doesn't exist yet either). (2) Any organization-level policy to require MFA for some or all members -- enrollment is entirely self-service and optional, there is no "principal requires MFA for this org" setting. (3) No "remember this device for N days" convenience -- every sign-in for an MFA-enrolled account requires a fresh code, with no reduced-friction return-visit path. | Each is a genuinely separate feature (backup codes need their own generation/storage/consumption design; an org-level policy needs a schema decision; device-remembering needs its own secure-cookie/trust mechanism) beyond "TOTP enrollment and challenge work end-to-end," which was this pass's scope. | Not yet assigned -- candidates for a follow-up MFA-hardening pass once real users are actually enrolling (currently zero, this shipped same-session). Backup codes are the highest-value of the three (the others are convenience/policy, this one is "how does a real locked-out user recover at all"). | Medium -- MFA itself is optional/self-service today, so nobody can be locked out by a policy that doesn't exist; the real risk is the first real user who enables MFA, loses their device, and has no recovery path -- worth closing before actively encouraging enrollment in the product. |

| TD-45 | **New, 2026-08-08** (Stage 3 MFA-fix follow-up, found during Playwright verification): `apps/admin/app/reset-password/ResetPasswordForm.tsx:64` calls `supabase.auth.getSession()` as a 1.5s fallback timer to decide whether to render the "set new password" form or an "invalid link" message, triggering Supabase's own console warning about trusting unverified storage-derived session data. Confirmed via grep this is the *only* `getSession()` call site in `apps/admin`, is unmodified by and unrelated to the Stage 3 MFA fix, and is low-severity: it only gates a UI affordance (which screen renders), not an authorization decision — the actual password change goes through `supabase.auth.updateUser()`, which Supabase's own server independently authenticates regardless of what this client-side check decided. | Pre-existing code, unrelated to the MFA-bypass fix that surfaced it; broadening that security patch to also refactor this would have mixed an unrelated change into a scoped security commit. | Not yet assigned — swap for `supabase.auth.getUser()` (or drop the fallback timer, since the `onAuthStateChange`/`exchangeCodeForSession` paths above it already cover the real cases) next time this file is touched. | Low — worst case today is a user seeing the wrong of two UI states (form vs. "invalid link") for up to 1.5s; the security boundary itself (`updateUser()`) is unaffected. |

## How this register is used

Every item above is either (a) already assigned to a specific `TASKS.md` milestone, in which case it's paid down as a normal part of that milestone's exit criteria, or (b) flagged as "not yet assigned," in which case it must get a milestone assignment before the phase of work it would block is considered complete. Nothing on this list is meant to persist indefinitely — a technical debt item with no paydown plan is really just an unaddressed risk wearing a more comfortable name, and should be re-filed in `RISK_REGISTER.md` if it's ever found drifting that way.
