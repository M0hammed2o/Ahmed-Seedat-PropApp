<?xml version="1.0" encoding="utf-8"?>
<!-- Real launcher background (Android V1 last local blocker pass, WORKLOG.md this date):
     #000615, the exact navy sampled from the real logo's own corner
     (apps/admin/branding/proplyst-logo.png, apps/admin/scripts/make-icons.mjs's own BG
     constant), matching every PWA/favicon icon already generated from this logo. Replaces the
     previous flat-teal placeholder now that ic_launcher_foreground.png (drawable-nodpi/) is the
     real cropped mark, not a placeholder rounded square; the mark's white/light elements need
     a dark ground to read correctly, which the old teal fill was never designed for. -->
<vector xmlns:android="http://schemas.android.com/apk/res/android"
    android:width="108dp"
    android:height="108dp"
    android:viewportWidth="108"
    android:viewportHeight="108">
    <path
        android:fillColor="#000615"
        android:pathData="M0,0h108v108h-108z" />
</vector>
