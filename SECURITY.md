# Security

Security is release-blocking (per project mandate). This document is PropertyVault's production security architecture — the multi-tenant rebuild of the PropVault-era document. PRIVACY_AND_COMPLIANCE.md covers the legal/compliance layer built on top of these technical controls; ACCOUNTING.md covers posting rules; DATABASE.md, PERMISSIONS.md, ARCHITECTURE.md, API_SPEC.md, and MOBILE_ARCHITECTURE_DECISION.md are the design documents this file builds on rather than repeats.

## Demo-mode auth bypass — carried forward from PropVault, resolved 2026-07-30

`EXISTING_CODEBASE_AUDIT.md` §3 originally confirmed the PropVault-era issue was present in code exactly as this document described it: `NEXT_PUBLIC_DEMO_MODE` (admin) and `EXPO_PUBLIC_DEMO_MODE` (mobile) **defaulted to ON when the environment variable was unset**. On the admin side this made `/login` accept any email/password, skipped the session check in what was then `middleware.ts` entirely, and made `getAdminSession()` (`apps/admin/lib/auth.ts`) fabricate a `super_admin` session without touching Supabase. On mobile, `AuthProvider.tsx` resolved `signIn`/`signUp` against a locally-constructed fake `Session` instead of calling Supabase Auth.

**Fixed and build-verified 2026-07-30** (`TECHNICAL_DEBT_REGISTER.md` TD-04, `TASKS.md` M2, `RISK_REGISTER.md` R-01 downgraded Critical→Medium) — kept below as the historical record of the fix, corrected from this document's own earlier stale wording (still describing the bypass as "unresolved" after the fix had already landed; caught during the commercial-launch execution plan's Stage 0 pass, WORKLOG.md this date). Two other stale details in the original wording, also corrected here: the auth-gate logic lives in `apps/admin/proxy.ts` (Next.js middleware was renamed to `proxy.ts` — see `apps/admin/proxy.ts`'s own comments), not `apps/web/middleware.ts` — `apps/web` never existed in this codebase, the app has always been `apps/admin`.

**Decision that shipped: unset means OFF, always.** `isDemoMode()` (`packages/config/src/demoMode.ts`) no longer defaults to ON when unset — an unset `NEXT_PUBLIC_DEMO_MODE`/`EXPO_PUBLIC_DEMO_MODE` resolves to `false`. Demo mode is something a developer must actively opt into (`NEXT_PUBLIC_DEMO_MODE=true`), never something an operator must remember to opt out of. On top of that inversion, a second, independent gate means demo mode cannot be switched on by app-level env alone:

- `ALLOW_DEMO_MODE=true` must also be set at the **infra level** (Vercel/hosting-platform project environment variable, not `.env` shipped in the app bundle) for any environment other than local dev, and this variable is never set in the production project's environment at all — not "set to false," genuinely absent, so there is no single flag flip that re-enables the bypass in production.
- `isDemoMode()` becomes `NEXT_PUBLIC_DEMO_MODE === 'true' && ALLOW_DEMO_MODE === 'true'` (mobile: the Expo-public equivalent, gated the same way through `app.config` per build profile) — both conditions required, both default absent/false.
- **Not yet built**: a CI build-time assertion for the `production` deploy target (fail the build if `ALLOW_DEMO_MODE` is set in that environment's configuration) — this is the one piece of TD-04 still open, and it's blocked on a real production deploy pipeline existing to assert against in the first place (`TASKS.md` M24, itself not started — see the commercial-launch execution plan's Phase 11). `.github/workflows/ci.yml`'s existing "Build admin" step deliberately sets both demo-mode flags to `true` — that is CI verifying the build compiles without a live Supabase project, not the production-target assertion described here; they are not the same check and must not be conflated once a deploy workflow exists.

Every dashboard/screen-level "Demo data" badge and boot-time console warning are retained as defense-in-depth, but per the hard rule that UI-layer signals are never the only enforcement (`PERMISSIONS.md` §5), they are not what makes this safe — the inverted default and the infra-level second gate are.

**Before any deployment reachable by anyone other than the person driving a demo:** confirm `ALLOW_DEMO_MODE` is absent from the target environment's infra config, confirm real `SUPABASE_SERVICE_ROLE_KEY`/`NEXT_PUBLIC_SUPABASE_URL`/`NEXT_PUBLIC_SUPABASE_ANON_KEY` values are set, and confirm the CI production-target assertion above is green.

## Multi-tenant trust boundaries

```
Client (web / iOS / Android)  →  API (Route Handlers / Edge Functions)  →  Postgres (RLS)
```

Per `ARCHITECTURE.md` § Multi-tenancy model and `API_SPEC.md` §0/§10, every request crosses two independent enforcement layers, not one:

1. **API layer (fail-fast)**: Supabase Auth verifies `auth.uid()`; the API re-derives which `org_id`(s) the caller belongs to from `organization_members`/`tenants`/`owners` — **never from a client-supplied `org_id`** (`API_SPEC.md` §0: "No endpoint accepts a client-supplied `org_id` as authoritative"). Role checks against `PERMISSIONS.md`'s org-role table happen here too, returning 403 before any query runs.
2. **RLS (ground truth)**: every table's policy independently re-derives the same scoping from `organization_members`/`tenants`/`owners` (`DATABASE.md` §12), assuming the API layer could be buggy, bypassed, or (in the case of a compromised client) simply lying. A client cannot forge its way to another organization's data by tampering with a request body, because no policy anywhere trusts a client-supplied identifier — every `select`/`insert`/`update`/`delete` predicate resolves org/role membership server-side from tables the client cannot write to arbitrarily.

**What stops a compromised or malicious client from reading another organization's data**: nothing the client sends is trusted as an authorization claim. A forged `org_id` in a request body is simply not what any RLS predicate checks — the predicate checks `org_id in (select org_id from organization_members where user_id = auth.uid() and status = 'active')` (and the `tenants.user_id`/`owners.user_id` equivalents for portal-scoped tables), which is a server-side lookup keyed only on the caller's authenticated `auth.uid()`. Even a fully compromised client (stolen JWT included) is bounded by whatever orgs that specific `auth.uid()` actually belongs to; it cannot expand its own reach by changing what it asks for. This mirrors the retained pattern from PropVault's single-owner RLS (`owner_user_id = auth.uid()`, `EXISTING_CODEBASE_AUDIT.md` §3) generalized to org/tenant/owner membership.

The one deliberate exception is the service-role client used by Super Admin route handlers and the accounting posting service, which bypasses RLS by design — see Secrets below and `PERMISSIONS.md` §5/§6 for how that elevated path is itself gated (server-side role check + audit write, never exposed to any client bundle).

## Secrets / PII handling — encrypted-secrets pointer pattern

Per `DATABASE.md` §11, banking details (`owners.banking_ref`, `bank_accounts.account_number_ref`) and ID numbers (`tenants.id_number_ref`) are never stored as plaintext columns on their business table. Each is a `uuid` pointer into a dedicated `encrypted_secrets` table (`id`, `ciphertext bytea`, `created_at`).

- **Encryption happens at the application layer**, before the row is ever inserted — not `pgcrypto` column-level encryption alone, and not reliance on Postgres-at-rest encryption as the only control. This means a full-table dump, a leaked `SELECT *`, a misconfigured RLS policy, or a compromised read replica exposes ciphertext, not a bank account number or an ID number.
- **Key management stays outside the database's trust boundary.** The key that decrypts `encrypted_secrets.ciphertext` is never stored in the same place as the ciphertext, and is never a value the application's Postgres role can read directly — it lives in a KMS (or hosting-provider equivalent secret manager) that the decrypting service calls at read time, so a Postgres-level compromise (leaked service-role key, SQL injection reaching this table, a rogue admin with database console access) is insufficient on its own to recover plaintext.
- Decryption is scoped to the specific server-side code paths that legitimately need the plaintext (e.g. generating an owner payout instruction, verifying a tenant's ID during screening) — never returned to a client, never logged, never included in an `audit_events.before`/`after` snapshot (those record which secret was referenced, not its value).
- This satisfies the hard rule that secrets/PII never appear unredacted in code, logs, or replies, extended from "don't log it" to "don't even store it in a form a database-level compromise can read."

## Auth

Supabase Auth is retained unchanged as the identity provider (`ARCHITECTURE.md` § Retained from PropVault) across web and both native apps — same `auth.users` table, same sign-in/up/out/password-reset/email-verification flows, now feeding into org/owner/tenant role resolution instead of a single-owner assumption.

- **Session handling**: web retains the existing Supabase-Auth-via-`@supabase/ssr` pattern; native apps (`MOBILE_ARCHITECTURE_DECISION.md` §7/§8) integrate `supabase-swift`/`supabase-kt` directly rather than through a JS bridge, with the same unauthenticated → verifying → authenticated → locked state machine `apps/mobile`'s `AuthProvider.tsx` already validated as a flow (spec reused, code not — nothing in a JS runtime links into a native binary).
- **Token storage**: iOS uses Keychain (`LocalAuthentication`/Keychain per §7), Android uses Keystore-backed `EncryptedSharedPreferences`/`DataStore` — both replace `expo-secure-store` with the native-first equivalent, never falling back to plain storage.
- **Biometric re-entry is a re-auth gate, not just an app-open convenience.** Face ID/Touch ID (iOS) and `BiometricPrompt` (Android) unlock the app on cold start/background-return, but sensitive actions re-prompt biometrics independently of session validity: initiating/confirming an owner payout, approving a bank-transaction match, reversing a journal entry, viewing a tenant's ID number or bank details, and ending a Super Admin support-mode session all require a fresh biometric (or device passcode fallback) check immediately before the action executes — a valid, unexpired Supabase session is necessary but not sufficient for these. This list is enforced client-side as UX (the prompt itself) but the underlying action is additionally gated server-side by the normal role checks in `PERMISSIONS.md`/`API_SPEC.md` §10, so a jailbroken/tampered client that skips the biometric prompt still cannot execute the action without the legitimate role/session — biometrics narrow the window for "someone picked up an unlocked, already-authenticated phone," they are not the authorization boundary itself.

## Tenant activation & OAuth identity linking (PRODUCT DECISION 1/2, 2026-08-03 — full design in `AUTHENTICATION.md`)

`AUTHENTICATION.md` is the authoritative design document for both of these; this section states the security properties they must satisfy, matching the pattern the WhatsApp/email webhook section below already follows (cross-reference, not a duplicate).

**Tenant invitation / activation-code security model** (`tenant_invitations`, migration `20260101000059`):

- **Nothing is ever stored in recoverable form.** Both the invitation token and the optional short code are hashed at rest (`digest(...,'sha256')`, `pgcrypto` — the `extensions` schema, not `public`, in this project) in `token_hash`/`short_code_hash`. `create_tenant_invitation()`/`regenerate_tenant_invitation()` return the plaintext exactly once, directly in the API response body — never logged, never re-derivable afterward. This is why "resend" and "regenerate" are the same underlying operation in the staff UI (`TenantInvitationPanel.tsx`): a literal resend of the original secret is cryptographically impossible, not merely unbuilt.
- **Failed-attempt lockout**: 5 wrong short-code attempts per invitation (`failed_attempt_count`, tracked on the invitation row) before it locks out (`error_code = locked_out`). Layered with, not a replacement for, rate limiting below.
- **Rate limiting**: `POST /api/v1/tenants/:id/invitations` (creation) is limited to 20/minute per staff user via the existing `check_rate_limit()` RPC (`TECHNICAL_DEBT_REGISTER.md` TD-10); `POST /api/v1/tenant-invitations/accept` is dual-gated — per authenticated user **and** per source IP — since an unauthenticated or newly-registered attacker could otherwise script code guesses against many invitations from one IP.
- **No PII disclosure on failure.** `accept_tenant_invitation()` returns one of a fixed set of generic `error_code` values (`not_found`, `invalid_code`, `locked_out`, `revoked`, `expired`, `already_used`, `org_inactive`, `already_linked`, `email_mismatch`) — never a message that would let a caller distinguish "this code doesn't exist" from "this code exists but belongs to someone else." A short code alone is never sufficient to identify or unlock a row: acceptance by short code requires the code **and** the tenant's on-file email together, so a wrong-code guess against a wrong email doesn't even identify which invitation to lock out.
- **Masked destination display**: `tenant_invitations.destination_hint` stores a masked form of the delivery address/number (e.g. partially redacted email/phone) for the staff UI's "sent to..." confirmation — the full destination is never re-displayed from this table after creation.
- **RLS is staff-only, with zero self-select for the invitee before acceptance.** `tenant_invitations_select_staff`/`_insert_staff`/`_update_staff` all require `has_org_role(org_id, 'agent')` — there is no policy letting the invitee (who isn't yet linked to any `auth.users` row, or is authenticating for the first time) read their own invitation row directly; they only ever interact with it indirectly through `accept_tenant_invitation()` (`security definer`, its own generic-error-code discipline above), never a direct table read.
- **One active invitation per tenant, enforced at the database level**, not just in application logic: a partial unique index (`tenant_invitations_one_active_per_tenant` on `tenant_id` where `accepted_at is null and revoked_at is null`) makes it structurally impossible to have two live, unaccepted invitations outstanding for the same tenant, regardless of which code path tries to create one.
- **Atomicity note, found by adversarial testing, not assumed**: `accept_tenant_invitation()` deliberately never raises a Postgres exception for any of the expected failure cases above — an earlier version did, and pgTAP proved this silently rolled back the `failed_attempt_count` increment made earlier in the same function call (PL/pgSQL rolls back every write in an invocation the instant it raises, not just the failing statement). This is recorded in full in `DECISIONS.md` 2026-08-03 as a general PL/pgSQL lesson, not just a local bug fix, because the same silent-rollback failure mode could defeat any other lockout counter built the same way.

**OAuth identity-linking safety model**: `LinkedAccountsPanel.tsx` uses Supabase Auth's native `linkIdentity()`/`unlinkIdentity()`/`getUserIdentities()` as-is — no custom email-based account-merge logic was written. This matters because `linkIdentity()` structurally requires the caller to already hold a valid, authenticated session for the account being linked _to_ before a second provider can be attached to it; there is no code path in this system that merges two identities based on a client-supplied or provider-supplied email match alone, which is the exact class of account-takeover risk ("attacker signs up with victim's email via a different provider, gets silently merged into victim's existing account") a bespoke merge implementation would have had to defend against and could get wrong. Automatic linking (Supabase auto-merging a new OAuth sign-in into an existing password account on email match) is explicitly not enabled — see `AUTHENTICATION.md` §4/§6 for the full rule and the required "Enable Manual Linking" / "Automatic linking stays off" Supabase Dashboard settings this depends on. Google/Apple OAuth are implemented but have never completed a real provider round trip (no live credentials in this environment — `TECHNICAL_DEBT_REGISTER.md` TD-29); the code path is structurally identical to the live-verified email/password and password-reset paths (`AUTHENTICATION.md` §7).

## Super Admin support-mode (audited, time-boxed, never silent)

Per `PERMISSIONS.md` §6 and `DATABASE.md` §1 (`support_access_sessions`): platform staff never get silent, unbounded impersonation of a client organization.

- Entering support mode requires a `reason` (not null, logged) and creates a `support_access_sessions` row (`platform_admin_id`, `org_id`, `reason`, `started_at`) before any elevated access is granted.
- While active, the session is scoped to `viewer`-equivalent read access to the target org by default — full write/`principal`-equivalent access is never implicitly granted. Any write action during the session requires a separate, explicitly logged escalation (`actions_taken jsonb[]`, append-only) — there is no single toggle that grants blanket impersonation.
- **Banner-visible, never silent**: while a support session is active, the platform admin's UI carries a persistent, unmissable banner identifying that they are in support mode against a specific org — this is a hard requirement, not a nice-to-have, because the master prompt's Super Admin isolation requirement (`ARCHITECTURE.md` § Why one web app, not two) depends on client staff and platform staff never being confusable, including to the platform staff member themselves mid-session.
- Sessions end explicitly (`ended_at` set by the session closing itself) or by automatic timeout — `ended_at` is the only field on `support_access_sessions` ever updated after insert, everything else is append-only, consistent with the audit-immutability posture below.
- `is_platform_admin(min_role)` gates entry server-side; a client-supplied role claim is never trusted (`PERMISSIONS.md` §1). Currently `is_admin()` — see `DATABASE.md` §1's naming note; the Milestone 13 rename hasn't landed yet.

## WhatsApp / email webhook security

Both inbound channels are external, unauthenticated-by-default surfaces (anyone can POST to a public webhook URL) and are treated accordingly: **no write happens before signature/HMAC verification succeeds.**

- `POST /api/v1/webhooks/whatsapp` and `POST /api/v1/webhooks/email` (`API_SPEC.md` §8) verify the provider's signature/HMAC against a server-held secret before the payload is parsed or any `whatsapp_messages`/`email_messages` row is written — a request that fails verification is rejected (401) and never reaches business logic, matching the existing RevenueCat/OCR webhook pattern this codebase already has (`EXISTING_CODEBASE_AUDIT.md` §3 confirms the pattern's shape; the routes themselves are net-new for this rebuild).
- WhatsApp inbound additionally must not resolve a conversation/entity from untrusted sender-text alone — number resolution is looked up server-side against known records, never inferred from message content (see `WHATSAPP.md` for the full number-resolution rule, §13 of the master prompt).
- Both webhook paths are idempotent by provider message id, so a redelivered valid request is a no-op rather than a duplicate send/write.
- Full protocol-level detail (exact header names, retry/backoff behavior, template-message constraints) lives in `WHATSAPP.md` and `EMAIL.md` — this section states the requirement those documents must satisfy, not a substitute for them.

## OWASP-relevant controls

- **Injection**: all database access goes through the Supabase client library or parameterized queries inside Postgres functions/Edge Functions — no string-concatenated SQL anywhere. All external input (mobile forms, web forms, webhook payloads) is parsed through a `packages/validation` Zod schema before touching the database, client-side for UX and **again server-side**, since client-side validation is a UX affordance, not a security control (retained from the existing pattern).
- **XSS**: React's default JSX escaping is the baseline control on web; user-supplied text (tenant names, maintenance descriptions, announcement bodies) is never rendered via `dangerouslySetInnerHTML` or an unescaped native-WebView bridge. A restrictive `Content-Security-Policy` is defense-in-depth on top, not a substitute for escaping — set per-request in `apps/admin/proxy.ts` with a fresh nonce (`script-src 'self' 'nonce-<value>' 'strict-dynamic'`), not statically in `next.config.ts` (moved there 2026-08-02, DECISIONS.md, after the static version was found to block Next.js's own inline hydration scripts in every real browser — every page had been silently rendering as a frozen loading skeleton since this project's first commit; `curl`-based smoke testing never caught it because `curl` doesn't execute JavaScript or enforce CSP).
- **CSRF** (verified live, closed 2026-08-06 — Stage 7 commercial-launch execution plan): session cookies (web) are `SameSite=Lax`-scoped — confirmed against the actual installed `@supabase/ssr` version's `DEFAULT_COOKIE_OPTIONS`, not just claimed, since this codebase never overrides it; a forged cross-site POST/PUT/PATCH/DELETE cannot carry the cookie in any modern browser. Native apps and any cross-origin API consumption use bearer-token auth (`Authorization: Bearer <supabase-jwt>` per `API_SPEC.md` §0) rather than ambient cookie auth, so there's no implicit-credential request for a forged cross-site request to ride on. `apps/admin/proxy.ts`'s `isTrustedOrigin()` adds Origin/Referer verification as deliberate defense-in-depth on every mutating (`POST`/`PUT`/`PATCH`/`DELETE`) request — exempting requests that already carry a bearer token (a different trust model CSRF doesn't apply to) and the PayFast ITN webhook (`/api/v1/billing/webhook`, signature-authenticated, not cookie-authenticated) by exact pathname.
- **Rate limiting** (auth endpoints closed 2026-08-06 — Stage 7, `TECHNICAL_DEBT_REGISTER.md` TD-31, resolved): `/api/v1/auth/{signin,signup,password-reset}` are now real server routes (`LoginForm.tsx`/`RegisterForm.tsx`/`ForgotPasswordForm.tsx` call them instead of the Supabase Auth SDK directly) enforcing `RATE_LIMITS` via `check_rate_limit()` (migration `20260101000058`), keyed on IP and, for signin, email independently. Verified live with a real Playwright test tripping the limit on the 11th rapid attempt. The webhook endpoints were already rate-limited before this pass.
- **Two-factor authentication** (TOTP, shipped 2026-08-06 — Stage 7): self-service enrollment/challenge/removal via Supabase Auth's own native MFA API (`apps/admin/app/api/v1/auth/mfa/*`), never a custom TOTP implementation. Verified end-to-end live: a real Playwright test generates an actual RFC 6238-correct code (implementation cross-checked against RFC 4226's own published test vectors before being trusted) and completes a full enroll → sign-out → challenge-and-verify round trip against real Supabase Auth. Optional/self-service only — no org-level enforcement policy and no backup/recovery codes exist yet if an enrolled user loses their device (`TECHNICAL_DEBT_REGISTER.md` TD-44).
- **Dependency auditing** (closed 2026-08-06 — Stage 7): `.github/workflows/ci.yml`'s `verify` job runs `pnpm audit --audit-level critical --prod` on every PR — scoped to production runtime dependencies (not devDependency build tooling) and gated on `critical` only, so a real, production-reachable critical vulnerability blocks a merge without the alert-fatigue of gating on findings nobody here can fix (transitive devDependency chains needing an upstream eslint/vite/vitest release). `TECHNICAL_DEBT_REGISTER.md` TD-42 tracks the known non-blocking findings.
- **IDOR**: every lookup by id is scoped through RLS (customer/owner/tenant side) or an explicit org-membership check (Super Admin/service-role side); `API_SPEC.md` §0 also closes the org-enumeration side channel specifically — a resource in another org 404s, never 403, so a client probing ids can't distinguish "doesn't exist" from "exists but isn't yours."
- **Secrets hygiene**: `.env.example` files contain only placeholders, never real keys; `SUPABASE_SERVICE_ROLE_KEY` is server-environment/Edge-Function-secret only, never `NEXT_PUBLIC_*`, never in a native app bundle, never logged — enforced at build time by the `server-only` package so an accidental client-component import fails the build rather than leaking at runtime (`ARCHITECTURE.md` § Environments).

## Accounting immutability as a security property

`ACCOUNTING.md` §1 and `DATABASE.md` §9/§12 establish that `journal_entries` and `journal_lines` are insert-only — corrections happen via reversing/adjusting entries, never an `UPDATE`. This is stated here, not just in the accounting/data-integrity docs, because it is also a security control: **`journal_entries`/`journal_lines`/`audit_events` carry no `update`/`delete` RLS policy at all, for any role, including service-role-adjacent paths** (`DATABASE.md` §12). That means a compromised service-role credential, a bug in the posting service, or a malicious insider with database console access cannot silently rewrite financial history — the only way to change what the ledger says happened is to post a new, equally-permanent, fully-audited entry that says so. Combined with the rule that the posting service (`ARCHITECTURE.md` § Business logic placement) is the _only_ code path permitted to write these tables at all, this makes "the ledger cannot be quietly edited" an enforced property of the system, not a convention that trusted code is expected to honor.

## Uploaded-file safety (added by Production Readiness Review, 2026-07-30 — previously unaddressed: MIME-type validation alone does not stop a malicious file)

Tenants, owners, and staff all upload files (documents, maintenance photos, lease PDFs) that get stored and later served to _other_ users (a landlord opening a tenant-submitted photo, an owner opening a statement PDF) — this is a real attack surface a MIME-type check alone does not close, since a MIME type is client-asserted metadata, not a guarantee about file content.

- **Malware/antivirus scanning** (closed 2026-08-06 — Stage 7, `TECHNICAL_DEBT_REGISTER.md` TD-43): a real ClamAV integration (`apps/admin/lib/providers/malwareScan.ts`, verified against a genuine local ClamAV instance including real EICAR-test-file detection) runs on every upload to `POST /api/v1/documents` and `POST /api/v1/lease-templates`, after MIME validation and before the file ever reaches Storage. A file that fails the scan is rejected outright (`422 malware_detected`) and never reaches Storage in a downloadable state; a scan the configured scanner itself can't complete (connection failure, timeout) also refuses the upload (`503`, fails closed) rather than silently letting it through. **Still genuinely open**: no real ClamAV instance is deployed anywhere in production yet — this is real, tested code with nowhere production-real to run until a Stage 8 hosting target is chosen (same TD-43).
- **File-type verification by content, not just extension/MIME header** — a magic-byte/content-sniffing check (confirming a `.pdf` upload's first bytes actually match the PDF format, etc.) in addition to the existing `ALLOWED_MIME_TYPES` check, closing the gap where a renamed executable could pass a naive MIME-type gate. **Still open** — not addressed by the Stage 7 malware-scanning work, which is a separate, real control, not a substitute for this one.
- **Size limits** (closed, verified 2026-08-06 while wiring the scan step above): both upload routes already enforce a 25MB `MAX_FILE_SIZE_BYTES` check server-side, not just client-side UX — this doc previously and incorrectly implied it was unaddressed; corrected here after checking the live route code.

## Storage security (retained pattern, re-scoped)

- Buckets remain private (`public = false`); no object is ever served via a public URL.
- Path convention becomes `{org_id}/{...}` (`DATABASE.md` §6) rather than `{user_id}/...` — Storage RLS requires `(storage.foldername(name))[1]` to resolve to an org the caller belongs to, so guessing another org's path still fails at the policy layer, not just by obscurity.
- Client access is always via a short-lived signed URL requested through a call that checks the `documents` row's org/entity ownership before signing — never generated from a client-supplied path alone.

## What still needs security review before production (not yet done)

- ~~The demo-mode fix above is designed in this document but not yet implemented in code~~ — **stale, corrected this date**: it shipped 2026-07-30 (see the section above). The one genuinely remaining piece is the CI production-target assertion, still open, tracked there.
- `WHATSAPP.md`/`EMAIL.md` webhook signature schemes: `apps/admin/lib/providers/whatsapp.ts`'s `verifyWebhookSignature()` currently always returns `true` with an explicit code comment that a real provider integration must replace it with genuine HMAC verification before this is ever wired to a live webhook route — both providers are mock-only (no real vendor selected yet), so this stays open, not close-to-done.
- ~~Distributed rate limiting has a scaffolded pattern but no wired backing store~~ — **stale, corrected this date**: `supabase/migrations/20260101000058_rate_limiting.sql` is a real, working mechanism (`rate_limit_buckets` + `check_rate_limit()`), wired into 4 endpoints (`POST /api/v1/billing/webhook`, both invite-accept routes, tenant-invitation create). The actual remaining gap is narrower and different in kind: **signin/signup/password-reset are unprotected**, and closing that is not a matter of wiring the existing mechanism to 3 more routes — `LoginForm.tsx`/`RegisterForm.tsx`/`ForgotPasswordForm.tsx` call the Supabase Auth (GoTrue) client SDK directly from the browser with no Next.js route handler in between, so `rateLimitOrRespond()` (an RPC called _from_ a route handler) has no hook point on these calls at all. Investigated this date: self-hosted GoTrue's `config.toml` `[auth.rate_limit]` section did not produce the expected effect in this environment either (`sign_in_sign_ups`/`email_sent` overrides tested live against the local stack — `GOTRUE_RATE_LIMIT_EMAIL_SENT` stayed at its default and no `GOTRUE_RATE_LIMIT_SIGN_IN_SIGN_UPS` env var was produced at all; not asserted to work elsewhere without re-verification). Closing this gap needs a real decision — route these three calls through a server-side proxy endpoint so the existing app-layer mechanism can reach them, or enforce it at the production edge/CDN layer in front of Kong — not a quick wiring pass.
- Dependency-vulnerability scanning in CI (beyond the existing secret-scan step) — confirmed still absent from `.github/workflows/ci.yml` this date.
- Formal penetration test / third-party review.
- CSP tightened against the actual final asset hosts once chosen.
- Legal review of retention periods (see `PRIVACY_AND_COMPLIANCE.md`).
- RLS test cases (org A cannot read/write org B's rows, tenant cannot escalate to org-staff data, owner cannot read another owner's statement) are specified per `PERMISSIONS.md`/`DATABASE.md` but require a live Postgres instance (`supabase start`) to execute — not run as part of producing this document.
