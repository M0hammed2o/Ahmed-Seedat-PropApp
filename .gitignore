# Dependencies
node_modules/
.pnpm-store/

# Build outputs
dist/
build/
.next/
.expo/
.expo-shared/
web-build/
*.tsbuildinfo
.turbo/

# Env files (never commit real secrets)
.env
.env.local
.env.*.local
!.env.example

# Supabase local
supabase/.branches/
supabase/.temp/

# Production database backups (schema + real customer data) -- never committed
.production-backups/

# Testing / coverage
coverage/
test-results/
playwright-report/
blob-report/
playwright/.cache/

# OS / editor
.DS_Store
Thumbs.db
*.log
npm-debug.log*
pnpm-debug.log*

# EAS / native (apps/mobile's Expo prebuild output -- apps/android has its own .gitignore)
*.p8
*.p12
*.mobileprovision
*.jks
*.keystore
ios/Pods/
apps/mobile/android/.gradle/
apps/mobile/android/app/build/

# IDE
.idea/

# Development reference material only -- never a runtime dependency, never committed
# (UI_INTEGRATION_PLAN.md: audited and adapted from, not imported)
/reference/

# Local agent tooling state (not project source)
.claude/
